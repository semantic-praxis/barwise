{"_type":"issue","id":"barwise-5t9.1","title":"Value constraints: support ranges and open-ended bounds (importer drops them)","description":"BUG + GAP (highest value). The metamodel's ValueConstraint is enumerated\nvalues only -- Constraint.ts:81-88: `readonly values: readonly string[]`,\nwith the doc comment \"Currently supports enumerated values.\"\n\nConsequence in the importer: NormaXmlParser.ts:262 keeps a value range\nONLY when `minVal === maxVal`:\n\n    if (minVal !== undefined \u0026\u0026 minVal === maxVal) { values.push(minVal); }\n\nSo every genuine ORM 2 value range -- `[1..10]`, `\u003e= 18`, `(0..100)`,\nmixed sets like `{1, 3, 5..9}` -- is **silently dropped on import**. Same\npattern at NormaXmlParser.ts:497-508 for role-level value constraints.\n\nORM 2 / NORMA value constraints support enumerations, closed ranges,\nopen/half-open ranges, and unions of these, with inclusive/exclusive\nbounds. This is one of the most common conceptual constraints in real\nmodels (ages, ratings, percentages, codes), so the loss is material.\n\nWork: (a) extend ValueConstraint to a list of items where each item is a\nliteral OR a range with optional lower/upper bound and inclusivity flags;\n(b) update NormaXmlParser to carry ranges through; (c) update validation\nand verbalization; (d) round-trip fixture test. Keep `values: string[]`\nworking as a sugar/back-compat for pure enumerations.\n","acceptance_criteria":"ValueConstraint can express inclusive/exclusive ranges, open-ended bounds, and mixed enumerations+ranges; NormaXmlParser imports MinValue!=MaxValue ranges and open bounds without loss; round-trip test over a NORMA value-range fixture; validation + verbalization handle ranges.","status":"closed","priority":1,"issue_type":"feature","assignee":"Claude","owner":"noreply@anthropic.com","created_at":"2026-06-17T01:58:17Z","created_by":"Claude","updated_at":"2026-06-17T13:44:04Z","started_at":"2026-06-17T13:19:11Z","closed_at":"2026-06-17T13:44:04Z","close_reason":"Value ranges and open/exclusive bounds implemented: metamodel (ValueRange + ranges on ValueConstraint/ValueConstraintDef), serializer round-trip, JSON Schema, verbalization, population validation, and the NORMA importer (parser+mapper) which no longer drops MinValue!=MaxValue ranges. Tests added across core and formats; full monorepo build/test/lint green.","labels":["constraints","data-loss","interop","metamodel","norma","orm2"],"dependencies":[{"issue_id":"barwise-5t9.1","depends_on_id":"barwise-5t9","type":"parent-child","created_at":"2026-06-17T01:58:17Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-5t9","title":"ORM 2 standard coverage audit: .orm.yaml/NORMA importer gaps","description":"Audit of conceptual ORM 2 coverage in the native `.orm.yaml` metamodel\n(packages/core/src/model) and the NORMA XML importer\n(packages/formats/src/Norma*), measured against the ORM 2 standard as\nrealized by NORMA. Goal: make barwise \"play well with others\" by closing\nthe genuine semantic divergences from NORMA before any export/round-trip\nwork.\n\nMethod: read each metamodel type and the NORMA parser/mapper, then\nclassify every candidate gap as REAL (file a child) or NOT-A-GAP\n(already covered). Diagram/layout interop is intentionally out of scope\nhere (tracked separately by barwise-e3g) -- this epic is about *conceptual*\nORM 2 semantics only.\n\nNOT gaps (verified covered, do not file):\n- Preferred identifier: activated in NormaToOrmMapper.ts:375 (is_preferred).\n- Unary/n-ary fact types: supported (FactType.ts:22-23, arity getter).\n- Subtype partition (exclusive/exhaustive): SubtypeFact carries both flags.\n- Implied mandatory constraints: intentionally filtered (NORMA auto-generated).\n- Conceptual data types: 20+ NORMA types normalized to portable names.\n\nREAL gaps (each a child of this epic):\n1. Value ranges + open-ended value constraints (HIGH; also a silent\n   import data-loss bug).\n2. Derived fact types / derivation rules.\n3. Deontic vs alethic constraint modality.\n4. Object cardinality constraints (distinct from role frequency).\n5. Independent (reference-independent) object types.\n6. Set-comparison/ring constraints over join paths.\n7. Default values on value types / roles.\n8. Multi-role (role-sequence) frequency constraints.\n\nChildren 1-2 are the priority. Once the gap list is agreed, the\nfollow-on work is (a) a NORMA-vs-yaml comparison doc and (b) a spec for\nNORMA export / round-trip; both are tracked separately.\n","notes":"Authoritative XSD census (2026-06-17) against ormsolutions/NORMA ORMModel/ObjectModel/ORM2Core.xsd confirmed the original (import-grounded) audit and surfaced constructs the code-grounded pass could not see (elements the importer never references). New children .9-.13 added. Minor verbalization/metadata not separately filed: IsPersonal (pronoun hint), RecognizedPhrases (verbalization phrases), IsExternal (externally-defined elements). Note: ORM2Core models value constraints as ValueTypeValueRestriction/RoleValueRestriction (not a *Constraint), which the importer already handles -- not a gap.\nCited deep-research pass (2026-06-17) compared the ORM2 book (Halpin \u0026 Morgan 2008; Halpin papers) against NORMA. Conclusion: ORM2Core.xsd is a faithful, near-superset realization of the book -- the gaps vs the book are TOOLING-completeness, not schema, in two spots: deontic modality (single-operator only, see .3) and formal derivation rules (partial, see .2). Methodology (CSDP, schema transformations, Rmap) is algorithms over models, not metamodel constructs, so 'missing' does not apply. Corrections to earlier chat claims: 'deep support for conceptual outer joins' was UNVERIFIED (inner joins confirmed; retract); semiderived('+') is NOT derived-and-stored('**'). Using ORM2Core.xsd as the completeness yardstick for STATIC STRUCTURAL ORM2 (the exporter's subset) is sound.","status":"open","priority":1,"issue_type":"epic","owner":"noreply@anthropic.com","created_at":"2026-06-17T01:57:38Z","created_by":"Claude","updated_at":"2026-06-17T12:44:17Z","labels":["interop","metamodel","norma","orm2"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-v8r","title":"Entity-centric diagram layout overhaul","description":"Rewrite the diagram layout engine to produce NORMA-style 2D layouts. Entity types are spatial anchors positioned via ELK stress algorithm; fact types are placed geometrically between their connected entities. Supports horizontal/vertical orientation, subtype fans, hub entities, and join constraints. See barwise/docs/specs/entity-centric-layout.spec.md for full spec.","notes":"Feature complete and pushed on feature/entity-centric-layout branch. Commits: a705fb8 (initial stress layout), 79689ae (cluster-aware), 47ef199 (radial subtype), 8f98704 (spacing/edge routing), 8286e70 (drag), f2f784a (drag + orientation toggle), 07ff4e0 (FT drag persistence), 1a380bc (position persistence + sidebar), 92512bf/4005910 (focus/hop filter), 780cb31/fe91317 (views + ghost), cf6bf70 (objectification envelope collapse), 617cf02 (tool auto-resolve + live-refresh + pan/zoom preservation), b0cae15 (sidebar sort). Verification: npm run build OK, npm run lint OK (0 errors, 18 pre-existing warnings), npm run test 1,990/1,990 pass across 7 packages. Remaining work before merging to main is organizational: decide whether to open a PR or merge directly.","status":"closed","priority":1,"issue_type":"feature","assignee":"Gabe Schenz","owner":"gschenz@ritchiebros.com","created_at":"2026-04-03T20:09:21Z","created_by":"Gabe Schenz","updated_at":"2026-04-10T21:26:16Z","closed_at":"2026-04-10T21:26:16Z","close_reason":"Feature complete and pushed. Spec scope fully delivered plus significant additions (drag, views, focus mode, sidebar, auto-refresh). 1,990 tests pass. Branch feature/entity-centric-layout is 34 commits ahead of main. Merge to main is tracked separately as an administrative step.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-oli","title":"Map diagnostics to YAML source positions instead of line 0","description":"Currently DiagnosticsProvider hard-codes all diagnostic ranges to line 0, character 0, which causes yellow squiggly lines to appear under orm_version instead of the actual problematic objects/facts. The core Diagnostic objects include an elementId (UUID) but the provider ignores it. Fix requires: (1) YAML source mapping during deserialization so element IDs can be traced back to line/character positions, (2) DiagnosticsProvider to use elementId to look up the correct range, (3) fallback to document start for project-level diagnostics that don't target a specific element.","status":"closed","priority":1,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T17:00:58Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T18:36:25Z","closed_at":"2026-03-01T18:36:25Z","close_reason":"Implemented YamlSourceMap and updated DiagnosticsProvider to resolve validation diagnostics to exact YAML source positions. PR #54.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-pcj","title":"Mandate identifier fact types in extraction prompt","description":"GPT-5 Mini skips explicit identifier fact types (e.g. 'Patient has MedicalRecordNumber') even when entities have reference_modes. This causes downstream constraint skips because constraints reference fact types that don't exist. The prompt must explicitly instruct: for every entity with a reference_mode, emit a binary fact type 'Entity has Value' with is_preferred:true uniqueness on the entity role, reverse uniqueness on the value role, and mandatory on the entity role. This is the single highest-impact improvement for extraction quality.","status":"closed","priority":1,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T16:44:29Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T18:19:15Z","closed_at":"2026-03-01T18:19:15Z","close_reason":"Resolved in v0.2.1 prompt (PR #51). Identifier fact types now mandated; all three transcripts produce them correctly.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-0ot","title":"Ternary fact type uniqueness should span Order+Product roles","description":"In 'Order contains Product in Quantity', the uniqueness constraint is only on the Order role, meaning each Order contains at most one thing total. The transcript says 'Each line in the order specifies a product and a quantity' -- uniqueness should span Order+Product roles (each Order-Product combination has at most one Quantity). The LLM is not correctly identifying composite uniqueness for ternary fact types.","status":"closed","priority":1,"issue_type":"bug","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T15:19:28Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T18:19:49Z","closed_at":"2026-03-01T18:19:49Z","close_reason":"Fixed in v0.2.2 prompt. Mini now produces composite uniqueness on Order+Product roles in ternary fact type.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-b1w","title":"Missing reverse uniqueness on reference-mode fact types","description":"Reference-mode fact types (e.g., 'Customer has CustomerId') only get uniqueness on the entity role but not on the value role. The second constraint (each CustomerId identifies at most one Customer) is needed to make the identifier a bijection. The LLM recognizes the constraint conceptually (see NOTE annotations) but emits a duplicate of the entity-side constraint instead of creating a value-side constraint. May be an LLM prompt issue or a parser mapping issue.","status":"closed","priority":1,"issue_type":"bug","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T15:19:27Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T18:19:50Z","closed_at":"2026-03-01T18:19:50Z","close_reason":"Fixed in v0.2.2 prompt. All identifier fact types now have both entity-role and value-role uniqueness constraints.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-rm7","title":"Customer places Order: uniqueness on wrong role","description":"In the order-management transcript extraction, 'Customer places Order' has uniqueness on the Customer role (each Customer places at most one Order) instead of the Order role (each Order is placed by at most one Customer). The transcript clearly states 'Every order belongs to exactly one customer, and customers can place multiple orders.' This is a semantic inversion error in LLM extraction. Investigate whether the extraction prompt needs clearer guidance on which role to constrain for many-to-one relationships.","status":"closed","priority":1,"issue_type":"bug","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T15:19:27Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T18:19:50Z","closed_at":"2026-03-01T18:19:50Z","close_reason":"Fixed in v0.2.2 prompt. Customer places Order now has uniqueness on Order role (each Order placed by one Customer).","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-3mu","title":"Duplicate is_preferred constraints on reference-mode fact types","description":"LLM extraction produces duplicate internal_uniqueness constraints with is_preferred: true on the same role in reference-mode fact types (e.g., 'Customer has CustomerId' has two identical constraints on the entity role). The parser should deduplicate constraints with identical type, roles, and is_preferred flag. Observed in order-management.orm.yaml lines 104-111, 127-134, 150-157.","status":"closed","priority":1,"issue_type":"bug","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T15:19:26Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T18:19:49Z","closed_at":"2026-03-01T18:19:49Z","close_reason":"Fixed in v0.2.2 prompt. No duplicate is_preferred constraints in any v0.2.2 outputs.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-ahf","title":"Add VS Code integration tests","description":"packages/vscode/tests/ is empty. Add integration tests using @vscode/test-electron covering: diagnostics rendering, command execution, diagram panel opening, completion behavior, and transcript import flow.","status":"closed","priority":1,"issue_type":"task","owner":"gschenz@ritchiebros.com","created_at":"2026-02-26T19:25:53Z","created_by":"Gabe Schenz","updated_at":"2026-02-26T22:34:43Z","closed_at":"2026-02-26T22:34:43Z","close_reason":"Merged via PR #23 -- 15 unit tests + 7 integration tests for barwise-vscode","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-yco","title":"Implement objectified fact types","description":"Add ObjectifiedFactType to the metamodel. Objectified fact types (nested fact types) are fact types reified as entity types so they can participate in other relationships. Specified in ARCHITECTURE.md Section 3.1.1. Requires changes across model, serialization, validation, verbalization, diagram, and schema.","status":"closed","priority":1,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-02-26T19:25:37Z","created_by":"Gabe Schenz","updated_at":"2026-02-26T22:17:35Z","closed_at":"2026-02-26T22:17:35Z","close_reason":"Merged via PR #22 -- objectified fact types implemented across all packages with 560 tests passing","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-8sq","title":"MCP source accepts a file object { path?, content? }, not just a string","description":"The 11 MCP source-taking tools accept source only as a string, disambiguated by the isFilePath heuristic (newline -\u003e inline; .yaml -\u003e path; exists-on-disk -\u003e path; else guess). This is implicit. Allow source to also be a structured file object so callers can be explicit: { path } reads from disk, { content } is inline, { path, content } parses content but uses path for spill/lineage location (the unsaved-editor-buffer case). String stays for back-compat. stdio transport: no HTTP/upload semantics -- a file object is just an explicit path-or-content. Scope: all 11 source-taking tools, via the shared resolver (resolveSource/resolveModels). Backward compatible (string is in the union). Spec-first.","acceptance_criteria":"resolveSource/resolveModels accept string | {path?,content?}; all 11 tools updated; project resolution requires a path (manifest references files); existing string callers unchanged; full mcp suite + lint green; spec written.","status":"closed","priority":2,"issue_type":"feature","assignee":"Claude","owner":"noreply@anthropic.com","created_at":"2026-06-21T21:39:23Z","created_by":"Claude","updated_at":"2026-06-21T23:11:12Z","started_at":"2026-06-21T22:25:19Z","closed_at":"2026-06-21T23:11:12Z","close_reason":"Implemented across MCP (PR #238, WS1+WS2) and VS Code (PR #239, WS3); both merged. source accepts string | { path?, content? }; VS Code tools act on the live editor buffer.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-5d7","title":"Rename helpers/ -\u003e workspace/ in CLI and MCP (name the imperative shell)","description":"The helpers/ folders in cli and mcp are a junk-drawer name over what is really the package's imperative shell: the impure boundary that resolves a \u003csource\u003e (file path / inline YAML / project manifest) into the model(s) a command or tool runs against, feeding the functional (pure) core. Rename helpers/ -\u003e workspace/ to name that architectural role.\n\nScope (settle leaf layout in the spec):\n- workspace/ holds the source-resolution + file-I/O cluster: io.ts (loadModel, isProjectFile, writeOutput), projectLoader.ts (loadProject), domainModels.ts/resolve.ts (resolveDomainModels/resolveModels), and lineageIo.ts (provenance I/O -- also shell I/O).\n- Rendering is NOT io and stays a separate concern: CLI format.ts (formatDiagnostics/Verbalizations) and MCP response.ts (boundedTextResult). Decide its home in the spec (render/ vs flatten).\n\nDecision (resolved with owner): keep the ~30-line loadProject duplication across CLI and MCP rather than a shared @barwise/workspace package. The pure assembly already lives in core (assembleProject, projectFilePaths); the three shells genuinely differ at the boundary (path vs inline YAML vs open editor document), so a shared resolver would force one to bend its interface -- exactly what the DRY-secondary principle warns against.\n\nTripwire to revisit the shared-package option: a third near-identical loader appears (e.g. vscode grows project loading) OR the duplicated impure glue grows past trivial.\n\nBlast radius: ~32 import sites in cli, ~19 in mcp. Mechanical. Spec-first per project convention. Land AFTER PR #237 merges (same dev branch).","acceptance_criteria":"helpers/ renamed to workspace/ in cli and mcp; all import sites updated; full suite + lint green; spec written documenting the functional-core/imperative-shell decision and the rendering split.","notes":"Refinement (2026-06-21): projectLoader.ts is byte-identical across cli/mcp (true duplication, unlike resolve.ts which genuinely diverges). Owner considered + rejected hoisting the loader orchestration into core behind an injected file-reader port -- declined to keep core strictly effect-free. Duplication stays; escalation remains a shared @barwise/workspace package outside core, never core/io.","status":"closed","priority":2,"issue_type":"task","assignee":"Claude","owner":"noreply@anthropic.com","created_at":"2026-06-21T21:27:51Z","created_by":"Claude","updated_at":"2026-09-06T02:11:59Z","started_at":"2026-06-23T12:59:21Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-09-06T02:11:59Z","close_reason":"Rename landed: packages/cli/src/workspace and packages/mcp/src/workspace exist, no helpers/ remains"}
{"_type":"issue","id":"barwise-5dd","title":"r4f WS4: VS Code project manifest LSP + language registration","description":"Register .orm-project.yaml in the LSP document selector and validate the manifest structure via core's ProjectSerializer (schema-level diagnostics, consistent with how .orm.yaml is LSP-validated). Register an orm-project language in package.json. Note: jsonValidation does not apply to YAML in core VS Code, so schema validation is delivered through the LSP. Per docs/specs/orm-project-surface-wiring.spec.md WS4.","notes":"Shipped in PR #237. LSP validateProject via ProjectSerializer + orm-project language registration. jsonValidation deferred (YAML-incompatible); cross-file project diagnostics deferred to barwise-sa4.","status":"closed","priority":2,"issue_type":"task","assignee":"Claude","owner":"noreply@anthropic.com","created_at":"2026-06-21T21:07:58Z","created_by":"Claude","updated_at":"2026-09-06T02:11:59Z","started_at":"2026-06-21T21:08:05Z","dependencies":[{"issue_id":"barwise-5dd","depends_on_id":"barwise-r4f","type":"parent-child","created_at":"2026-06-21T21:08:04Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-09-06T02:11:59Z","close_reason":"Shipped in PR #237 (LSP validateProject + orm-project language registration); spec orm-project-surface-wiring archived as Implemented; cross-file diagnostics tracked in barwise-sa4"}
{"_type":"issue","id":"barwise-vli","title":"r4f WS3: MCP project support (domain input on read tools)","description":"Add a project branch to mcp/src/helpers/resolve.ts (resolveModels) and an optional 'domain' input on the read tools: validate_model, verbalize_model, query_model, review_model, export_model, describe_domain. A manifest source resolves per-domain; no domain yields a combined per-domain view, a domain selects one. Single-model behavior preserved. Per docs/specs/orm-project-surface-wiring.spec.md.","notes":"Shipped in PR #236. resolveModels + projectLoader in MCP; domain input on validate/verbalize/query/describe/review/export.","status":"closed","priority":2,"issue_type":"task","assignee":"Claude","owner":"noreply@anthropic.com","created_at":"2026-06-21T20:28:01Z","created_by":"Claude","updated_at":"2026-09-06T02:11:59Z","started_at":"2026-06-21T20:28:05Z","dependencies":[{"issue_id":"barwise-vli","depends_on_id":"barwise-r4f","type":"parent-child","created_at":"2026-06-21T20:28:04Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-09-06T02:11:59Z","close_reason":"Shipped in PR #236 (MCP resolveModels + projectLoader, domain input on read tools); spec orm-project-surface-wiring archived as Implemented"}
{"_type":"issue","id":"barwise-62o","title":"Book-verify: read 3rd ed. serialization (JSON/YAML) coverage (CC5)","description":"The 3rd ed. (2024) newly covers JSON/YAML/markup serialization of ORM models -- barwise's exact domain. Read it and check whether the book sanctions a serialization framing the native .orm.yaml should align to. See docs/book-verification-checklist.md CC5.","notes":"BOOK QUESTIONS (3rd ed., 2024) to answer from the serialization / data-file-format / NoSQL chapters. Each maps to a barwise .orm.yaml choice to verify. barwise format: root keys {orm_version, model}; model = {name, domain_context, note, object_types[], fact_types[] (constraints INLINE), subtype_facts[], objectified_fact_types[], populations[], definitions[], diagrams[]}; roles/object-types referenced by opaque id.\nQ1 Does the book show a worked JSON and/or YAML serialization of an ORM model (not just relational DDL)? Which listings/figures?\nQ2 CRUX: is that JSON/YAML the CONCEPTUAL ORM model serialized, or the PHYSICAL target schema for a document/NoSQL DB the model maps TO? Does the book keep conceptual vs physical separate (ours: .orm.yaml is conceptual; DDL is derived)?\nQ3 Top-level structure: separate object-type and fact-type collections (like ours), or organized differently?\nQ4 Constraints INLINE on the fact type, or in a separate id-referenced section (NORMA-style)? [our key anti-NORMA choice]\nQ5 Keyed by opaque ids + id-refs (id graph, NORMA-style), or by names/readings as primary keys?\nQ6 How are fact-type readings represented (placeholder templates like '{0} ... {1}'?)\nQ7 Are sample populations (fact instances) in the same file as the schema, or separate (CSV for data)? [we embed populations]\nQ8 Does the conceptual serialization include diagram/layout/presentation info, or keep it out? [we embed diagrams]\nQ9 Any schema/format VERSION field (like our orm_version)?\nQ10 Derivation rule serialized as text or formal expression? Join/set-comparison path serialized as root+roles+projection?\nQ11 Does the book name a canonical ORM interchange format, or treat NORMA .orm XML as the standard? Is JSON/YAML offered as interchange or just illustration?","status":"open","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-06-21T18:45:22Z","created_by":"Claude","updated_at":"2026-06-21T18:54:58Z","dependencies":[{"issue_id":"barwise-62o","depends_on_id":"barwise-8ir","type":"parent-child","created_at":"2026-06-21T18:45:22Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-d9a","title":"Book-verify: cardinality unary-role restriction (T2.1)","description":"Confirm the book's cardinality constraints and whether restricting role cardinality to unary roles is correct or too narrow (the unary split was read off the NORMA XSD). See docs/book-verification-checklist.md.","status":"open","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-06-21T18:30:05Z","created_by":"Claude","updated_at":"2026-06-21T18:30:05Z","dependencies":[{"issue_id":"barwise-d9a","depends_on_id":"barwise-8ir","type":"parent-child","created_at":"2026-06-21T18:30:20Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-q82","title":"Book-verify: frequency role-sequence + min floor (T2.3)","description":"Confirm the book permits frequency over a role sequence with tuple semantics, and whether min\u003e=1 is a book rule or a tooling default we carried. See docs/book-verification-checklist.md.","status":"open","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-06-21T18:30:05Z","created_by":"Claude","updated_at":"2026-06-21T18:30:05Z","dependencies":[{"issue_id":"barwise-q82","depends_on_id":"barwise-8ir","type":"parent-child","created_at":"2026-06-21T18:30:20Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-diu","title":"Book-verify: deontic = warning rendering (T1.6)","description":"Confirm against Halpin's modality treatment that a deontic violation is faithfully a recorded/permitted state (our warning, not error), the per-constraint tag is correct, and excluding compound modal operators is sound. See docs/book-verification-checklist.md.","status":"open","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-06-21T18:30:04Z","created_by":"Claude","updated_at":"2026-06-21T18:30:04Z","dependencies":[{"issue_id":"barwise-diu","depends_on_id":"barwise-8ir","type":"parent-child","created_at":"2026-06-21T18:30:19Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-k5q","title":"Book-verify: verbalization FORML fidelity (CC1)","description":"Audit every constraint verbalizer against ORM2-02 FORML2; several emit deterministic approximations (e.g. join 'project the same [Person, Country]' vs Halpin's relative-clause form). Decide per family whether to move toward FORML. See docs/book-verification-checklist.md.","status":"open","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-06-21T18:30:04Z","created_by":"Claude","updated_at":"2026-06-21T18:30:04Z","dependencies":[{"issue_id":"barwise-k5q","depends_on_id":"barwise-8ir","type":"parent-child","created_at":"2026-06-21T18:30:19Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-8ir","title":"Book-verification audit (anti-NORMA-centrism)","description":"Standing audit that the Tier 1-3 metamodel constructs follow Halpin's book, not NORMA's tooling. Full enumerated list lives in docs/book-verification-checklist.md; child issues track the high-value verifications. Resolve items in the doc as they are checked.","status":"open","priority":2,"issue_type":"epic","owner":"noreply@anthropic.com","created_at":"2026-06-21T18:29:52Z","created_by":"Claude","updated_at":"2026-06-21T18:29:52Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-5t9.9","title":"Value-comparison constraints (role-to-role value comparison)","description":"GAP (confirmed against ORM2Core.xsd: ValueComparisonConstraintType).\nORM 2 supports value-comparison constraints asserting an ordering\nrelationship between the values of two roles, e.g. \"Employee.startDate \u003c\nEmployee.endDate\" or \"Sale.price \u003e= Sale.cost\". Operators: \u003c, \u003c=, =, \u003c\u003e,\n\u003e=, \u003e. This is distinct from ring constraints (which relate two roles\nplayed by the same object type by instance identity, not value order).\n\nNeither the metamodel nor the importer has any notion of it (grep for\nValueComparisonConstraint in packages/formats is empty), so NORMA models\nusing it lose the constraint silently. This was invisible to the original\nimport-grounded audit because the importer never references the element.\n\nWork: add a value-comparison constraint kind (two role ids + operator);\nimport ValueComparisonConstraint; verbalize; validate against populations.\n","acceptance_criteria":"Metamodel can express a value-comparison constraint between two roles with an operator (\u003c, \u003c=, =, \u003c\u003e, \u003e=, \u003e); importer reads ValueComparisonConstraint; verbalization renders it; not conflated with ring constraints.","notes":"Core construct landed (PR pending): model (ValueComparisonConstraint + operator union + isValueComparison guard), serializer round-trip, JSON Schema branch, verbalization (operator phrases), population validation (numeric/lexical), diff key, and the one-time orm_version 1.0 -\u003e 1.1 bump (CURRENT_ORM_VERSION + schema const + no-op forward migration) per ADR-0001. Scope: v1 compares two roles of the SAME fact type; join-path value comparison is deferred to the role-path model (barwise-0s8/5t9.10). NORMA importer/exporter mapping deferred to a follow-on (RT-B fidelity) -- NORMA's ValueComparisonConstraint operator enum needs XSD verification. Leaving open until the importer lands.","status":"closed","priority":2,"issue_type":"feature","assignee":"Claude","owner":"noreply@anthropic.com","created_at":"2026-06-17T02:49:02Z","created_by":"Claude","updated_at":"2026-09-06T02:24:06Z","started_at":"2026-06-18T13:12:52Z","labels":["constraints","interop","metamodel","norma","orm2"],"dependencies":[{"issue_id":"barwise-5t9.9","depends_on_id":"barwise-5t9","type":"parent-child","created_at":"2026-06-17T02:49:01Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-09-06T02:24:06Z","close_reason":"Acceptance met: the note's own condition 'leaving open until the importer lands' is met -- NormaXmlParser reads ValueComparisonConstraint with the operator map (line 811) and the writer emits it; norma-round-trip-completion.spec.md WS2 Complete 2026-08-07; NormaConstructRoundTrip 'value-comparison round-trip' passes; distinct constraint type from ring"}
{"_type":"issue","id":"barwise-8kh","title":"Implement NORMA XML export (model -\u003e .orm) per spec","description":"Add the NORMA exporter -- the inverse of the existing importer -- as an\nExportFormatAdapter on the `normaFormat` descriptor in @barwise/formats,\nso `barwise export --format norma` and the MCP export tool light up\nthrough the registry.\n\nDesign and rationale: barwise/docs/specs/norma-export.spec.md. Key\nframing: NORMA is a superset of barwise's metamodel, so model -\u003e NORMA is\nlossless for the representable subset today; barwise-5t9 bounds only the\nNORMA -\u003e model -\u003e NORMA round-trip (RT-B), not the exporter.\n\nWorkstreams:\n- WS1 semantic exporter + RT-A (model -\u003e NORMA -\u003e model) round-trip. Now.\n- WS2 diagram geometry emission. Gated on barwise-e3g.\n- WS3 RT-B fidelity as each barwise-5t9 construct becomes representable.\n","design":"See barwise/docs/specs/norma-export.spec.md. WS1 (semantic exporter + RT-A round-trip) is unblocked and shippable now; WS2 (diagram geometry) is gated on barwise-e3g; WS3 (RT-B fidelity) grows as barwise-5t9 items land.","acceptance_criteria":"normaFormat descriptor gains an exporter; barwise export \u003csrc\u003e --format norma produces a .orm that re-imports equal to the source (RT-A) across tests/fixtures/*.orm; reference schemes re-expanded; implied constraints not emitted; no core change.","status":"open","priority":2,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-06-17T02:35:18Z","created_by":"Claude","updated_at":"2026-06-17T02:35:19Z","labels":["export","formats","interop","norma"],"dependencies":[{"issue_id":"barwise-8kh","depends_on_id":"barwise-e3g","type":"blocks","created_at":"2026-06-17T02:35:19Z","created_by":"Claude","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-5t9.4","title":"Object cardinality constraints (population size), distinct from role frequency","description":"GAP. barwise has FrequencyConstraint (Constraint.ts:216-231) which bounds\nhow many times an object plays a *role* (\"each Customer places 2..5\nOrders\"). ORM 2 also has CARDINALITY constraints that bound the *number\nof instances* of an object type (or the number of instances playing a\nrole): \"there are at most 50 Departments\", \"between 2 and 10 active\nPromotions\". These are different concepts and NORMA models them\nseparately.\n\nThere is no way to express object-population cardinality today, so such\nconstraints are lost on import and unrepresentable in authored models.\n\nWork: add a cardinality constraint kind keyed on an object type (and\noptionally a role), with min/max (max may be unbounded); wire import,\nverbalization, validation against populations.\n","acceptance_criteria":"Metamodel can express a cardinality bound on an object type's population (and optionally on a role's instance count); importer reads NORMA CardinalityConstraint; verbalization renders it; not conflated with FrequencyConstraint.","notes":"Core construct (WS1-WS3) implemented: shared CardinalityRange; ObjectType.cardinality field + unary CardinalityConstraint union member; round-trip + JSON Schema; population-gated validation (object-type count via buildObjectUniverse, unary-role count via valuesPlayedInRole; deontic -\u003e warning); unary arity-1 structural check; verbalization (both forms); diff. WS4 NORMA mapping deferred -\u003e see follow-up issue.","status":"closed","priority":2,"issue_type":"feature","assignee":"Claude","owner":"noreply@anthropic.com","created_at":"2026-06-17T01:58:47Z","created_by":"Claude","updated_at":"2026-09-06T02:24:06Z","started_at":"2026-06-19T01:46:58Z","labels":["constraints","interop","metamodel","norma","orm2"],"dependencies":[{"issue_id":"barwise-5t9.4","depends_on_id":"barwise-5t9","type":"parent-child","created_at":"2026-06-17T01:58:47Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0,"closed_at":"2026-09-06T02:24:06Z","close_reason":"Acceptance met: core construct per docs/specs/archive/object-cardinality.spec.md (Implemented, e4d43f4); NORMA import/export of both XSD cardinality elements verified and closed as barwise-34g; not conflated with FrequencyConstraint (separate 'cardinality' constraint type)"}
{"_type":"issue","id":"barwise-5t9.2","title":"Derived fact types and derivation rules","description":"GAP. No derivation concept exists in the metamodel (rg for\nderiv/derived across core/src/model returns only an unrelated comment in\nSubtypeFact.ts). ORM 2 supports derived predicates -- purely derived\n(computed on demand) and derived-and-stored -- each carrying a\nderivation rule (formal/relational or natural-language). Subtype-defining\nrules are the same family (a subtype defined by a predicate rather than\nasserted).\n\nToday every fact type in `.orm.yaml` is asserted/base; a modeler cannot\nsay \"Person is an Adult iff Person has Age \u003e= 18\" or \"TotalPrice is\nderived from Quantity * UnitPrice.\" NORMA models that use derivation lose\nthat intent on import.\n\nWork: add an optional derivation field to FactType (and a defining-rule\noption to SubtypeFact), decide the rule representation (start with a\nfree-text/natural-language rule string + a derivation kind enum), wire\nimport where NORMA expresses it, and verbalize. Keep formal rule grammar\nout of scope for v1.\n","acceptance_criteria":"Metamodel can mark a fact type as derived (purely-derived or derived-and-stored) and attach a derivation rule; importer reads NORMA derivation rules where present; verbalization renders the rule; validation treats derived facts appropriately.","notes":"Core construct (WS1-WS3) implemented: DerivationRule/DerivationKind/DerivationStorage; FactType.derivation + SubtypeFact.definingRule; round-trip + JSON Schema; derivation consistency validation (blank-rule warning, purely-derived-with-population warning); verbalization (fact-type derivation sentence + subtype defining-rule clause); diff (derivation change on fact types). Subtype-fact diffing does not exist in the codebase, so the spec's 'defining_rule into subtype diff' line is N/A (noted in PR). WS4 NORMA mapping deferred -\u003e follow-up issue.","status":"closed","priority":2,"issue_type":"feature","assignee":"Claude","owner":"noreply@anthropic.com","created_at":"2026-06-17T01:58:18Z","created_by":"Claude","updated_at":"2026-09-06T02:24:06Z","started_at":"2026-06-19T02:21:19Z","labels":["interop","metamodel","norma","orm2"],"dependencies":[{"issue_id":"barwise-5t9.2","depends_on_id":"barwise-5t9","type":"parent-child","created_at":"2026-06-17T01:58:17Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0,"closed_at":"2026-09-06T02:24:06Z","close_reason":"Acceptance met: core construct per docs/specs/archive/derived-fact-types.spec.md (Implemented, e550938); NORMA import/export half verified and closed as barwise-q4g; packages/formats NormaConstructRoundTrip passes (21/21)"}
{"_type":"issue","id":"barwise-5t9.3","title":"Deontic vs alethic constraint modality","description":"GAP. No modality concept exists (rg for deontic/alethic across\ncore/src/model returns nothing). ORM 2 distinguishes ALETHIC constraints\n(logical necessity -- cannot be violated in any valid population, the\ndefault) from DEONTIC constraints (obligation -- should hold, but\nviolations are recorded rather than impossible). NORMA renders these\ndifferently (solid vs dashed/violet, \"must/may\" vs \"should/should not\").\n\nCurrently all barwise constraints are implicitly alethic, so deontic\nbusiness rules (\"a manager should not approve their own expense\") cannot\nbe expressed and are lost on import.\n\nWork: add an optional `modality: \"alethic\" | \"deontic\"` to the shared\nconstraint shape (default alethic), thread it through import,\nverbalization (modal verb selection), and validation (deontic violations\nare warnings/recorded, not errors).\n","acceptance_criteria":"Every constraint can carry a modality (alethic default | deontic); importer reads NORMA modality; verbalization uses the correct modal wording (must/may vs should/should not); default remains alethic for back-compat.","notes":"Implemented: shared ConstraintBase carries modality?; serializer round-trips (deontic-only emission); JSON Schema enum on all branches; verbalization prefixes 'It is obligatory that'; deontic population violations are warnings via severityForModality; diff key folds modality. DEFERRED: NORMA Modality=Deontic import/export pending ORM2Core.xsd verification (same caution as other deferred NORMA mappings).","status":"closed","priority":2,"issue_type":"feature","assignee":"Claude","owner":"noreply@anthropic.com","created_at":"2026-06-17T01:58:18Z","created_by":"Claude","updated_at":"2026-06-19T01:39:53Z","started_at":"2026-06-19T01:15:56Z","closed_at":"2026-06-19T01:39:53Z","close_reason":"Implemented and pushed in PR #219","labels":["constraints","interop","metamodel","norma","orm2"],"dependencies":[{"issue_id":"barwise-5t9.3","depends_on_id":"barwise-5t9","type":"parent-child","created_at":"2026-06-17T01:58:18Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-6i6","title":"Symbolic model query API in @barwise/core","description":"Build a deterministic, programmatic query interface over the ORM metamodel. Treats the model as a queryable graph of object types, fact types, roles, and constraints.\n\nThis is foundational: it makes the @barwise chat agent cheaper and more trustworthy (most 'questions about the model' become structured queries, not LLM prompts), it gives derivation rules a target language ('a derivation rule is just a named query'), and it gives CLI/MCP a stable surface for model introspection.\n\nExamples of questions it must answer deterministically:\n- What fact types involve \u003cobject type\u003e?\n- What constraints reference \u003crole\u003e?\n- What is downstream / upstream of objectifying this fact?\n- What object types have no mandatory roles? (model-quality query)\n- What fact types share roles with \u003cfact type\u003e?\n\nInterface surface:\n- @barwise/core: new public API (likely src/query/ alongside validation, verbalization)\n- CLI: new 'barwise query' command, or extend 'barwise show' subcommands\n- MCP: new query_model tool exposing the API\n- VS Code: chat agent (@barwise) delegates to this API for deterministic questions before falling back to LLM","acceptance_criteria":"Public API documented; CLI subcommand works; MCP tool registered; chat agent uses it for at least 5 representative deterministic queries","status":"open","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-05-11T22:25:13Z","created_by":"Gabe Schenz","updated_at":"2026-05-11T22:25:13Z","dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"barwise-e8c","title":"OWL/SHACL import/export connector","description":"Add a format connector for the W3C semantic-web stack: OWL for class hierarchies and property characteristics, SHACL for closed-world data validation.\n\nPer discussion comparing conceptual modeling approaches: ORM2's constraint vocabulary maps closely to SHACL's, and ORM2 + SHACL is the strongest combination on the page that no current tool ships. This connector reframes Barwise from 'ORM2 tool' to 'fact-oriented front-end to the W3C stack'.\n\nPlugs into the existing FormatDescriptor registry in @barwise/core (packages/core/src/format/).\n\nInterface surface:\n- CLI: 'barwise export --format=owl|shacl', 'barwise import --format=owl'\n- MCP: existing export_model / import_model tools pick up the new format automatically via the registry\n- VS Code: register format in import/export commands and quick-pick\n\nMapping notes:\n- ORM uniqueness -\u003e sh:unique\n- ORM mandatory / cardinality -\u003e sh:minCount / sh:maxCount / owl cardinality restrictions\n- ORM ring constraints -\u003e owl property characteristics (subset) + SHACL-SPARQL for the rest\n- ORM subtypes -\u003e rdfs:subClassOf / owl:disjointWith\n- ORM objectified facts -\u003e intermediate resource pattern","acceptance_criteria":"OWL+SHACL output validates with pySHACL; round-trip preserves ORM constraints that have direct OWL/SHACL analogs; CLI, MCP, and VS Code can all use the format","status":"open","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-05-11T22:25:06Z","created_by":"Gabe Schenz","updated_at":"2026-05-11T22:25:06Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-dle","title":"MCP diagram test fixture: Customer vs Client mismatch","description":"The MCP diagram tool test references an entity named 'Customer' in packages/mcp/tests/fixtures/simple.orm.yaml, but the fixture currently defines an entity named 'Client'. The test fails because the expected element is missing. During the previous session I tried to edit the fixture to use 'Customer' but the edit was reverted by an unknown mechanism (possibly a hook, possibly a generator script) each time. Need to: (1) identify what is reverting edits to this fixture, (2) decide whether the fixture or the test is the source of truth, (3) fix whichever side is wrong. Current state: 1 pre-existing MCP test failure in the diagram suite.","acceptance_criteria":"packages/mcp tests pass with no skipped/failing diagram tests; the source-of-truth decision is documented in the fix commit.","status":"closed","priority":2,"issue_type":"bug","owner":"gschenz@ritchiebros.com","created_at":"2026-04-10T15:40:55Z","created_by":"Gabe Schenz","updated_at":"2026-04-10T20:00:57Z","closed_at":"2026-04-10T20:00:57Z","close_reason":"Root cause was test isolation, not fixture drift. The merge tool auto-writes the merged YAML back to the base file path (merge.ts:94-97), so running the merge test overwrote packages/mcp/tests/fixtures/simple.orm.yaml with the content of simple-modified.orm.yaml on every run. Fixed merge.test.ts to copy the base fixture to a tmp file before each test so the committed fixture is not mutated. All 76 MCP tests now pass from a clean state.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-r4f","title":"Verify and wire up .orm-project.yaml support in CLI, MCP, and VS Code","description":"Audit the current state of .orm-project.yaml support across the user-facing surfaces and fill gaps. Today the core package can load a project and resolve cross-domain refs, but it is unclear whether (a) the barwise CLI commands (validate, verbalize, diagram, export, diff) accept a .orm-project.yaml as input, (b) the MCP generate_diagram / validate / verbalize tools accept a project manifest, (c) the VS Code diagram command and DiagramPanel can open a project, and (d) the LSP registers .orm-project.yaml as a handled file type. For any surface that does not support it, decide whether to add support now or document the limitation. This blocks the splitting guide because the guide needs to tell the user how to actually use the result.","acceptance_criteria":"Written audit of current project-file support across CLI/MCP/VS Code/LSP; each gap either fixed or tracked as a follow-up issue; at minimum 'barwise validate path/to/project.orm-project.yaml' and 'barwise diagram path/to/project.orm-project.yaml --domain catalog' work.","status":"closed","priority":2,"issue_type":"task","assignee":"Claude","owner":"gschenz@ritchiebros.com","created_at":"2026-04-10T15:40:32Z","created_by":"Gabe Schenz","updated_at":"2026-09-06T02:11:59Z","started_at":"2026-06-21T19:09:53Z","dependency_count":0,"dependent_count":4,"comment_count":0,"closed_at":"2026-09-06T02:11:59Z","close_reason":"Audit delivered and gaps fixed: .orm-project.yaml wired through CLI, MCP (PR #236), and VS Code (PR #237); spec archived as Implemented; deferred items tracked in barwise-hwe, barwise-byy, barwise-sa4"}
{"_type":"issue","id":"barwise-knq","title":"OrmProject multi-file workflow end-to-end","description":"Epic: make the OrmProject multi-file workflow a first-class, usable feature. Today the metamodel, serializers, and schemas exist (see ARCHITECTURE.md §3.1.4 and packages/core/tests/integration/fixtures/multi-domain/) but there is no user-facing path to get from a monolithic .orm.yaml to a working multi-file project. This epic covers the docs, CLI tooling, and VS Code/MCP integration needed to close that gap. Worked example should be splitting auction.orm.yaml into catalog/auctions/payments/parties domains with crossing context mappings.","acceptance_criteria":"User can start from a single .orm.yaml, run a command to scaffold a project, open the resulting .orm-project.yaml in VS Code, and generate a diagram of a single domain or the full project.","status":"open","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-04-10T15:40:20Z","created_by":"Gabe Schenz","updated_at":"2026-06-17T01:55:14Z","dependencies":[{"issue_id":"barwise-knq","depends_on_id":"barwise-gfz","type":"parent-child","created_at":"2026-04-10T10:41:02Z","created_by":"auto-import","metadata":"{}"},{"issue_id":"barwise-knq","depends_on_id":"barwise-r4f","type":"parent-child","created_at":"2026-04-10T10:41:01Z","created_by":"auto-import","metadata":"{}"},{"issue_id":"barwise-knq","depends_on_id":"barwise-e4k","type":"parent-child","created_at":"2026-04-10T10:41:03Z","created_by":"auto-import","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-rqb","title":"Add user-defined custom attributes to model elements","description":"Allow users to attach arbitrary key/value metadata to ObjectType, FactType, Role, constraints, SubtypeFact, OrmModel, and OrmProject so they can customize models for purposes we haven't anticipated (subdomain tagging, ownership, external IDs, review status, etc.). Must round-trip through YAML serialization, update JSON schemas, and be surfaced in diff output. Custom keys should not trigger validation errors. Provides substrate for barwise-un8 (subdomain categorization).","design":"Start with flat Record\u003cstring, string|number|boolean\u003e; reserve 'barwise.*' namespace; serializer round-trip tests required; diff engine must compare custom fields.","acceptance_criteria":"Round-trip tests pass for all element types; JSON schema updated; diff surfaces custom changes; validation ignores unknown keys; MCP tool outputs include custom field.","status":"open","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-04-10T15:37:47Z","created_by":"Gabe Schenz","updated_at":"2026-04-10T15:37:47Z","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"barwise-d9f","title":"Show neighbors: ghost node preview for saved views","description":"Right-click entity in diagram -\u003e Show neighbors reveals 1-hop neighbors as ghost nodes (dimmed, dashed). Click ghost to add to view. Clear preview button to dismiss. Enables two-step explore-then-commit workflow for building saved views.","status":"closed","priority":2,"issue_type":"feature","assignee":"Gabe Schenz","owner":"gschenz@ritchiebros.com","created_at":"2026-04-06T17:54:03Z","created_by":"Gabe Schenz","updated_at":"2026-04-06T18:02:08Z","closed_at":"2026-04-06T18:02:08Z","close_reason":"Implemented ghost node preview with Show Neighbors context menu, click-to-add, and Clear Preview","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-cl2","title":"Click-to-highlight entity connections in diagram","description":"Clicking an entity in the diagram highlights its direct connections: connected fact types, edges, and partner entities. Provides immediate visual feedback about an entity's neighborhood without changing the view. Shift-click to multi-select for creating views later.","status":"closed","priority":2,"issue_type":"feature","assignee":"Gabe Schenz","owner":"gschenz@ritchiebros.com","created_at":"2026-04-06T00:26:09Z","created_by":"Gabe Schenz","updated_at":"2026-04-06T01:26:57Z","closed_at":"2026-04-06T01:26:57Z","close_reason":"Implemented click-to-highlight in both sidebar tree and diagram. Clicking an element dims unrelated nodes/edges and highlights the element with its direct connections. Context menus added for Highlight in Diagram and Copy Name.","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"barwise-7dc","title":"Sidebar model browser tree view","description":"VS Code sidebar tree view showing the model structure: entity types, fact types, subtype hierarchies, constraints. Clicking an element highlights it in the active diagram. Foundation for view management -- later phases add the ability to create/switch views from this panel.","status":"closed","priority":2,"issue_type":"feature","assignee":"Gabe Schenz","owner":"gschenz@ritchiebros.com","created_at":"2026-04-06T00:26:08Z","created_by":"Gabe Schenz","updated_at":"2026-04-06T01:00:34Z","closed_at":"2026-04-06T01:00:34Z","close_reason":"Implemented sidebar model browser tree view with activity bar icon, ModelTreeProvider showing entity types, value types, fact types with constraints, subtype relationships, and diagram layouts. Auto-refreshes on file switch and save.","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"barwise-7bp","title":"Persist diagram positions in .orm.yaml","description":"Save user-arranged diagram positions in the orm.yaml file so layouts survive across sessions. The coordinate system should be simple and human-editable: integer pixel coordinates with (0,0) at top-left, x increasing right, y increasing down. No fractional inches (NORMA style) or internal padding offsets. A diagrams section in the YAML with per-element x,y positions and optional width/height overrides. Users should be able to hand-edit positions without confusion. This is the foundation for multi-view support (barwise-dks).","status":"closed","priority":2,"issue_type":"feature","assignee":"Gabe Schenz","owner":"gschenz@ritchiebros.com","created_at":"2026-04-06T00:26:05Z","created_by":"Gabe Schenz","updated_at":"2026-04-06T01:00:40Z","closed_at":"2026-04-06T01:00:40Z","close_reason":"Implemented diagram position persistence in .orm.yaml: DiagramLayout on OrmModel, JSON schema, serializer round-trip, Save button in webview, auto-load on open. Integer pixels, name-keyed, screen coordinates.","dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"barwise-223","title":"ORM meta-model of diagram layout rules","description":"Create a .orm.yaml model that formalizes the diagram layout rules using ORM itself. Transcript saved at examples/transcripts/diagram-layout.transcript.txt. Import via barwise MCP or CLI, validate, generate diagram. Covers: cluster detection, subtype radial placement, leaf distribution, fact type stacking, boundary entity nudging.","status":"open","priority":2,"issue_type":"task","owner":"gschenz@ritchiebros.com","created_at":"2026-04-03T23:02:06Z","created_by":"Gabe Schenz","updated_at":"2026-04-03T23:02:06Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-q28","title":"Draggable entity positions in diagram webview","description":"Make entity types draggable in the VS Code webview SVG. When dragged, re-run Pass 2 (fact type placement + edge routing) with the new entity position and re-render. Optionally persist manual positions in a companion .orm-layout.yaml file. Foundation for verbalization-on-selection and click-to-focus features.","status":"closed","priority":2,"issue_type":"feature","assignee":"Gabe Schenz","owner":"gschenz@ritchiebros.com","created_at":"2026-04-03T23:01:54Z","created_by":"Gabe Schenz","updated_at":"2026-04-05T03:05:06Z","closed_at":"2026-04-05T03:05:06Z","close_reason":"Drag-to-reposition entities and double-click to toggle fact type orientation both implemented and pushed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-prm","title":"Interactive diagram: click-to-focus subset view","description":"Add click handlers to SVG entity types in the VS Code webview. When an entity is clicked, show a toolbar with hop selector (1/2/3/All). Re-generate diagram with only the selected entity's N-hop neighborhood. Maps to Looker Explore concept: each entity-centric view is like an Explore centered on a fact table. Requires filterGraph() in @barwise/diagram and webview message passing.","status":"closed","priority":2,"issue_type":"feature","assignee":"Gabe Schenz","owner":"gschenz@ritchiebros.com","created_at":"2026-04-03T23:01:48Z","created_by":"Gabe Schenz","updated_at":"2026-04-06T01:46:39Z","closed_at":"2026-04-06T01:46:39Z","close_reason":"Implemented click-to-focus subset view. Double-click entity to enter focus mode with hop selector (1/2/3/All). N-hop neighborhood BFS filter computes connected entities through fact types and subtype relationships. Clear button returns to full model.","dependencies":[{"issue_id":"barwise-prm","depends_on_id":"barwise-t7v","type":"blocks","created_at":"2026-04-03T18:02:09Z","created_by":"auto-import","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"barwise-t7v","title":"Radial subtype placement","description":"Subtypes should radiate outward from the diagram center relative to their supertype. The fan direction is determined by the vector from diagram center through the supertype position. Subtypes with fewer connections belong at the periphery. Replace the current rigid 'subtypes below' rule with this radial approach.","status":"closed","priority":2,"issue_type":"feature","assignee":"Gabe Schenz","owner":"gschenz@ritchiebros.com","created_at":"2026-04-03T23:01:35Z","created_by":"Gabe Schenz","updated_at":"2026-04-03T23:12:54Z","closed_at":"2026-04-03T23:12:54Z","close_reason":"Fixed stale object reference bug in normalizeCoordinates and removed redundant second placeSubtypesRadially call","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"barwise-8n5","title":"Add tests for MCP resources and prompts","description":"Both MCP resources (orm-schema://, orm-model://) and both prompts (analyze-domain, review-model) have no real test coverage — only a dummy server-creation assertion exists. Add tests for: orm-model path resolution and YAML deserialization, orm-schema schema content verification, prompt parameter passing and workflow output.","status":"open","priority":2,"issue_type":"task","owner":"gschenz@ritchiebros.com","created_at":"2026-03-02T03:46:06Z","created_by":"Gabe Schenz","updated_at":"2026-03-02T03:46:06Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-4i9","title":"Add tests for MCP import_transcript tool","description":"The import_transcript tool is the most complex MCP tool (LLM integration, provider selection, model override) but has zero test coverage. Add unit tests with mock LlmClient covering: provider selection, model override, error handling for API failures, and verification that extraction features (all 11 constraints, objectification, aliases) flow through correctly.","status":"open","priority":2,"issue_type":"task","owner":"gschenz@ritchiebros.com","created_at":"2026-03-02T03:46:05Z","created_by":"Gabe Schenz","updated_at":"2026-03-02T03:46:05Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-cz6","title":"Add dbt and OpenAPI export tools to MCP server","description":"PR #64 added export dbt and export openapi to the CLI but never updated the MCP server. Add export_dbt and export_openapi tools so AI assistants have parity with CLI users. Core already has renderDbt() and renderOpenApi().","status":"open","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-02T03:45:51Z","created_by":"Gabe Schenz","updated_at":"2026-03-02T03:45:51Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-cp4","title":"Review Phase 2 constraint extraction test coverage","description":"Review the 35 tests for Phase 2 constraint extraction in DraftModelParser.test.ts. Ensure all edge cases and validation paths are covered for all 8 new constraint types (external_uniqueness, disjunctive_mandatory, exclusion, exclusive_or, subset, equality, ring, frequency). Check that the handler logic in DraftModelParser.ts has no untested branches.","status":"closed","priority":2,"issue_type":"task","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T19:58:22Z","created_by":"Gabe Schenz","updated_at":"2026-03-02T02:42:55Z","closed_at":"2026-03-02T02:42:55Z","close_reason":"PR #61 merged -- 3 equality constraint tests added, all branches now covered","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-bbb","title":"Extract role-level value constraints from transcripts","description":"DraftModelParser currently skips role-level value constraints with the message 'Role-level value constraints from transcripts are not yet supported.' These are value restrictions on a role within a fact type (as opposed to on the value type itself), e.g. 'in the context of grading, only grades A-F are allowed'. The prompt should instruct the LLM to emit these, and the parser should apply them.","status":"closed","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T18:05:11Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T19:14:38Z","closed_at":"2026-03-01T19:14:38Z","close_reason":"Implemented role-level value constraint extraction in DraftModelParser. LLM can now emit values array in inferred_constraints, parser resolves role and applies constraint. PR #55.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-qsq","title":"Show transcript name and LLM model in extraction progress message","description":"When extracting a transcript, the VS Code progress message says 'Extracting ORM model from transcript...' which is generic. It should include the transcript filename and the LLM model being used, e.g. 'Extracting ORM model from transcript clinic-appointments.md with model gpt-5-mini...'. This gives the user better feedback, especially when comparing extraction quality across models.","status":"closed","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T17:25:31Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T20:58:53Z","closed_at":"2026-03-01T20:58:53Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-afb","title":"Disallow fabricated reference modes in extraction prompt","description":"GPT-5 Mini sometimes invents reference_mode values like 'auto_counter (generated OrderLineId)' or 'CourseCode + TermCode (composite of Course and Term) - inferred' which are not valid ORM notation. The prompt should forbid composite or generated reference modes. If identification is unclear, flag it as an ambiguity rather than fabricating a scheme.","status":"closed","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T16:44:31Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T18:19:16Z","closed_at":"2026-03-01T18:19:16Z","close_reason":"Resolved in v0.2.1 prompt (PR #51). No fabricated reference modes in v0.2.1 or v0.2.2 outputs.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-cma","title":"Require data type lengths on text fields in extraction prompt","description":"GPT-5.3 Codex often omits length on text data types while GPT-5 Mini includes them. The prompt should instruct: always provide a length for text data types. Infer reasonable lengths from context (codes 10-20, names 100-200, free text 500+). This ensures the generated schema is more complete for downstream DDL generation.","status":"closed","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T16:44:30Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T18:19:16Z","closed_at":"2026-03-01T18:19:16Z","close_reason":"Resolved in v0.2.1 prompt (PR #51). Both Mini and Codex include data type lengths on all text fields.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-h9q","title":"Strengthen ternary/n-ary fact type guidance in extraction prompt","description":"When transcripts describe constraints spanning 3+ roles (e.g. 'a patient can only have one appointment per time slot on a given day'), GPT-5 Mini fails to model these as multi-role fact types with appropriate uniqueness constraints. The prompt should instruct the LLM to recognize scheduling/composite uniqueness patterns and emit proper n-ary fact types rather than leaving them as prose TODOs.","status":"closed","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T16:44:30Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T18:19:16Z","closed_at":"2026-03-01T18:19:16Z","close_reason":"Resolved in v0.2.2 prompt (PR #53). Mini now produces 5-ary scheduling fact type with multiple uniqueness constraints.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-rq3","title":"Batch transcript import to reduce Copilot premium model quota usage","description":"When using GitHub Copilot as the LLM provider, each separate import consumes premium model quota. Add a batch import mode that processes multiple transcripts in a single LLM session/request where possible, reducing quota consumption. Could be exposed as: multi-select in VS Code file picker, a CLI command accepting multiple files, or a combined prompt that extracts multiple models in one LLM call. Key consideration: need to evaluate whether a single combined prompt produces comparable quality to individual extractions, and whether the Copilot API supports long enough context windows for multiple transcripts.","status":"closed","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T16:32:29Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T19:38:52Z","closed_at":"2026-03-01T19:38:52Z","close_reason":"Merged in PR #56. Batch import command implemented.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-a8c","title":"Add verbose LLM interaction logging to VS Code extension","description":"Add a BARWISE.verboseLogging setting that controls detailed LLM interaction logging in the VS Code output channels. When enabled, log: prompt length and model config, raw JSON response (or truncated), token usage and latency (if reported by provider), full extraction timing. Applies to both Import Transcript and Import dbt commands. Currently the output channels only show post-extraction summaries (ambiguities, warnings, skipped constraints) with no visibility into the LLM call itself.","status":"closed","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T16:30:17Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T20:58:53Z","closed_at":"2026-03-01T20:58:53Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-8zy","title":"Diagram layout: minimize line overlap and distance between connected nodes","description":"Current ELK layout places all object types in one horizontal row and fact types in another, causing long crossing connector lines. Observed in order-management-diagram.png: Product connects across full width to 'Order has OrderStatus', Order connects far right to 'Customer places Order'. Goals: (1) minimize edge crossings, (2) place fact type boxes near their participating object types, (3) reduce total connector length. Investigate ELK layout options: layered algorithm with crossing minimization, port constraints, node ordering strategies.","status":"closed","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T15:07:23Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T20:36:00Z","closed_at":"2026-03-01T20:36:00Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-mq9","title":"Incremental model update workflow for breaking changes","description":"When importing a second conversation transcript (same or different stakeholders), the new information may conflict with or override existing domain knowledge. We need a workflow that: (1) detects breaking changes (renamed entities, removed fact types, changed constraints), (2) presents them clearly to the modeler with before/after context, (3) allows selective acceptance/rejection of each change, and (4) preserves audit trail of why changes were made. The existing diff/merge infrastructure (diffModels, mergeModels, reviewDeltas in ImportTranscriptCommand) handles additive changes but needs to be extended for breaking change scenarios -- e.g. when a fact type's arity changes, when an entity is split or merged, or when a constraint is reversed.","status":"closed","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-02-28T17:23:47Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T01:42:03Z","closed_at":"2026-03-01T01:42:03Z","close_reason":"All 6 stages complete and merged","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-5ln","title":"Annotate .orm.yaml from transcript import with actionable clarification TODOs","description":"When the LLM extracts a model from a transcript, inject TODO(BARWISE) and NOTE(BARWISE) comments directly into the generated .orm.yaml file based on ambiguities, skipped constraints, and missing information.\n\nInstead of generic 'this was ambiguous' flags, each TODO should frame a specific follow-up question the engineer can take back to their domain expert:\n\n- Missing reference mode: 'Ask: How do you uniquely identify a Customer?'\n- Unclear cardinality: 'Ask: Can a Customer place multiple Orders, or only one?'\n- Possible value constraint: 'The transcript mentioned pending/shipped/delivered -- ask: Are those the only valid statuses?'\n- Skipped constraint: 'Sounded mandatory but could not confirm direction -- ask: Must every Order have a Customer?'\n\nData sources already available in DraftModelResult:\n- ambiguities[] (with description and source_references)\n- warnings[]\n- constraintProvenance[] (applied flag, skipReason)\n\nNeeds a new OrmYamlAnnotator (similar pattern to DbtYamlAnnotator and DbtExportAnnotator) that operates at the text level after serialization to preserve YAML formatting. Should be idempotent via stripBARWISEComments().\n\nTies into barwise-8ud (@orm chat participant) -- when the user returns with answers, the participant could re-process and resolve the TODOs.","status":"closed","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-02-28T17:08:46Z","created_by":"Gabe Schenz","updated_at":"2026-02-28T17:36:29Z","closed_at":"2026-02-28T17:36:29Z","close_reason":"Merged PR #37","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-cy6","title":"Add project-level settings to .orm-project.yaml","status":"closed","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-02-27T16:49:46Z","created_by":"Gabe Schenz","updated_at":"2026-02-27T20:41:56Z","closed_at":"2026-02-27T20:41:56Z","close_reason":"Merged in PR #34","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-4zm","title":"Add dbt import and export commands to command palette","description":"Add Import and Export workflow commands to the VS Code command palette.\n\n**ORM: Import...** (quick pick):\n- From Transcript -- existing LLM transcript extraction, renamed from 'Import Transcript'\n- From dbt Project -- prompts for DBT_PROJECT_DIR (folder picker), scans for schema.yml and sources.yml, runs importDbtProject + annotates originals in place, opens resulting .orm.yaml\n\n**ORM: Export...** (quick pick):\n- As dbt Project -- prompts for target DBT_PROJECT_DIR (folder picker), writes schema.yml + model SQL into the project's model directory, runs export annotator on generated files. Needs to understand dbt project structure (model paths, source naming).\n- As DDL -- renderDdl, writes CREATE TABLE statements\n- As Avro Schemas -- renderAvro, writes .avsc files\n\nDesign notes:\n- Import scans the dbt project root for YAML files rather than asking for individual files\n- Export writes INTO an existing dbt project rather than to a standalone output directory\n- Both workflows should respect dbt_project.yml conventions where possible\n\nAlso rename the existing 'ORM: Import Transcript' command to route through the Import picker.\n\nRelated: barwise-8fj (fix double ORM: prefix -- should be resolved as part of this work).","status":"closed","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-02-27T16:28:44Z","created_by":"Gabe Schenz","updated_at":"2026-02-28T17:12:19Z","closed_at":"2026-02-28T17:12:19Z","close_reason":"Merged in PR #36","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-8fj","title":"Fix double ORM: prefix in command palette","description":"Commands in VS Code command palette show 'ORM: ORM: Show Diagram' instead of 'ORM: Show Diagram'. The category prefix is likely duplicated between the package.json command category and the command title itself.","status":"closed","priority":2,"issue_type":"bug","owner":"gschenz@ritchiebros.com","created_at":"2026-02-27T16:27:56Z","created_by":"Gabe Schenz","updated_at":"2026-02-27T22:51:25Z","closed_at":"2026-02-27T22:51:25Z","close_reason":"Merged in PR #35","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-7k8","title":"Add dataType and isPreferred support to LLM extraction pipeline","description":"The LLM extraction pipeline (packages/llm) does not currently extract dataType for value types or mark isPreferred on uniqueness constraints. Models extracted from transcripts are missing this information, which affects relational mapping quality (PK types default to TEXT, preferred identifiers fall back to heuristic).\n\nRequired changes:\n- Add data_type field to ExtractedObjectType in ExtractionTypes.ts\n- Add is_preferred field to InferredConstraint for internal_uniqueness constraints\n- Update ExtractionPrompt to instruct the LLM to infer data types for value types and mark preferred identifiers\n- Update buildResponseSchema() to include data_type and is_preferred in the JSON schema sent to the LLM\n- Update DraftModelParser to propagate dataType when creating object types\n- Update constraint parsing to propagate isPreferred\n- Add tests for all new extraction paths","status":"closed","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-02-27T04:11:09Z","created_by":"Gabe Schenz","updated_at":"2026-02-27T04:38:38Z","closed_at":"2026-02-27T04:38:38Z","close_reason":"Implemented: data_type on ExtractedObjectType, is_preferred on InferredConstraint, prompt/schema/parser updates, 12 new tests. 864 tests passing.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-02t","title":"Enrich NORMA import with missing XML element support","description":"Add support for NORMA XML elements not yet captured: ConceptualDataType (with DataTypes section), role multiplicity, IsImplied on constraints, PreferredIdentifier propagation. Includes adding DataTypeDef to the BARWISE model, updating serialization/schema, and using data types in the relational mapper.","status":"closed","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-02-27T01:58:47Z","created_by":"Gabe Schenz","updated_at":"2026-02-27T04:11:22Z","closed_at":"2026-02-27T04:11:22Z","close_reason":"All 4 stages complete: enriched NORMA intermediate types + parser, added DataTypeDef to model/serialization/schema, data-type-aware relational mapping, isPreferred on uniqueness constraints. Downstream fixes applied to ModelDiff, ModelMerge, and RelationalMapper. 852 tests passing across all packages. Branch feature/norma-import-enrichment ready for PR.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-y2i","title":"Implement NORMA XML import","description":"Add NormaXmlImporter to support importing legacy NORMA .orm files. Requires NormaXmlParser, NormaToOrmMapper, and integration tests with sample NORMA files.","status":"closed","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-02-26T19:25:46Z","created_by":"Gabe Schenz","updated_at":"2026-02-27T01:46:01Z","closed_at":"2026-02-27T01:46:01Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-n60","title":"Implement population instances for constraint validation","description":"Add Population and PopulationInstance models to support sample data instances that validate constraints. Specified in ARCHITECTURE.md Sections 3.1.1 and 3.2 as a Phase 2 deliverable.","status":"closed","priority":2,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-02-26T19:25:42Z","created_by":"Gabe Schenz","updated_at":"2026-02-26T23:41:59Z","closed_at":"2026-02-26T23:41:59Z","close_reason":"Implemented in PR #24: Population model, serialization, validation rules, ModelBuilder support, integration tests. 543 core tests passing.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-1l9","title":"Interactive elicitation: tools ask a clarifying question instead of guessing or erroring","description":"When a barwise tool hits genuine ambiguity, surface a structured question (a form) to the user and act on the answer, rather than guessing, dumping everything, or erroring. Motivating cases: a multi-domain .orm-project.yaml with an action that needs one domain (offer a domain picker); an ambiguous source; a missing-but-required selector. This is an LLM/interaction-layer capability, distinct from the deterministic resolver.\n\nArchitecture note (per interface, no UI in core/MCP-server itself):\n- MCP stdio server has no UI -- use MCP 'elicitation' (server requests structured input, the CLIENT renders it; only works if the client supports it) or return a structured 'needs a choice: [options]' result the calling agent acts on.\n- VS Code Language Model Tools run in the extension host and CAN show a native quick-pick directly.\n- Claude Code / agent clients render an AskUserQuestion-style form.\n\nRelates to barwise-r4f (per-domain vs --domain decision) and the project work; would be the disambiguation mechanism an interview/elicitation flow uses.","acceptance_criteria":"A design (spec) for per-interface interactive elicitation (MCP elicitation/structured-choice result; VS Code quick-pick; agent form), with at least one concrete tool using it (e.g. domain disambiguation on a project source). No silent guessing where a clarifying question is warranted.","status":"open","priority":3,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-06-22T13:10:57Z","created_by":"Claude","updated_at":"2026-06-22T13:10:57Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-x4z","title":"Analyze @barwise/core: stronger functional design and a domain-faithful type system","description":"An analysis pass over @barwise/core (findings + recommendations, not a refactor) along two intertwined axes.\n\n1. Functional design. Survey how functional the core actually is and where it can go further: confirm the deterministic capabilities (validation, verbalization, mapping, diff, query, serialization) are genuinely pure -- same input, same output, no fs/network/clock/randomness and no injected effect ports. Look for hidden mutation, shared mutable state, or effectful seams that crept in. Identify where immutability, total functions (no partial/throwing paths that could be encoded in the return type), and pure transformation pipelines would strengthen the core. The standing commitment from barwise-e8m holds: core stays clean and functional.\n\n2. Domain-faithful, strongly typed core. Review how the TypeScript types model the ORM 2 / Object-Role Modeling domain and how clearly they reflect it. Goal: a strongly typed core whose types ARE the domain model -- a reader should learn ORM from the types. Look at:\n   - Making illegal states unrepresentable (discriminated unions over the metamodel element kinds; exhaustive switches the compiler enforces).\n   - Precise nominal/branded types for identifiers and references (e.g. distinct id types per element kind) instead of bare string.\n   - Tightening loose spots (string unions vs enums, optional fields that encode invariants, any/unknown leakage at boundaries, over-wide config types vs constructed domain types).\n   - Whether the serialized .orm.yaml config shapes and the in-memory domain types are cleanly separated (parse-into-domain at the edge, domain types in the interior).\n   - Parse-don't-validate framing: push validation into type construction so downstream core code receives already-valid domain values.\n\nThe two axes reinforce each other: a stronger type system makes the functional core safer and more self-documenting, and a pure core makes precise types tractable.\n\nOutput: a findings doc enumerating concrete opportunities, each tied to a core principle, ranked by payoff vs blast radius. Relates to barwise-e8m (functional-core tension discussion) and barwise-5d7 (imperative-shell boundary).","acceptance_criteria":"A written findings/recommendations doc covering (a) functional-purity opportunities and (b) type-system/domain-modeling opportunities in @barwise/core, each tied to a design principle and ranked by payoff vs blast radius. Analysis only -- no behavior change and no refactor landed by this issue.","status":"open","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-06-21T22:08:20Z","created_by":"Claude","updated_at":"2026-09-06T03:19:35Z","dependency_count":0,"dependent_count":0,"comment_count":0,"notes":"Partly answered by docs/specs/core-branching-load.spec.md (barwise-924, 2026-09-06) on the type-system axis: it recommends AGAINST the EntityType | ValueType split and against branded ids as standalone work (small payoff, large radius; no id-kind confusion found), and FOR resolving references once at capability boundaries plus one field table per element kind. The purity axis (hidden mutation, effect seams) is not covered there beyond noting skipPlayerValidation is a flag where a type belongs; that half remains this issue's."}
{"_type":"issue","id":"barwise-e8m","title":"Standing discussion: shell duplication vs a clean functional core","description":"Capture and think through the standing tension between two of our principles in the imperative-shell area, without prescribing a fix.\n\nThe tension: DRY (secondary) vs a clean functional core (primary). projectLoader.ts is byte-identical across cli and mcp -- real duplication of project-assembly orchestration that is conceptually pure core logic, stranded in the shells only by an embedded readFileSync. The obvious dedup moves (a shared resolver, or hoisting the orchestration into core behind an injected file-reader port) each pull against keeping core strictly effect-free. We have deliberately chosen, for now, to live with the duplication rather than compromise the core.\n\nThis issue is a place to keep discussing that trade-off as the code evolves -- what we are observing, where the duplication is actually costing us, and how a stronger functional core would reshape the boundary -- NOT to land a specific solution. The one fixed commitment is that the core stays clean and functional (no side effects, no injected effect ports); everything else about how we resolve the shell duplication is open and exploratory.\n\nRelates to barwise-5d7 (imperative-shell naming) and the memory cli-mcp-helpers-folders-are-the-imperative-shell.","acceptance_criteria":"An evolving written discussion of the tension (kept current as we learn more), anchored on the one commitment to a clean functional core. No solution prescribed and no behavior change required by this issue.","status":"open","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-06-21T22:06:12Z","created_by":"Claude","updated_at":"2026-06-21T22:08:29Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-byy","title":"VS Code: Show Diagram / Validate accept a project (domain quick-pick)","description":"Wire the VS Code Show Diagram and Validate commands to open a .orm-project.yaml with a domain quick-pick UI. Deferred from barwise-r4f; see docs/specs/orm-project-surface-wiring.spec.md.","status":"open","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-06-21T19:15:48Z","created_by":"Claude","updated_at":"2026-06-21T19:15:50Z","dependencies":[{"issue_id":"barwise-byy","depends_on_id":"barwise-r4f","type":"blocks","created_at":"2026-06-21T19:15:50Z","created_by":"Claude","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-hwe","title":"Project-level diff for two .orm-project.yaml manifests","description":"barwise diff over two project manifests: per-domain model pairing plus manifest-structure changes (domains added/removed, mapping edits). Needs its own design. Deferred from barwise-r4f; see docs/specs/orm-project-surface-wiring.spec.md.","notes":"Design input (owner, 2026-06-21): version control is a strong assist for semantic diff as a conceptual model evolves. Use case: models saved on disk, diff the SEMANTICS of the working copy against whatever is on main (or any git revision) -- a conceptual changelog of how the model is evolving, not a line diff. Git is the SOURCE of the two snapshots (e.g. `git show main:model.orm.yaml` as base, working tree as incoming); barwise's semantic diff is the engine. The file-objects work (barwise-8sq) helps feed git-retrieved versions directly: base = { content: \u003cgit show output\u003e }, incoming = { path }, no temp files. Applies to single-model diff too, not just project-level -- consider a small 'diff against a git ref' affordance (CLI/MCP) separate from the project-manifest diff design.","status":"open","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-06-21T19:15:48Z","created_by":"Claude","updated_at":"2026-06-21T22:26:54Z","dependencies":[{"issue_id":"barwise-hwe","depends_on_id":"barwise-r4f","type":"blocks","created_at":"2026-06-21T19:15:49Z","created_by":"Claude","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-1np","title":"NORMA export + RT-A + merge id-remap for join constraints","description":"PR 4 of role-path model (5t9.10). Emit join variants to NORMA; add a join-path RT-A fixture (personCountryDemo); extend ModelMerge.remapConstraintIds to remap role ids inside every RolePath. Closes the RT-A guard for joins.","status":"open","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-06-21T00:02:51Z","created_by":"Claude","updated_at":"2026-06-21T00:02:59Z","dependencies":[{"issue_id":"barwise-1np","depends_on_id":"barwise-5t9.10","type":"blocks","created_at":"2026-06-21T00:02:59Z","created_by":"Claude","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-o8i","title":"NORMA import for join role paths","description":"PR 3 of role-path model (5t9.10). Route a NORMA LeadRolePath/JoinRule/ObjectUnifier join role sequence to the matching join_subset/equality/exclusion variant; keep no-join sequences -\u003e flat constraints. Un-drops personCountryDemo on import. XSD-grounded.","status":"open","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-06-21T00:02:51Z","created_by":"Claude","updated_at":"2026-06-21T00:03:00Z","dependencies":[{"issue_id":"barwise-o8i","depends_on_id":"barwise-5t9.10","type":"blocks","created_at":"2026-06-21T00:03:00Z","created_by":"Claude","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-1sk","title":"Join constraint population-satisfaction evaluation","description":"PR 2 of role-path model (5t9.10). Evaluate join_subset/equality/exclusion over a sample population: for each root value, follow each operand path collecting endpoint values, then check subset/equality/disjointness correlated by root. Pure over the population (check-core-purity). PR 1 (representation + structural well-formedness) landed; this adds the satisfaction rule.","notes":"IMPLEMENTED (PR 2 of 5t9.10): per-root endpoint reach over the sample population; subset/equality/exclusion checks (closed-world); registered in populationValidation. Closeable once 5t9.10 wraps.","status":"open","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-06-21T00:02:50Z","created_by":"Claude","updated_at":"2026-06-21T00:12:18Z","dependencies":[{"issue_id":"barwise-1sk","depends_on_id":"barwise-5t9.10","type":"blocks","created_at":"2026-06-21T00:03:00Z","created_by":"Claude","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-q4g","title":"NORMA import/export mapping for derivation rules","description":"WS4 of derived-fact-types (docs/specs/derived-fact-types.spec.md). Map NORMA InformalDerivationRule text + the derivation taxonomy markers (DerivationStorageValues, DerivationCompletenessValues) onto FactType.derivation / SubtypeFact.definingRule, plus symmetric export. The formal FactTypeDerivationRule role-path form is out of scope until the role-path model (barwise-5t9.10). Ground the exact element nesting against ORM2Core.xsd before writing the mapping; defer if uncertain, per the standing NORMA-mapping caution. Core construct (WS1-WS3) already landed.","status":"closed","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-06-19T02:48:29Z","created_by":"Claude","updated_at":"2026-09-06T02:24:06Z","dependencies":[{"issue_id":"barwise-q4g","depends_on_id":"barwise-5t9.2","type":"blocks","created_at":"2026-06-19T02:48:47Z","created_by":"Claude","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0,"closed_at":"2026-09-06T02:24:06Z","close_reason":"Verified on main: NormaXmlParser.parseDerivationRule reads DerivationRule with DerivationCompleteness/DerivationStorage and the informal rule text; writer emits it (defaults omitted); NormaConstructRoundTrip.test.ts 'round-trips the derivation rule'; docs/NORMA_VS_ORM_YAML.md row 214 Yes/Yes/Yes. Formal role-path form stays with barwise-5t9.10 as the issue scoped"}
{"_type":"issue","id":"barwise-34g","title":"NORMA import/export mapping for cardinality constraints","description":"WS4 of object-cardinality (docs/specs/object-cardinality.spec.md). Map ObjectTypeCardinalityRestrictionType -\u003e ObjectType.cardinality and UnaryRoleCardinalityRestrictionType -\u003e CardinalityConstraint, plus symmetric export. Ground the exact CardinalityRanges/CardinalityRange (From/To) nesting against ORM2Core.xsd before writing the mapping; defer if the encoding is uncertain, per the standing NORMA-mapping caution. Core construct (WS1-WS3) already landed.","status":"closed","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-06-19T02:13:03Z","created_by":"Claude","updated_at":"2026-09-06T02:24:06Z","dependencies":[{"issue_id":"barwise-34g","depends_on_id":"barwise-5t9.4","type":"blocks","created_at":"2026-06-19T02:13:16Z","created_by":"Claude","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0,"closed_at":"2026-09-06T02:24:06Z","close_reason":"Verified on main: NormaXmlParser.parseCardinalityRestriction reads ObjectType CardinalityRestriction and UnaryRoleCardinalityConstraint; writer emits both; NormaConstructRoundTrip.test.ts round-trips object-type (bounded/unbounded) and unary-role (bounded/deontic-unbounded) cardinality; docs/NORMA_VS_ORM_YAML.md rows 216-217 Yes/Yes/Yes"}
{"_type":"issue","id":"barwise-5t9.14","title":"NORMA import/export mapping for value-comparison constraints","design":"Map NORMA ValueComparisonConstraint (2-role, no-join) \u003c-\u003e value_comparison. Verify NORMA's Operator enum strings against ORM2Core.xsd before mapping. Importer: NormaXmlTypes constraint union + parser + mapper. Exporter: emit value_comparison in NormaXmlWriter + a RT-A fixture. Also: bump the remaining hardcoded orm_version \"1.0\" emit-points (vscode NewProjectCommand PROJECT_SCAFFOLD, core splitModel fallback) to CURRENT_ORM_VERSION -- harmless (they migrate on load) but should track current.","acceptance_criteria":"NORMA value-comparison constraints import to value_comparison and export back (RT-A over a fixture); scaffold/splitModel emit the current orm_version.","status":"closed","priority":3,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-06-18T14:38:11Z","created_by":"Claude","updated_at":"2026-09-06T13:32:44Z","labels":["formats","interop","metamodel","norma","orm2","value-comparison"],"dependencies":[{"issue_id":"barwise-5t9.14","depends_on_id":"barwise-5t9","type":"parent-child","created_at":"2026-06-18T14:38:10Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0,"notes":"Grounded 2026-09-06: the NORMA mapping half is done -- norma-round-trip-completion.spec.md WS2 (Complete 2026-08-07), parser/writer/serializer all handle ValueComparisonConstraint with the operator map, RT-A fixture in NormaConstructRoundTrip.test.ts. What remains is only the second sentence of the design note: packages/vscode/src/commands/NewProjectCommand.ts PROJECT_SCAFFOLD still emits orm_version \"1.0\" and packages/core/src/project/splitModel.ts falls back to \"1.0\" while CURRENT_ORM_VERSION is \"1.1\". Retitle mentally as 'scaffold/splitModel emit the current orm_version'; small code change, own PR.","closed_at":"2026-09-06T13:32:44Z","close_reason":"Merged in PR #422 (main e53f6ab): scaffold/splitModel half -- PROJECT_SCAFFOLD in packages/vscode/src/commands/projectScaffold.ts emits CURRENT_ORM_VERSION (test projectScaffold.test.ts); splitModel's dead orm_version fallback deleted, RawModelDoc.orm_version required, refusal pinned in tests/project/splitModel.test.ts. Mapping half shipped earlier: NormaXmlParser.ts value_comparison parse + writer, round-tripped in NormaConstructRoundTrip.test.ts."}
{"_type":"issue","id":"barwise-5t9.11","title":"Modeler-defined queries / subqueries","description":"GAP (ORM2Core.xsd: QueryType, QueryDerivationRuleType,\nQueryDerivationPathType, QueryParametersType, SubqueryRef). NORMA lets a\nmodeler define named queries with parameters, derived over a role path,\ncomposable via subqueries -- a derived-predicate capability beyond\nindividual derived fact types. barwise has no query concept.\n\nDepends on the role-path model (sibling issue) and overlaps derivation\n(5t9.2). Lower priority; may be partially out of scope -- decide during\nspec. Filed so the construct is tracked rather than silently unmodeled.\n","acceptance_criteria":"Metamodel can represent a named query (derivation path + parameters) and subquery references; importer reads QueryType/QueryDerivationRule/QueryParameters; documented scope limits.","status":"open","priority":3,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-06-17T02:49:03Z","created_by":"Claude","updated_at":"2026-06-17T02:49:03Z","labels":["advanced","interop","metamodel","norma","orm2"],"dependencies":[{"issue_id":"barwise-5t9.11","depends_on_id":"barwise-5t9","type":"parent-child","created_at":"2026-06-17T02:49:02Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-5t9.12","title":"Model notes / element notes (first-class annotations)","description":"GAP (ORM2Core.xsd: ModelNoteType/ModelNotesType, NoteType/NotesType,\nModelNoteReferencedByType). NORMA supports free-text notes attached to\nthe model or to specific elements, separate from the formal Definition.\nbarwise has `definition` but no general note/annotation channel, so\nNORMA notes are dropped on import. Partial overlap with barwise-rqb\n(user-defined custom attributes) -- reconcile during design.\n\nWork: add an optional notes channel on the model and elements; import\nModelNote/Note; serialize. Small.\n","acceptance_criteria":"Model and individual elements can carry free-text notes distinct from formal definitions; importer reads ModelNote/Note; round-trips through .orm.yaml.","notes":"Implemented (PR pending): a free-text note channel distinct from formal definition -- OrmModel.note (model-level), ObjectType.note, FactType.note, each with config field + getter/setter, serializer round-trip (emitted when non-empty), and JSON Schema property. Additive, no orm_version bump (1.1 open). Deferred: NORMA ModelNote/Note import/export (NORMA's ModelNote + ModelNoteReferencedBy linking and the note/definition distinction need XSD verification); role-level notes (value-type/object/fact notes cover the common case). Overlap with barwise-rqb (custom attributes) noted -- notes are a single free-text channel, custom attributes would be a keyed map; complementary.","status":"in_progress","priority":3,"issue_type":"feature","assignee":"Claude","owner":"noreply@anthropic.com","created_at":"2026-06-17T02:49:03Z","created_by":"Claude","updated_at":"2026-06-19T00:36:50Z","started_at":"2026-06-19T00:30:55Z","labels":["interop","metamodel","norma","orm2"],"dependencies":[{"issue_id":"barwise-5t9.12","depends_on_id":"barwise-5t9","type":"parent-child","created_at":"2026-06-17T02:49:03Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-5t9.10","title":"Role-path / join-rule model (substrate for join constraints, derivation, queries)","description":"GAP (architectural; ORM2Core.xsd: LeadRolePathType, RoleSubPathType,\nJoinRuleType, ObjectUnifierType, PathedRoleType, RootObjectTypeType).\nORM 2 has a general ROLE-PATH model: a path starts at a root object type\nand traverses roles across fact types, joining on shared object types,\nwith object unifiers merging path nodes. This substrate underlies three\nthings barwise lacks: join-path set-comparison/ring constraints (5t9.6),\nderivation rule paths (5t9.2), and modeler-defined queries (sibling\nissue). The flat role-id sequences barwise uses today cannot express it.\n\nWork (spec-first; larger design): design the role-path representation and\nits serialization, then layer the dependents on it. 5t9.6 depends on this.\n","acceptance_criteria":"A role-path representation (lead role path from a root object type, sub-paths, joins on shared object types, object unifiers) usable by set-comparison constraints, derivation rules, and queries; importer reads LeadRolePath/JoinRule/ObjectUnifier.","notes":"PR (revised representation) implemented per the canonical-research finding: JoinOperand = RolePath + projection (node indices); join_subset/equality/exclusion compare projected tuple SETS (Halpin's role-sequence-projected-from-a-join-path), not single endpoints. Updated model/serde/schema (join_operand_def)/structural validation (projection range, arity + column-type agreement)/tuple-set population evaluation/verbalization/diff. Single-column is projection length 1; PR-1's endpoint behavior is the [root,endpoint] special case. NORMA substrate (purpose tags, RolePathOwner, calculated projections) intentionally not copied.","status":"in_progress","priority":3,"issue_type":"feature","assignee":"Claude","owner":"noreply@anthropic.com","created_at":"2026-06-17T02:49:02Z","created_by":"Claude","updated_at":"2026-06-21T02:12:18Z","started_at":"2026-06-20T23:49:38Z","labels":["advanced","interop","metamodel","norma","orm2"],"dependencies":[{"issue_id":"barwise-5t9.10","depends_on_id":"barwise-5t9","type":"parent-child","created_at":"2026-06-17T02:49:02Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":4,"comment_count":0}
{"_type":"issue","id":"barwise-5t9.8","title":"Multi-role (role-sequence) frequency constraints","description":"GAP (minor). FrequencyConstraint binds a single role\n(Constraint.ts:227 `readonly roleId: string`). ORM 2 frequency\nconstraints can range over a role SEQUENCE (a combination of roles), e.g.\n\"each (Room, TimeSlot) combination is booked 0..1 times\". Multi-role\nNORMA frequency constraints can't be represented and are dropped/narrowed.\n\nWork: allow FrequencyConstraint to take a role-id sequence (keep single\n-role as the common case); update import, verbalization, validation.\n","acceptance_criteria":"FrequencyConstraint can span a role sequence, not just a single role; importer maps NORMA multi-role FrequencyConstraint; verbalization handles it.","status":"closed","priority":3,"issue_type":"feature","assignee":"Claude","owner":"noreply@anthropic.com","created_at":"2026-06-17T01:58:50Z","created_by":"Claude","updated_at":"2026-06-20T23:36:26Z","started_at":"2026-06-19T13:58:28Z","closed_at":"2026-06-20T23:36:26Z","close_reason":"Implemented: FrequencyConstraint.roleId -\u003e roleIds (role sequence); back-compat serialization (role/roles); per-tuple validation; multi-role verbalization; diff; NORMA multi-role round-trip included. Tier 2 complete.","labels":["constraints","interop","metamodel","norma","orm2"],"dependencies":[{"issue_id":"barwise-5t9.8","depends_on_id":"barwise-5t9","type":"parent-child","created_at":"2026-06-17T01:58:49Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-5t9.6","title":"Set-comparison and ring constraints over join paths","description":"GAP (advanced). barwise's set-comparison constraints reference flat role\n-id sequences (SubsetConstraint.subsetRoles/supersetRoles,\nEqualityConstraint, ExclusionConstraint). There is no join-path concept\n(rg join/role-path/rolepath in core/src/model finds nothing relevant).\n\nORM 2 allows these constraints to range over JOIN PATHS -- role sequences\nthat traverse several fact types joined on shared object types (e.g. a\nsubset constraint between a path A-plays-B-in-FT1 and B-relates-C-in-FT2).\nNORMA persists these as multi-fact RoleSequences with join semantics.\nWithout a join-path model, such constraints can't round-trip and complex\nNORMA models lose them.\n\nWork: design a join-path representation (ordered hops over fact types,\njoined on object types) usable by subset/equality/exclusion/ring; parse\nNORMA's joined RoleSequences. Larger design effort -- spec first.\n","acceptance_criteria":"Subset/equality/exclusion (and ring) constraints can reference role sequences defined over a join path spanning multiple fact types; importer reads NORMA join-path role sequences; documented limitations for v1.","notes":"XSD: set-comparison constraints use ConstraintRoleSequenceWithJoinType over the role-path model (LeadRolePath/JoinRule/ObjectUnifier). This is an instance of the general role-path substrate filed as .13; .6 depends on .13.","status":"open","priority":3,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-06-17T01:58:49Z","created_by":"Claude","updated_at":"2026-06-17T02:49:05Z","labels":["advanced","constraints","interop","metamodel","norma","orm2"],"dependencies":[{"issue_id":"barwise-5t9.6","depends_on_id":"barwise-5t9.10","type":"blocks","created_at":"2026-06-17T02:49:05Z","created_by":"Claude","metadata":"{}"},{"issue_id":"barwise-5t9.6","depends_on_id":"barwise-5t9","type":"parent-child","created_at":"2026-06-17T01:58:48Z","created_by":"Claude","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-5t9.7","title":"Default values on value types and roles","description":"GAP (minor). No default-value concept (rg default in Role.ts/ObjectType.ts\nfinds nothing). NORMA/ORM 2 allow a default value on a value type or a\nrole. Useful for relational mapping (SQL DEFAULT) and lost on import.\n\nWork: optional `defaultValue?: string` on ObjectType (value types) and/or\nRole; import from NORMA; thread into DDL rendering.\n","acceptance_criteria":"Value types/roles can carry a default value; importer reads NORMA default values; relational mapping can emit DEFAULT where applicable.","notes":"Implemented (PR pending): ObjectType.defaultValue (value-type default) + config + getter; serializer round-trip (default_value, emitted when present); JSON Schema property; and the DDL payoff -- Column.defaultValue, threaded by the relational mapper from the value type onto its column, rendered as SQL DEFAULT (numeric/boolean bare, else quoted with '' escaping). No orm_version bump (1.1 open). Deferred: NORMA import/export of DefaultValue -- NORMA's exact DefaultValue representation (attribute vs element, InvariantDefaultValue) needs XSD verification, like value-comparison's operator enum; folded into the NORMA-mapping follow-on. Role-level defaults also deferred (value-type default is the common/DDL-relevant case).","status":"in_progress","priority":3,"issue_type":"feature","assignee":"Claude","owner":"noreply@anthropic.com","created_at":"2026-06-17T01:58:49Z","created_by":"Claude","updated_at":"2026-06-18T18:55:15Z","started_at":"2026-06-18T18:49:07Z","labels":["interop","metamodel","norma","orm2"],"dependencies":[{"issue_id":"barwise-5t9.7","depends_on_id":"barwise-5t9","type":"parent-child","created_at":"2026-06-17T01:58:49Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-5t9.5","title":"Independent (reference-independent) object types","description":"GAP. ObjectType has no `independent` flag (rg independent in\nObjectType.ts finds only an unrelated data-type comment). In ORM 2 an\nINDEPENDENT object type (drawn with an open dot) may have instances that\nparticipate in no fact other than its own reference -- i.e. it is not\nexistentially dependent on playing any asserted role. This matters for\nreference schemes and for correct mandatory-role inference.\n\nWork: add optional `independent?: boolean` (default false) to ObjectType,\nimport NORMA's IsIndependent attribute, and surface it in\nverbalization/diagram. Low effort, improves fidelity.\n","acceptance_criteria":"ObjectType can be marked independent; importer reads NORMA's independent flag; verbalization/diagram note it; default false.","notes":"Implemented (PR pending): ObjectType.independent (default false) + config; serializer round-trip (emitted only when true); JSON Schema property; completeness-warning exemption (independent types are not flagged 'isolated'); and the full NORMA round-trip -- parser reads IsIndependent, mapper maps it, writer+serializer emit IsIndependent (RT-A test). No orm_version bump (1.1 already open). Remaining: diagram open-dot marker for independent types is a @barwise/diagram follow-on (verbalization has no object-type-property slot today; the ADR 'verbalizable' gate is satisfied conceptually).","status":"in_progress","priority":3,"issue_type":"feature","assignee":"Claude","owner":"noreply@anthropic.com","created_at":"2026-06-17T01:58:48Z","created_by":"Claude","updated_at":"2026-06-18T18:43:24Z","started_at":"2026-06-18T18:32:51Z","labels":["interop","metamodel","norma","orm2"],"dependencies":[{"issue_id":"barwise-5t9.5","depends_on_id":"barwise-5t9","type":"parent-child","created_at":"2026-06-17T01:58:48Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-e6n","title":"Verify R2RML/RML export status; build if missing","description":"Per discussion, R2RML (W3C standard for declaring how relational rows map to RDF triples) was thought to be on the roadmap or already implemented. But the current core/src/export/ directory only contains Avro, dbt, DDL, and OpenAPI exporters — no R2RML.\n\nR2RML addresses the real-world scenario the W3C-stack section of the comparison doc names explicitly: relational database of record, but want to publish as ontology-backed graph. Pairs naturally with the OWL/SHACL connector (barwise-e8c) — together they cover the 'relational + RDF' workflow that ORM2 is uniquely positioned to drive from a single conceptual model.\n\nScope of THIS issue: investigate. If R2RML is genuinely missing, this issue converts into the build task. If it's somewhere I missed, close as resolved.\n\nInterface surface (if built):\n- CLI: 'barwise export --format=r2rml'\n- MCP: existing export_model tool picks it up via FormatDescriptor registry\n- VS Code: export commands","acceptance_criteria":"Status confirmed (present/absent). If absent and we decide to build: R2RML output validates against a reference tool (Ontop or Morph-KGC); CLI/MCP/VS Code all expose it","status":"open","priority":3,"issue_type":"task","owner":"gschenz@ritchiebros.com","created_at":"2026-05-11T22:25:56Z","created_by":"Gabe Schenz","updated_at":"2026-05-11T22:25:56Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-m5r","title":"Move web viewer mockup from Claude Designer into repo","description":"ORM2 scores 'None' on the Cloud/web tooling row of the conceptual-modeling-approaches matrix — NORMA is Visual Studio only. Barwise can close that gap with even a read-only web viewer: 'share this model with a stakeholder via a URL.'\n\nA mockup exists in Claude Designer under a personal account. First step is to move it into this repo so it can be iterated on with everyone.\n\nFuture scope (separate issues once mockup is in repo):\n- Static HTML/SVG export of a model (no server needed)\n- Interactive viewer with verbalization tooltips\n- Hosted viewer (likely a small Vercel/Cloudflare deployment) for sharing\n\nInterface surface (eventually):\n- Standalone web interface — new surface\n- CLI: 'barwise serve' or 'barwise viewer build' to generate the static artifact\n- Possibly MCP: a 'render_for_web' tool\n\nScope of THIS issue: just import the mockup files and document where they live. Implementation issues to follow.","acceptance_criteria":"Mockup files committed under a clearly named directory (e.g. packages/viewer/ or apps/viewer/); README points to it","status":"open","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-05-11T22:25:51Z","created_by":"Gabe Schenz","updated_at":"2026-05-11T22:25:51Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-tju","title":"Add sample population to an example .orm.yaml file","description":"The ORM metamodel and serialization already support sample populations (instances populating fact types, validated against constraints in real time). Per discussion: this is one of the most stakeholder-friendly features. Combined with verbalization, populations are the killer demo — 'here's the rule in English, here's data that satisfies it, here's data that violates it.'\n\nBut no example .orm.yaml currently demonstrates populations. New users and reviewers don't see the feature exists.\n\nScope: pick an existing example (or add a new small one), add a sample population that exercises multiple constraint types — mandatory, uniqueness, value constraint, maybe a ring constraint. Make sure validation catches deliberate violations and lets correct rows pass.\n\nInterface surface:\n- Examples/docs only — no code interface change\n- But: VS Code population validation UI, CLI 'barwise validate', and MCP 'validate_model' should all light up usefully when run on the example","acceptance_criteria":"Example .orm.yaml includes a populated section; running validation surfaces both passes and intentional failures; example is referenced from docs/README","status":"open","priority":3,"issue_type":"task","owner":"gschenz@ritchiebros.com","created_at":"2026-05-11T22:25:45Z","created_by":"Gabe Schenz","updated_at":"2026-05-11T22:25:45Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-v9o","title":"LLM-assisted derivation rule elicitation (English to formal rule)","description":"ORM2 derivation rules define a fact type whose population is computed from other fact types (e.g., 'Person is a Senior Citizen if Person has Age \u003e 65'). FORML (ORM's textual rule language) is limited and academically oriented.\n\nTwo complementary pieces:\n1. A formal rule language as a target: likely Datalog-style over the metamodel, leveraging the symbolic query API (barwise-6i6). 'A derivation rule is a named query.'\n2. LLM-assisted elicitation: user describes a rule in English; LLM translates to the formal language; user reviews and saves. Mirrors how verbalization works in the other direction.\n\nKey principle: LLM is a translator, NOT a reasoner. The rule itself is formal and executable; the LLM just helps author it.\n\nInterface surface:\n- @barwise/core: rule language parser + evaluator (built on query API)\n- CLI: 'barwise rule add' / 'barwise rule list' / 'barwise rule eval'\n- MCP: 'propose_rule' tool (LLM-assisted authoring) + 'evaluate_rule' tool (execution)\n- VS Code: chat agent supports 'add a derivation rule that...' workflow; rules editor in webview\n\nDepends on barwise-6i6 (symbolic query API).","acceptance_criteria":"Formal rule language documented; round-trip English -\u003e rule -\u003e verbalization works; evaluator produces correct derivations on test models; all three interfaces support the workflow","status":"open","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-05-11T22:25:40Z","created_by":"Gabe Schenz","updated_at":"2026-06-17T01:55:14Z","dependencies":[{"issue_id":"barwise-v9o","depends_on_id":"barwise-6i6","type":"blocks","created_at":"2026-05-13T14:31:00Z","created_by":"auto-import","metadata":"{}"},{"issue_id":"barwise-v9o","depends_on_id":"barwise-5t9.2","type":"blocks","created_at":"2026-06-17T02:00:22Z","created_by":"Claude","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-hpc","title":"Strengthen @barwise chat to delegate deterministic questions to symbolic query API","description":"The @barwise VS Code chat agent currently answers questions about ORM models primarily via LLM prompting. For deterministic questions (what fact types involve X, what constraints reference role Y, etc.) this is wasteful and untrustworthy. The agent should delegate these to the symbolic model query API (barwise-6i6) before falling back to LLM reasoning.\n\nTwo distinct concerns to separate:\n- Symbolic Q\u0026A over the model — deterministic, no LLM needed (use query API)\n- Inferred entailments, natural-language synthesis, ambiguity resolution — LLM appropriate\n\nPattern: the chat agent receives a question, attempts to classify it as symbolic vs reasoning, calls the appropriate path. LLM still does English-to-query translation and result narration.\n\nInterface surface:\n- VS Code: @barwise chat participant — primary consumer\n- MCP: the same delegation logic should be exposed so external agents (not just VS Code) get the same benefit\n- CLI: less direct, but 'barwise query' subcommand gives the same surface for scripting\n\nDepends on barwise-6i6 (symbolic model query API).","acceptance_criteria":"At least 5 representative deterministic questions answered without LLM call; chat agent transcripts show clear delegation; same behavior available via MCP","status":"open","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-05-11T22:25:34Z","created_by":"Gabe Schenz","updated_at":"2026-06-17T01:55:14Z","dependencies":[{"issue_id":"barwise-hpc","depends_on_id":"barwise-6i6","type":"blocks","created_at":"2026-05-13T14:30:59Z","created_by":"auto-import","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-amg","title":"Reference ontology import connectors (schema.org, FIBO, QUDT)","description":"Add import connectors for well-known reference ontologies so modelers can pull in standard vocabularies rather than re-defining concepts. This addresses the 'no IRI / no linked-data' gap in ORM2 by anchoring object types and value types to external definitions.\n\nLikely an epic with one connector per vocabulary:\n- schema.org (general purpose, very broad)\n- FIBO (financial industry business ontology)\n- QUDT (units / quantities / dimensions)\n- Possibly others (DCAT for data catalogs, FOAF, etc.)\n\nPlugs into the FormatDescriptor registry.\n\nInterface surface:\n- CLI: 'barwise import --format=schema-org --filter=\u003c...\u003e' or 'barwise reference add \u003ciri\u003e'\n- MCP: import_model tool, or a new reference_lookup tool\n- VS Code: a 'browse reference ontologies' UI or command palette entry\n\nOpen design questions:\n- Do we materialize imports into the .orm.yaml, or maintain references to external IRIs?\n- How does this interact with our (currently absent) IRI support on object types?","acceptance_criteria":"At least schema.org import works end-to-end; modeler can anchor an ORM object type to a referenced concept; CLI/MCP/VS Code all support the workflow","status":"open","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-05-11T22:25:18Z","created_by":"Gabe Schenz","updated_at":"2026-05-11T22:25:18Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-4ch","title":"Property Graph Schema (PG-Schema) export connector","description":"Add a format connector for PG-Schema (LDBC, 2023) and/or Neo4j Cypher schema. Property graphs are the increasingly common 'I want a graph but OWL is too much' middle ground, and ORM2's attribute-free nature is a near-perfect source for property graphs.\n\nPlugs into the FormatDescriptor registry in @barwise/core.\n\nInterface surface:\n- CLI: 'barwise export --format=pg-schema' (and possibly --format=cypher)\n- MCP: existing export_model tool picks it up via the registry\n- VS Code: register in export commands\n\nMapping notes:\n- ORM object types -\u003e node labels\n- ORM binary fact types -\u003e relationship types (or properties on nodes for single-valued)\n- ORM objectified facts -\u003e intermediate nodes\n- ORM uniqueness / mandatory -\u003e PG-Schema key / cardinality constraints","acceptance_criteria":"PG-Schema output parses with a reference validator; CLI/MCP/VS Code can all invoke export","status":"open","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-05-11T22:25:11Z","created_by":"Gabe Schenz","updated_at":"2026-05-11T22:25:11Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-e4k","title":"Write 'Splitting a model into an OrmProject' guide","description":"Write a user-facing guide under docs/ that walks through converting a monolithic .orm.yaml into a multi-file OrmProject. The conceptual treatment in ARCHITECTURE.md §3.1.4 covers the metamodel but not the workflow. The guide should use auction.orm.yaml as the worked example, splitting it into catalog/auctions/payments/parties domains with context mappings between them. Cover: when to split (signals you've outgrown a single file), how to identify bounded contexts, the .orm-project.yaml manifest, .map.yaml for cross-context entity mappings, the 'context:ObjectType' qualified-ref syntax, and how to load/validate/diagram the result. Cross-link from docs/CLI.md, docs/MCP.md, and examples/README.md.","acceptance_criteria":"docs/ORM_PROJECT_GUIDE.md (or similar) exists with a worked auction.orm.yaml split example; CLI.md/MCP.md/examples/README.md link to it; a new reader can follow the steps and end up with a working multi-domain project.","status":"open","priority":3,"issue_type":"task","owner":"gschenz@ritchiebros.com","created_at":"2026-04-10T15:40:48Z","created_by":"Gabe Schenz","updated_at":"2026-06-17T01:55:14Z","dependencies":[{"issue_id":"barwise-e4k","depends_on_id":"barwise-r4f","type":"blocks","created_at":"2026-04-10T10:41:07Z","created_by":"auto-import","metadata":"{}"},{"issue_id":"barwise-e4k","depends_on_id":"barwise-gfz","type":"blocks","created_at":"2026-04-10T10:41:08Z","created_by":"auto-import","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-gfz","title":"CLI tooling to scaffold an OrmProject from a monolithic model","description":"Add CLI commands to make project scaffolding ergonomic. Today converting auction.orm.yaml into a multi-domain project means hand-editing YAML and cutting entities across files — error-prone and tedious. Proposed commands: (a) 'barwise project init \u003cname\u003e' creates a new empty .orm-project.yaml with the standard directory layout, (b) 'barwise project split \u003csource.orm.yaml\u003e --domains catalog,auctions,payments' interactively or by config assigns entities/facts to domains and writes the resulting files plus suggested context mappings for any entity referenced across seams. The split command should produce a valid project on output (round-trip loads cleanly) and flag entities that need manual resolution (multiple seams, ambiguous ownership).","design":"Start with 'barwise project init' (trivial). For 'split', start with a config-driven mode: user supplies a YAML file mapping entity IDs to domain names, tool does the cut. Interactive/LLM-assisted modes can follow later.","acceptance_criteria":"'barwise project init' produces a valid empty project; 'barwise project split' with a config file cuts auction.orm.yaml into the 4 subdomains from the discussion and the output loads, validates, and diagrams cleanly.","status":"open","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-04-10T15:40:41Z","created_by":"Gabe Schenz","updated_at":"2026-04-10T15:40:41Z","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"barwise-un8","title":"Subdomain categorization for entities and fact types","description":"## Problem\n\nAn ORM model has a single top-level 'domain_context' string, but individual\nentity types and fact types have no way to be tagged by subdomain. In a\nrealistic model, many entities naturally belong to sub-domains like\n'transaction', 'catalog', 'customer', 'fulfillment', etc. Users need a way\nto understand which parts of a model belong to which subdomain, filter\nby subdomain, and answer questions like \"show me everything in the\ntransaction domain.\"\n\n## Current state\n\n- 'OrmModel.domainContext' is a single string on the whole model\n- 'ObjectType' and 'FactType' have no domain/category/tag fields\n- 'OrmProject' supports multi-file models where each file has its own\n  'domain_context' — that covers cross-file separation but not within-file\n  subdomains\n- 'DiagramLayout' can define named visual views that subset the model,\n  but those are visual groupings, not semantic domain tags\n- 'describeDomain' tool operates on the whole model; it does not filter\n  by subdomain\n\n## Design space to explore\n\n1. **Explicit domain tags** — add an optional 'domain' (or 'domains: string[]')\n   field to ObjectType and FactType. Simple, explicit, but puts the burden\n   on the modeler to tag things consistently.\n2. **Named subdomains as first-class elements** — a top-level 'subdomains'\n   list, each naming a set of entity/fact type IDs. Benefits: supports\n   overlapping subdomains, displayable in the sidebar, can be used for\n   filtering and scoped exports.\n3. **Derived clustering** — infer subdomains automatically from graph\n   structure (connected components, louvain community detection, shared\n   fact types). No modeler burden, but fuzzy and may not match intent.\n4. **Namespace-prefixed names** — e.g. 'Transaction.Payment' → derived\n   from naming convention. Discourages good naming practice.\n5. **Hybrid** — first-class subdomains (option 2) with LLM-assisted\n   suggestion of subdomain membership from the model structure + names.\n\n## Downstream uses\n\n- Sidebar: group entities/facts by subdomain\n- Diagram: filter to a single subdomain (similar to named views)\n- describeDomain: filter by subdomain\n- Export (DDL, OpenAPI): scoped export of a subdomain\n- Impact analysis: \"changes to X affect which subdomains\"\n- LLM context: when merging incoming changes, group related elements\n  by subdomain for clearer diffs and verbalizations\n\n## Not yet decided\n\n- Should subdomains be hierarchical (Transaction \u003e Payment \u003e Refund)?\n- Can an entity belong to multiple subdomains? (Probably yes.)\n- How do subdomains interact with the existing 'OrmProject' multi-file\n  domain_context — are they nested, parallel, or the same thing?\n- Is this a core metamodel change (breaks serialization) or an\n  annotation layer?\n\n## Acceptance criteria\n\nThis issue is scoping/design only. A follow-up implementation issue\nwill be filed once the design direction is chosen. Success here means:\n\n- [ ] ADR-style document capturing the chosen design (under docs/)\n- [ ] Serialization impact assessed (round-trip, migration path)\n- [ ] Impact on downstream consumers documented (sidebar, diagram,\n      describeDomain, exports)","status":"open","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-04-10T15:25:51Z","created_by":"Gabe Schenz","updated_at":"2026-06-17T01:55:14Z","dependencies":[{"issue_id":"barwise-un8","depends_on_id":"barwise-rqb","type":"blocks","created_at":"2026-04-10T10:37:56Z","created_by":"auto-import","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-44e","title":"Expose sidebar/diagram interactions as LLM-accessible tools","description":"The sidebar and diagram now support click-to-highlight, orientation toggle, position save, and model browsing. These same capabilities should be available to LLMs via the MCP server and Copilot chat tools so that an AI assistant can navigate, highlight, and manipulate diagrams programmatically. Examples: 'highlight Customer and its connections', 'save the current diagram layout', 'list all fact types for Order'. Bridges the gap between UI-driven and conversational workflows.","status":"open","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-04-06T01:27:11Z","created_by":"Gabe Schenz","updated_at":"2026-04-06T01:27:11Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-jti","title":"dbt source/staging YAML generation from diagram view","description":"Generate dbt source and staging model YAML from a view's entities. Column names, data types, and descriptions come from the ORM model's entity types, fact types, and definitions. Bridges conceptual modeling directly into the analytics engineering workflow.","status":"open","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-04-06T00:26:32Z","created_by":"Gabe Schenz","updated_at":"2026-04-06T00:26:32Z","dependencies":[{"issue_id":"barwise-jti","depends_on_id":"barwise-803","type":"blocks","created_at":"2026-04-05T19:26:52Z","created_by":"auto-import","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-1jr","title":"Data dictionary export from diagram view","description":"Generate a human-readable data dictionary document from a view's elements. Lists entities with definitions, fact type readings, constraints, and data types. Output as Markdown or HTML. Useful for stakeholder review and documentation deliverables.","status":"open","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-04-06T00:26:30Z","created_by":"Gabe Schenz","updated_at":"2026-04-06T00:26:30Z","dependencies":[{"issue_id":"barwise-1jr","depends_on_id":"barwise-803","type":"blocks","created_at":"2026-04-05T19:26:52Z","created_by":"auto-import","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-lcz","title":"Scoped OpenAPI export from diagram view","description":"Export OpenAPI schema for only the entities in the active view. Uses the existing OpenAPI format support scoped to the view's element subset.","status":"open","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-04-06T00:26:27Z","created_by":"Gabe Schenz","updated_at":"2026-04-06T00:26:27Z","dependencies":[{"issue_id":"barwise-lcz","depends_on_id":"barwise-803","type":"blocks","created_at":"2026-04-05T19:26:51Z","created_by":"auto-import","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-b9t","title":"Scoped DDL export from diagram view","description":"Export DDL (CREATE TABLE statements) for only the entities in the active view, not the full model. Uses the existing relational mapping engine scoped to the view's element subset.","status":"open","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-04-06T00:26:26Z","created_by":"Gabe Schenz","updated_at":"2026-04-06T00:26:26Z","dependencies":[{"issue_id":"barwise-b9t","depends_on_id":"barwise-803","type":"blocks","created_at":"2026-04-05T19:26:51Z","created_by":"auto-import","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-87a","title":"View switching in sidebar","description":"Extend the sidebar model browser to show named views. Click a view to open it in the diagram panel. Active view highlighted. Add/remove elements from views via right-click context menu.","status":"open","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-04-06T00:26:19Z","created_by":"Gabe Schenz","updated_at":"2026-04-06T00:26:19Z","dependencies":[{"issue_id":"barwise-87a","depends_on_id":"barwise-803","type":"blocks","created_at":"2026-04-05T19:26:50Z","created_by":"auto-import","metadata":"{}"},{"issue_id":"barwise-87a","depends_on_id":"barwise-7dc","type":"blocks","created_at":"2026-04-05T19:26:50Z","created_by":"auto-import","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-803","title":"Named diagram views with element subsets","description":"Support multiple named views per model, each containing a subset of elements. Create views from: subtype tree of an entity, N-degree neighborhood, or manual selection. Auto-include fact types that connect included entities so views are structurally complete. Store view definitions in the diagrams section of .orm.yaml.","status":"closed","priority":3,"issue_type":"feature","assignee":"Gabe Schenz","owner":"gschenz@ritchiebros.com","created_at":"2026-04-06T00:26:17Z","created_by":"Gabe Schenz","updated_at":"2026-04-06T17:14:15Z","closed_at":"2026-04-06T17:14:15Z","close_reason":"Implemented named diagram views with element subsets. Save View button in hop bar captures current filtered elements. Views stored in .orm.yaml with element names. Clicking a saved view in the sidebar loads it. Auto-includes connecting fact types and subtype facts.","dependencies":[{"issue_id":"barwise-803","depends_on_id":"barwise-cl2","type":"blocks","created_at":"2026-04-05T19:26:49Z","created_by":"auto-import","metadata":"{}"},{"issue_id":"barwise-803","depends_on_id":"barwise-7bp","type":"blocks","created_at":"2026-04-05T19:26:49Z","created_by":"auto-import","metadata":"{}"}],"dependency_count":2,"dependent_count":6,"comment_count":0}
{"_type":"issue","id":"barwise-dks","title":"Multi-view diagrams with submodel export","description":"Support multiple named diagram views per .orm.yaml model, each containing a subset of elements with persisted positions. Views serve as scoped lenses (e.g., a report's data requirements, a type hierarchy). Key capabilities: (1) diagrams section in .orm.yaml schema, (2) sidebar tree view for navigation, (3) add/remove elements from views, (4) auto-include connecting fact types, (5) export a view's submodel as DDL, OpenAPI, or data dictionary. Inspired by NORMA's multi-diagram support (ormDiagram:ORMDiagram sections with per-shape AbsoluteBounds). Analytic use case: model known data domains, create views scoped to reports/deliverables, identify structural gaps, export scoped artifacts.","status":"open","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-04-06T00:22:54Z","created_by":"Gabe Schenz","updated_at":"2026-06-17T01:55:14Z","dependencies":[{"issue_id":"barwise-dks","depends_on_id":"barwise-3wc","type":"blocks","created_at":"2026-04-05T19:23:04Z","created_by":"auto-import","metadata":"{}"},{"issue_id":"barwise-dks","depends_on_id":"barwise-7bp","type":"blocks","created_at":"2026-04-05T19:26:53Z","created_by":"auto-import","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-q4q","title":"LLM transcript import: emit candidate constraints with TODO annotations","description":"The LLM transcript extraction currently produces fact types without constraints, even when the transcript contains clear signals like 'exactly one', 'at most one', 'identified by', etc. The LLM should infer likely uniqueness and mandatory constraints from these phrases and emit them with TODO/NOTE annotations (similar to how it already annotates ambiguous identifiers) so the modeler can confirm, adjust, or remove them. This reduces the manual work needed after import while keeping the human in the loop.","status":"open","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-04-04T00:03:12Z","created_by":"Gabe Schenz","updated_at":"2026-04-04T00:03:12Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-6u5","title":"Verbalization on entity selection","description":"When an entity type is clicked in the diagram webview, display verbalizations of its fact types in a tooltip or side panel. Reuses click handler infrastructure from the focus subset feature. Requires integration with the verbalization engine from @barwise/core.","status":"open","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-04-03T23:02:00Z","created_by":"Gabe Schenz","updated_at":"2026-04-03T23:02:00Z","dependencies":[{"issue_id":"barwise-6u5","depends_on_id":"barwise-prm","type":"blocks","created_at":"2026-04-03T18:02:08Z","created_by":"auto-import","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-96l","title":"Add import dbt CLI subcommand","description":"Core has a fully implemented importDbtProject() pipeline for reverse-engineering ORM models from dbt projects but no CLI subcommand exposes it. Add 'BARWISE import dbt \u003cdir\u003e' with directory scanning and schema.yml parsing. Estimated ~100 lines.","status":"open","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-02T03:45:54Z","created_by":"Gabe Schenz","updated_at":"2026-03-02T03:45:54Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-a6e","title":"Add export avro CLI subcommand","description":"Core has a fully implemented renderAvro() function for generating Apache Avro schema definitions (.avsc JSON) but no CLI subcommand exposes it. Add 'BARWISE export avro' following the same pattern as export dbt and export openapi. Estimated ~50 lines.","status":"open","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-02T03:45:53Z","created_by":"Gabe Schenz","updated_at":"2026-03-02T03:45:53Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-cb6","title":"Add import norma CLI subcommand","description":"Core has a fully implemented importNormaXml() pipeline (parse, map, validate) for importing NORMA .orm XML files but no CLI subcommand exposes it. Add 'BARWISE import norma \u003cfile\u003e' following the same pattern as import transcript. Estimated ~70 lines.","status":"closed","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-02T03:45:53Z","created_by":"Gabe Schenz","updated_at":"2026-08-30T13:25:56Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-30T13:25:56Z","close_reason":"Done and verified end to end. `barwise import norma \u003cfile\u003e` exists (packages/cli/src/commands/import/norma.ts, wired through getImporter('norma')), and running it on packages/formats/tests/fixtures/employeeProject.orm imports 4 object types and 3 fact types and writes valid .orm.yaml.\n\nFound stale by the barwise-912 pilot: norma-export.spec.md's Status claims WS1 and WS2 implemented while all three issues its Tracking names were open. This one was simply never closed. That matters beyond the tidy-up, because barwise-912's proposed remedy is to DERIVE a spec's Status from the beads store -- which only works if the store is accurate. Here 1 of 3 was not. Deriving is still the right direction; it just needs the store's own staleness treated as part of the problem rather than assumed away.\n\nThe other two stay open on purpose: barwise-e3g has one manual NORMA-load check of an exported diagram outstanding (the spec says so), and barwise-5t9 is a live epic with 15 children in mixed states."}
{"_type":"issue","id":"barwise-nsn","title":"Extract objectified fact types from transcripts","description":"The metamodel supports objectified fact types (a fact type that is also treated as an entity type, e.g. 'Enrollment' objectifying 'Student enrolls in CourseOffering'). The extraction prompt and DraftModelParser do not handle objectification. The LLM could be instructed to identify when a relationship is itself referenced as an entity in other fact types.","status":"closed","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T18:05:16Z","created_by":"Gabe Schenz","updated_at":"2026-03-02T03:12:44Z","closed_at":"2026-03-02T03:12:44Z","close_reason":"PR #63 merged. Objectification extraction added to LLM pipeline.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-c7j","title":"Extract frequency constraints from transcripts","description":"The metamodel supports frequency constraints ('each X plays this role at least N / at most M times', e.g. 'each Employee works on at least 1 and at most 3 Projects'). Not handled in extraction.","status":"closed","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T18:05:01Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T19:56:05Z","closed_at":"2026-03-01T19:56:05Z","close_reason":"Implemented in PR #57","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-wiw","title":"Extract ring constraints from transcripts","description":"The metamodel supports ring constraints (irreflexive, asymmetric, intransitive, acyclic, etc. on self-referencing fact types, e.g. 'a Person cannot manage themselves' or 'the reports-to hierarchy must be acyclic'). Not handled in extraction.","status":"closed","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T18:04:57Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T19:56:07Z","closed_at":"2026-03-01T19:56:07Z","close_reason":"Implemented in PR #57","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-c8w","title":"Extract equality constraints from transcripts","description":"The metamodel supports equality constraints (two role populations are equal, e.g. 'a Person teaches a Course if and only if that Person is qualified for that Course'). Not handled in extraction.","status":"closed","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T18:04:52Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T19:56:05Z","closed_at":"2026-03-01T19:56:05Z","close_reason":"Implemented in PR #57","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-rq4","title":"Extract subset constraints from transcripts","description":"The metamodel supports subset constraints (one role population is a subset of another, e.g. 'every Person who manages a Department must also work in that Department'). Not handled in extraction.","status":"closed","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T18:04:48Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T19:56:07Z","closed_at":"2026-03-01T19:56:07Z","close_reason":"Implemented in PR #57","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-8xm","title":"Extract exclusive-or constraints from transcripts","description":"The metamodel supports exclusive_or (exactly one of several roles must be played -- combines disjunctive mandatory + exclusion, e.g. 'every Payment is exactly one of CashPayment, CardPayment, or CheckPayment'). Not handled in extraction.","status":"closed","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T18:04:44Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T19:56:04Z","closed_at":"2026-03-01T19:56:04Z","close_reason":"Implemented in PR #57","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-84w","title":"Extract exclusion constraints from transcripts","description":"The metamodel supports exclusion constraints (two role combinations cannot both occur, e.g. 'a Person cannot be both a Student and an Instructor in the same Department'). Not handled in extraction prompt, ExtractionTypes, or DraftModelParser.","status":"closed","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T18:04:37Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T19:56:04Z","closed_at":"2026-03-01T19:56:04Z","close_reason":"Implemented in PR #57","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-fu1","title":"Extract disjunctive mandatory constraints from transcripts","description":"The metamodel supports disjunctive_mandatory (at least one of several roles must be played, e.g. 'every Person must have either a Phone or an Email'). Not handled in extraction prompt, ExtractionTypes, or DraftModelParser.","status":"closed","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T18:04:33Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T19:56:06Z","closed_at":"2026-03-01T19:56:06Z","close_reason":"Implemented in PR #57","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-lwu","title":"Extract external uniqueness constraints from transcripts","description":"The metamodel supports external_uniqueness (uniqueness spanning roles across multiple fact types, e.g. composite identification of CourseOffering by Course+Semester). The extraction prompt and DraftModelParser do not handle this constraint type. ExtractionTypes.ts InferredConstraintType only allows internal_uniqueness | mandatory | value_constraint.","status":"closed","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T18:04:27Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T19:56:06Z","closed_at":"2026-03-01T19:56:06Z","close_reason":"Implemented in PR #57","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-t5a","title":"Stale TODO for OrderStatus value constraint that was actually applied","description":"Line 4 of order-management.orm.yaml has a TODO saying 'Role-level value constraints from transcripts are not yet supported' for OrderStatus, but the value constraint was successfully applied on the OrderStatus value type (lines 68-73). The TODO is stale/incorrect -- the annotator should not emit it when the constraint was handled at the value-type level.","status":"closed","priority":3,"issue_type":"bug","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T15:19:29Z","created_by":"Gabe Schenz","updated_at":"2026-03-02T02:44:51Z","closed_at":"2026-03-02T02:44:51Z","close_reason":"Already resolved -- annotator no longer emits stale TODO for value constraints applied at value-type level. Historical transcript files retain the old output as versioned snapshots.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-myh","title":"Excessive positional-fallback NOTE annotations","description":"The OrmYamlAnnotator emits 17 NOTE annotations saying 'could not precisely match role ... Using positional fallback' for constraints that were all correctly applied. The constraint matcher should recognize common patterns (e.g., role names matching object type names) to reduce noise. The positional fallback itself works fine but the volume of notes drowns out important annotations.","status":"closed","priority":3,"issue_type":"chore","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T15:19:28Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T18:20:10Z","closed_at":"2026-03-01T18:20:10Z","close_reason":"Resolved by v0.2.2 prompt improvements. LLM now produces constraint role references that the annotator matches without positional fallback.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-cjt","title":"Add OpenAPI export format","description":"Add an OpenAPI (Swagger) export format that generates an OpenAPI 3.x spec from an ORM model. Entity types become schemas, fact types inform endpoints and relationships. Would extend the existing export command alongside dbt, ddl, and avro formats.","status":"closed","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T15:11:01Z","created_by":"Gabe Schenz","updated_at":"2026-03-02T03:27:52Z","closed_at":"2026-03-02T03:27:52Z","close_reason":"PR #64 merged. OpenAPI 3.0 export format added to core renderer and CLI.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-om7","title":"MCP docs: add GitHub Copilot setup instructions","status":"closed","priority":3,"issue_type":"bug","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T15:03:20Z","created_by":"Gabe Schenz","updated_at":"2026-03-02T02:53:41Z","closed_at":"2026-03-02T02:53:41Z","close_reason":"PR #62 merged -- GitHub Copilot setup instructions added to docs/MCP.md","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-93q","title":"Review internal naming for semantic precision","description":"Audit internal type names, field names, and variable names across all packages for semantic precision. A project that models business domains using ubiquitous language should hold itself to the same standard internally.\n\nKnown examples to review:\n- MergeValidationResult.errors -- field name is accurate (contains only errors) but the comment and usage patterns could be clearer\n- Survey all exported interfaces and types for naming that could mislead consumers\n\nScope: @BARWISE/core public API surface first, then internal naming, then downstream packages.","status":"closed","priority":3,"issue_type":"task","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T14:34:26Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T22:19:38Z","closed_at":"2026-03-01T22:19:38Z","close_reason":"PR #60 merged -- all 5 renames landed across 23 files in 4 packages","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-brt","title":"Annotate dbt export output with TODO/NOTE comments","description":"When rendering dbt exports (schema.yml, model SQL) from an ORM model via renderDbt(), inject TODO/NOTE comments for gaps in the model that affect the export quality. Examples: missing column descriptions, ambiguous constraint mappings, value types without data_type (defaulting to TEXT), fact types without readings. Follows the same pattern as DbtYamlAnnotator used for dbt imports.","status":"closed","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-02-27T12:48:50Z","created_by":"Gabe Schenz","updated_at":"2026-02-27T16:03:52Z","closed_at":"2026-02-27T16:03:52Z","close_reason":"Implemented in PR #33","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-4hg","title":"Import dbt project and infer ORM facts from project structure","description":"Reverse-engineer an ORM 2 model from an existing dbt project. Infer entity types from model names, fact types from relationships (refs), value types from column definitions, and constraints from tests (unique, not_null, accepted_values). Identify gaps where dbt structure is ambiguous -- e.g. missing reference modes, unclear cardinality, denormalized columns that could represent multiple facts. Output should be a draft OrmModel that a modeler can refine, with completeness warnings highlighting what could not be inferred.","status":"closed","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-02-27T11:38:55Z","created_by":"Gabe Schenz","updated_at":"2026-02-27T12:05:14Z","closed_at":"2026-02-27T12:05:14Z","close_reason":"Implemented dbt project importer with parser, mapper, gap report, and 39 tests","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-c16","title":"Add validation warnings for dataType and isPreferred consistency","description":"Add two completeness/consistency validation rules:\n\n1. Warn when a value type has no dataType set. Value types should ideally declare their conceptual data type so the relational mapper can generate accurate column types instead of defaulting to TEXT.\n\n2. Warn when an entity type has zero isPreferred uniqueness constraints (no preferred identifier declared) or more than one isPreferred constraint (conflicting preferred identifiers). This helps catch modeling issues early.\n\nThese are non-blocking warnings (not errors) in the validation engine, similar to existing completeness warnings in src/validation/rules/completenessWarnings.ts.\n\nFiles to modify:\n- src/validation/rules/completenessWarnings.ts (or new file)\n- tests/validation/completenessWarnings.test.ts","status":"closed","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-02-27T04:11:16Z","created_by":"Gabe Schenz","updated_at":"2026-02-27T11:13:21Z","closed_at":"2026-02-27T11:13:21Z","close_reason":"Implemented two completeness warning rules for dataType and isPreferred consistency","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-bwh","title":"Extend population validation to Phase 2 constraints (subset, ring, exclusion, equality)","status":"closed","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-02-26T22:39:04Z","created_by":"Gabe Schenz","updated_at":"2026-02-27T11:33:28Z","closed_at":"2026-02-27T11:33:28Z","close_reason":"Implemented population validation for all 5 Phase 2 constraint types with 26 new tests","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-r20","title":"Integration exports (dbt, CI/CD validator, MCP server)","description":"Build downstream integration points: dbt model export, CI/CD validation CLI, and MCP server for external tool access. These are separate projects that consume @BARWISE/core.","status":"closed","priority":3,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-02-26T19:25:58Z","created_by":"Gabe Schenz","updated_at":"2026-02-27T11:46:53Z","closed_at":"2026-02-27T11:46:53Z","close_reason":"Implemented dbt and Avro renderers alongside existing DDL renderer","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-sa4","title":"Decide VS Code Validate command vs LSP project diagnostics","description":"Once the LSP handles .orm-project.yaml, the Validate Model command is largely redundant; decide deprecation. Deferred from barwise-r4f; see docs/specs/orm-project-surface-wiring.spec.md.","status":"open","priority":4,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-06-21T19:15:49Z","created_by":"Claude","updated_at":"2026-06-21T19:15:50Z","dependencies":[{"issue_id":"barwise-sa4","depends_on_id":"barwise-r4f","type":"blocks","created_at":"2026-06-21T19:15:50Z","created_by":"Claude","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-5t9.13","title":"Dynamic (state-transition) rules","description":"GAP / SCOPE QUESTION (ORM2Core.xsd: DynamicRuleType, DynamicRuleNodeState,\nGeneralRuleType). NORMA supports dynamic rules that constrain state\ntransitions (before/after model states), as opposed to the static\nconstraints barwise models. This is exotic and arguably outside barwise's\nconceptual-modeling scope.\n\nFiled at low priority as a decision: either model it or record it as an\nexplicit non-goal so the omission is intentional, not accidental.\n","acceptance_criteria":"Decision recorded on whether barwise models dynamic rules; if yes, metamodel + importer support DynamicRuleType; if no, documented as an explicit non-goal.","status":"open","priority":4,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-06-17T02:49:04Z","created_by":"Claude","updated_at":"2026-06-17T02:49:04Z","labels":["advanced","interop","metamodel","norma","orm2"],"dependencies":[{"issue_id":"barwise-5t9.13","depends_on_id":"barwise-5t9","type":"parent-child","created_at":"2026-06-17T02:49:04Z","created_by":"Claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-xtw","title":"test issue (will delete)","description":"test write","status":"closed","priority":4,"issue_type":"task","owner":"gschenz@ritchiebros.com","created_at":"2026-05-11T21:12:03Z","created_by":"Gabe Schenz","updated_at":"2026-05-11T21:12:18Z","closed_at":"2026-05-11T21:12:18Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-e3g","title":"NORMA XML diagram export (round-trip)","description":"Export barwise models back to NORMA .orm XML format, including diagram positions. Combined with existing NORMA import, this enables round-trip interop with the NORMA toolset.","status":"open","priority":4,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-04-06T00:26:40Z","created_by":"Gabe Schenz","updated_at":"2026-04-06T00:26:40Z","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"barwise-b2m","title":"Visual lineage: entity usage across views","description":"Show where an entity appears across all named views. Provides cross-view lineage for understanding how entities connect across different business contexts. Supports impact analysis -- if an entity changes, which views are affected.","status":"open","priority":4,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-04-06T00:26:38Z","created_by":"Gabe Schenz","updated_at":"2026-04-06T00:26:38Z","dependencies":[{"issue_id":"barwise-b2m","depends_on_id":"barwise-803","type":"blocks","created_at":"2026-04-05T19:26:52Z","created_by":"auto-import","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-3wc","title":"Research OMG Ontology and EKG standards for barwise integration","description":"Investigate connections between ORM 2 fact-based modeling and OMG working groups: (1) OMG Ontology group (https://www.omg.org/ontology/) and (2) Enterprise Knowledge Graphs (https://www.omg.org/ekg/, https://ekgf.org/). ORM's situation semantics foundation may align well with ontological and knowledge graph representations. Explore whether barwise could serve as a bridge between business-readable ORM models and these standard formats. This is exploratory research, not implementation.","status":"open","priority":4,"issue_type":"task","owner":"gschenz@ritchiebros.com","created_at":"2026-04-05T15:44:11Z","created_by":"Gabe Schenz","updated_at":"2026-04-05T15:44:11Z","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"barwise-bii","title":"Add standalone merge command to CLI","description":"Core has mergeModels() and mergeAndValidate() for three-way model merging, but it is only used implicitly during 'import transcript' when an existing file is present. Add a standalone 'BARWISE merge \u003cbase\u003e \u003cours\u003e \u003ctheirs\u003e' command for collaborative modeling workflows. Estimated ~70 lines.","status":"open","priority":4,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-02T03:45:54Z","created_by":"Gabe Schenz","updated_at":"2026-03-02T03:45:54Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-d4y","title":"Extract aliases/synonyms from transcripts","description":"The metamodel supports aliases on ObjectType (synonym tracking). The extraction prompt does not instruct the LLM to identify when stakeholders use different names for the same concept and record them as aliases (vs. flagging as ambiguity). Example: 'Client' and 'Customer' used interchangeably could be captured as aliases.","status":"closed","priority":4,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-03-01T18:05:21Z","created_by":"Gabe Schenz","updated_at":"2026-03-02T03:38:03Z","closed_at":"2026-03-02T03:38:03Z","close_reason":"Implemented in PR #65 — aliases extracted from LLM transcripts and passed through to OrmModel","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-8ud","title":"Add @orm Copilot Chat participant for guided domain elicitation","description":"Implement a VS Code Chat Participant (@orm) that guides users through structured ORM domain elicitation directly in the Copilot Chat panel.\n\nThe participant would:\n- Walk users through a structured interview (identify entities, reference modes, relationships, cardinality, constraints, value restrictions)\n- Build the ORM model incrementally with feedback at each step\n- Use the existing LLM extraction pipeline from @BARWISE/llm\n- Generate the .orm.yaml file when the conversation is complete\n- Solve both the 'what questions to ask' and 'import the result' problems in a single interaction\n\nThis is more impactful than a static conversation guide because it lives where engineers already work (Copilot Chat panel), provides real-time guidance, and doesn't require a separate transcript import step.\n\nRequires the VS Code Chat Participant API (vscode.chat.createChatParticipant).","status":"open","priority":4,"issue_type":"feature","owner":"gschenz@ritchiebros.com","created_at":"2026-02-28T01:58:40Z","created_by":"Gabe Schenz","updated_at":"2026-03-01T20:59:59Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-pf2","title":"OSI (Open Semantic Interchange) interoperability","description":"Treat the Open Semantic Interchange (OSI) ontology spec as an interop target for barwise. OSI (github.com/open-semantic-interchange/OSI, Apache-2.0, spec v0.2.0.dev0) is an emerging vendor-agnostic semantic-interchange standard (YAML+JSON) with reference converters for dbt, GoodData, Polaris, and Salesforce. It is ORM-rooted and overlaps heavily with the barwise metamodel (EntityType/ValueType, roles, verbalizes patterns, identify_by preferred identifiers, built-in datatypes). This epic groups the work to interoperate with OSI and to close two capability gaps it surfaced (derivation expressions; logical-schema ingestion).","notes":"Source: comparison performed 2026-06-23 against the raw OSI ontology spec. barwise remains richer on the ORM 2 constraint taxonomy (ring/exclusion/exclusive-or/subset/equality/frequency) and on DDD bounded-context mapping / data products; no action needed there.","status":"open","priority":1,"issue_type":"epic","owner":"noreply@anthropic.com","created_at":"2026-06-23T14:48:32Z","created_by":"claude","updated_at":"2026-06-23T14:48:32Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-pf2.1","title":"OSI import/export format adapter","description":"Add an 'osi' FormatDescriptor (importer + exporter) to the format registry so barwise can read and write OSI ontology files. Reuse existing plumbing: barwise/packages/core/src/format/types.ts (FormatDescriptor), formats.ts (registration), registry.ts. Mapping: OSI concept -\u003e ObjectType (EntityType/ValueType -\u003e kind); relationships -\u003e FactType/Role; identify_by -\u003e preferred identifier; multiplicity (ManyToOne/OneToOne) -\u003e internal uniqueness + mandatory; verbalizes -\u003e ReadingOrder; requires -\u003e value/role constraints; extends -\u003e SubtypeFact; built-in datatypes -\u003e value types.","design":"Key reshaping concern: OSI is concept-centric (relationships nested under the concept that plays the first role, OO dot-notation), whereas barwise keeps fact types as top-level fact-centric elements. The adapter must lift nested OSI relationships into standalone FactTypes on import and re-nest on export. Spec-first per repo conventions. derived_by and full requires-expression fidelity depend on the derivation-expression work (sibling issue) -- scope v1 to structural concepts/relationships/constraints and round-trip what maps cleanly.","acceptance_criteria":"Round-trip an OSI example (e.g. the TPC-DS sample under OSI examples/) through import then export with no structural loss for supported constructs; unit tests in packages/core covering the concept/relationship/identifier/multiplicity/verbalization mappings.","status":"open","priority":1,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-06-23T14:48:58Z","created_by":"claude","updated_at":"2026-06-23T14:48:58Z","dependencies":[{"issue_id":"barwise-pf2.1","depends_on_id":"barwise-pf2","type":"parent-child","created_at":"2026-06-23T14:48:57Z","created_by":"claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-pf2.2","title":"Derivation expression language for derived fact types/concepts","description":"Design a deterministic expression layer (parser + evaluator) in @barwise/core covering OSI derived_by semantics: dot-notation role navigation, boolean/comparison/aggregation operators, and recursion. OSI uses derived_by on both concepts (e.g. EXISTS(Person.earns)) and relationships (e.g. Person.parent_of(descendant) OR Person.ancestor_of.parent_of(descendant)). barwise currently has no formal expression language for derived fact types -- only prose definitions.","design":"Must preserve the core determinism principle: same input, same output, no I/O. Core-only (no llm/cli deps). Spec-first. Enables higher-fidelity OSI round-trip and barwise-native derived metrics for data products (see ARCHITECTURE.md 3.1.5).","status":"open","priority":2,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-06-23T14:48:59Z","created_by":"claude","updated_at":"2026-06-23T14:48:59Z","dependencies":[{"issue_id":"barwise-pf2.2","depends_on_id":"barwise-pf2","type":"parent-child","created_at":"2026-06-23T14:48:58Z","created_by":"claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-pf2.3","title":"Logical-schema to model mapping (reverse Rmap / DB ingestion)","description":"Add generic mapping from a logical/SQL schema into an OrmModel, analogous to OSI concept_mappings: object_mappings (SQL expressions populating concept instances), referent_mappings (resolving compound/entity identity through identifier relationships), and link_mappings (tree-structured patterns mapping logical data to relationship links at varying arities). Complements the existing forward Rmap in barwise/packages/core/src/mapping/ (ORM -\u003e relational DDL) and the NORMA XML importer, which are currently the only ingestion paths.","design":"OSI's mapping direction is logical-schema UP into the ontology -- the inverse of Rmap. This is the bridge that lets barwise consume existing warehouse schemas. Likely lands as an importer in the format/import layer rather than core/mapping. Spec-first.","status":"open","priority":2,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-06-23T14:48:59Z","created_by":"claude","updated_at":"2026-06-23T14:48:59Z","dependencies":[{"issue_id":"barwise-pf2.3","depends_on_id":"barwise-pf2","type":"parent-child","created_at":"2026-06-23T14:48:59Z","created_by":"claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-pf2.4","title":"ADR: OSI vs barwise comparison and interop strategy","description":"Record an Architecture Decision Record under barwise/docs/decisions/ capturing the OSI-vs-barwise comparison and the decision to treat OSI as an interoperability format (import/export via the FormatDescriptor registry) rather than a metamodel replacement. Summarize the verified overlaps (EntityType/ValueType, roles, verbalization, preferred identifiers, built-in datatypes) and the divergences (OSI concept-centric organization, first-class derivation expressions, logical-schema-up mapping; barwise richer ORM 2 constraints and DDD bounded-context/data-product modeling).","status":"open","priority":3,"issue_type":"decision","owner":"noreply@anthropic.com","created_at":"2026-06-23T14:49:00Z","created_by":"claude","updated_at":"2026-06-23T14:49:00Z","dependencies":[{"issue_id":"barwise-pf2.4","depends_on_id":"barwise-pf2","type":"parent-child","created_at":"2026-06-23T14:49:00Z","created_by":"claude","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-804","title":"spec-writer: fold barwise domain modeling into the spec flow","description":"Prototype: the spec-writer flow gains an optional 'model the domain' step -- when a spec introduces or reshapes a domain (new artifact kinds, file formats, persistent records, schema changes), author a companion .orm.yaml of the core domain, validated in CI (examples/models/ is already globbed by validate:examples).\n\nProven by the learning-design spec (PR #265): examples/models/learning-design.orm.yaml encodes C1 as a mandatory functional ternary, the catalog as 'each ReductiveBias is caught by exactly one CheckKind', and C6's card anatomy as mandatory Diagnosis/ReadingReference roles -- making the missing-diagnosis-field class of spec bug structurally inexpressible. Verbalization doubles as the articulation skill's verbalization test made mechanical; constraints are sensemaking anchors made executable.\n\nScope to consider:\n1. spec-writer SKILL.md: a workflow step and a design-gate item ('domain-shaped spec ships a CI-validated companion model'), with criteria for when a spec earns a model (domain reshaping: yes; pure code movement: usually no).\n2. template.spec.md: a companion-model section stub.\n3. Bug-revisit protocol: when a bug traces to a wrong domain assumption, evolve the ORM model first (barwise diff shows the semantic change), then the spec prose, then the code.\n4. Further out: use @barwise/code-analysis importers to extract a model FROM the implementation and barwise diff it against the spec's model -- a real spec-vs-code conformance check, closing the 'have we satisfied the requirements' loop deterministically.\n\nOrigin: session discussion on PR #265; the model-as-machine-checkable-core framing is already in the spec's 'The standard, as a barwise model' section.","status":"open","priority":2,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-07-25T17:55:00Z","created_by":"claude","updated_at":"2026-07-25T17:55:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-805","title":"Ambiguity should carry a questions list, not just a description","description":"The extraction prompt asks the model to state the question behind each ambiguity. The schema has nowhere to put one, so whether you get a question is luck.\n\nsystemPrompt.ts:112 instructs: \"For each ambiguity, describe the specific question that needs to be answered\", and all eight review categories model that in their examples. But the type carries only `description` and `source_references`. The question, when asked at all, is prose buried in the description; ReasoningTrail.ts:45 reduces each ambiguity to a.description, so nothing downstream could find it even when it is there.\n\nWhat that produces, from the recorded order-management payload: \"Stakeholders use 'customer' (CRM) and 'client' (billing) to refer to what appears to be the same concept, but billing only considers someone a client once they have paid. This may require separate entity types or a subtype relationship.\" Detection is excellent -- a real overloaded term with two source references -- but it is an observation plus a modeling suggestion, and a modeler still has to derive what to ask. A different run of the same pipeline produced \"The stakeholders should clarify whether MembershipId exists or if composite identification is intended\", which is actionable. Nothing structural holds the extractor to the second form.\n\nProposal: add `questions: readonly string[]` to Ambiguity. A list, not a single string, because one fork routinely needs more than one thing settled -- the customer/client case raises both \"are these one concept or two?\" and \"does 'client' status begin at first payment, making it a state rather than a type?\", and either answer alone leaves the model underdetermined.\n\nWhy it matters: an ambiguity is an agenda item for the next working session. An observation is not actionable; a question is. It also unblocks measurement -- requires_ambiguity (PR #302) substring-matches the description, so it scores topic detection and cannot tell a sharp question from a vague note. With a declared questions list a check can assert questions are present and non-empty: structural, deterministic, no LLM judge, which the harness rejects on principle.\n\nBlast radius: ExtractionTypes.ts, prompt/responseSchema.ts, the parse in ExtractionPrompt.ts:65-79, ReasoningTrail.ts, the MCP import formatter, the VS Code display, and every recorded fixture payload under packages/llm/tests/fixtures/ and packages/promptlab/tests/fixtures/responses/.\n\nTo decide in the spec: optional vs required in the response schema. Required is what actually enforces the behavior, but it changes the prompt contract, can shift extraction, and would need re-measuring through `barwise prompt eval` -- which needs an API key that is not yet configured. Optional avoids forcing a re-baseline and leaves existing fixtures valid.\n\nSpec before code, per project convention. Related: docs/specs/eval-transcript-realism.spec.md, whose non-goals exclude scoring ambiguity prose quality because it needs a judge -- a structured questions list is the route that does not. Mirrored on GitHub as issue #303.","status":"open","priority":2,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-08-20T11:45:00Z","created_by":"claude","updated_at":"2026-08-20T11:45:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-806","title":"prompt eval folds failed runs into the mean as zeros","description":"A run that never reached the model is scored 0 and averaged in as if the model produced a terrible extraction. runSuite.ts:82 catches every exception into {error}, and the aggregation does runs.map((r) =\u003e r.score?.score ?? 0), so an auth failure, a rate limit, or a dropped connection is indistinguishable in the mean from a genuine bad score.\n\nCONFIRMED IN THE FIRST REAL USE (2026-08-20). Four runs against claude-haiku-4-5 produced four history rows, three of them junk:\n  12:23 - all seven cases 0.000 (total failure: key set but not exported)\n  12:27 - order-management 0.907, other six 0.000 (partial failure)\n  12:31 - all seven cases 0.000 (total failure)\n  12:44 - all seven non-zero, mean 0.781 (mostly real)\nThe 12:27 row is the diagnostic one: order-management is first in the manifest and runSuite walks cases in declared order, so three calls succeeded and every subsequent call failed -- the shape of a rate limit, not of a bad model. And the 12:44 row is not clean either: university-enrollment shows mean 0.356 with worst 0.000, so at least one of its three runs scored a hard zero, and nothing in the record says whether that was a failed call or a genuine miss on the known-bimodal case.\n\nAlso worth noting from the same session: all four rows record artifactVersion 1.0.0, the default. No tuned variant was measured, and nothing in the output made that obvious at a glance.\n\nRESOLUTION (agreed 2026-08-20): retry is the primary mechanism, but it does not replace the recording fix -- the two failure classes observed above need different handling.\n\n1. Retry with exponential backoff for transient failures: 429, 5xx, timeouts, connection resets. This is what the 12:27 row needed.\n2. Fail fast for terminal failures: 401/403 auth, 400 malformed request. Retrying a bad key just fails more slowly, which is what the 12:23 and 12:31 rows would have done.\n3. Either way, a run that never produced a payload must not enter the mean as a zero. Exclude it, carry a failure count on CaseSummary and SuiteReport, and print it.\n4. The history writer should refuse to record a run with any surviving failure, or record it with an explicit sample count and a failure count so a later reader can tell a partial run from a complete one. Refusing is simpler and safer.\n\nA parse failure is a separate case and should stay a zero: the model answered, the answer was unusable, that is a real score. Only failures that produced no response at all are excluded.\n\nOpen question for the fix: where retry belongs. Putting it in runSuite keeps it scoped to the eval lane; putting it in the provider clients under @barwise/llm helps every caller including MCP and VS Code, but widens the blast radius and needs its own decision about defaults. Recommend runSuite first, since the eval lane is where an unattended sweep makes a failure expensive.\n\nClosed: shipped earlier in this session. runSuite now separates a provider that never answered (excluded from the mean, counted as a failure) from a model that answered unusably (a real zero), with retry classification in run/retry.ts and appendRunHistory refusing an incomplete run unless forced.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-20T12:30:00Z","created_by":"claude","updated_at":"2026-08-21T03:00:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-807","title":"CLI bin is not executable after a fresh clone, install, and build","description":"`npx barwise` fails with \"Permission denied\" on a fresh checkout. packages/cli build is plain `tsc`, which emits dist/index.js at mode 644, and there is no chmod step. npm sets the executable bit on bin targets during install, but dist/ is gitignored, so on a fresh clone the file does not exist at install time and npm has nothing to chmod. The documented sequence -- npm install \u0026\u0026 npm run build -- therefore reliably produces a bin that cannot be executed.\n\nWorkarounds today: `node packages/cli/dist/index.js ...`, which ignores the mode, or a manual chmod +x, or re-running npm install after the build.\n\nProposed: a postbuild step on @barwise/cli using node rather than the shell, so it works on Windows too:\n  \"postbuild\": \"node -e \\\"require('node:fs').chmodSync('dist/index.js', 0o755)\\\"\"\n\nCI never caught this because the release workflow bundles through esbuild and invokes the bundle directly, never the bin symlink. Worth a check in the CLI test plan (test-plan/cli-checks.sh) so a regression is caught by the release gate.\n\nRESOLVED 2026-08-20. Wider than filed: three built files carry a shebang and none got the bit. packages/cli/dist/index.js (tsc, the bin target that failed with Permission denied), and both esbuild bundles -- packages/cli/dist/bundle/index.cjs and packages/mcp/dist/bundle/index.cjs, the latter being the barwise-mcp bin -- whose own header comment says they are meant to run \"directly, via the shebang\".\n\nFixed where each file is produced: a postbuild script on @barwise/cli for the tsc output, and a chmodSync at the end of each esbuild.mjs. node's chmodSync rather than a shell chmod, so Windows is unaffected.\n\nGuarded in test-plan/cli-checks.sh, which the release workflow runs as a gate. The check was verified to fail when the bit is removed (gate exits 1), not merely to pass when it is present.\n\nCost before the fix: two eval runs. Once as Permission denied on npx barwise, and once as a stale dist/ that silently ran the old harness and produced three history rows without sample counts.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-20T12:30:00Z","created_by":"claude","updated_at":"2026-08-20T19:05:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-808","title":"Edge release displays a stale date, hiding that its assets are current","description":"The edge pre-release shows a publish date two weeks older than its actual build, so it reads as stale when the downloads are current.\n\nVerified on 2026-08-20: all four assets (CLI bundle, MCP bundle, VSIX, SHA256SUMS) carry updated_at 2026-08-20T12:03:44Z, one minute after the #302 merge, and the release body records the real build commit ee35478. But published_at is frozen at 2026-08-07T11:46:23Z and target_commitish still points at 042657380e, a June commit. GitHub surfaces the stale fields.\n\nCause is deliberate and worth keeping: release.yml never moves the edge tag, because a moving tag makes every contributor's git pull fail with \"would clobber existing tag\". `gh release edit --draft=false` on an already-published release does not bump published_at.\n\nProposed: put the build commit and date in the release title, which is the most prominent field in the releases list, via one added flag on the gh release edit call already in the workflow:\n  --title \"Edge build (${GITHUB_SHA::7} - $(date -u +%Y-%m-%d))\"\n\nNo tag movement, so the git pull hazard stays solved, and the listing stops claiming a two-week-old build.\n\nRESOLVED 2026-08-20. The edge release title now carries the build commit and date: \"Edge build \u003cshort-sha\u003e (\u003cYYYY-MM-DD\u003e)\". Set on both branches of the step -- the gh release edit path, which previously set no title at all and left the stale \"Edge build (latest main)\" in place, and the gh release create path.\n\nThe tag still never moves, so the git pull hazard that motivated the fixed tag stays solved, and published_at and target_commitish stay stale as before. The difference is that the releases list no longer shows only stale fields: the title is the most prominent thing on the page and now says when the assets were actually built.\n\nNot runnable outside CI, so validated three ways short of a live run: the YAML parses and the step still triggers on push with a title on both branches; the run block passes bash -n; and the title expression was executed against this repo's HEAD, producing \"Edge build 2354177 (2026-08-20)\".","status":"closed","priority":3,"issue_type":"chore","owner":"noreply@anthropic.com","created_at":"2026-08-20T12:30:00Z","created_by":"claude","updated_at":"2026-08-20T19:00:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-809","title":"CLI has no review command, though MCP and VS Code both expose it","description":"reviewModel (packages/llm/src/review/reviewModel.ts:288) is registered as the MCP review_model tool and called from packages/vscode/src/mcp/ToolRegistration.ts:248. packages/cli/src contains no reference to it and cli.ts registers no review command, so a CLI user cannot review a model while users of either other surface can.\n\nFound by the cross-surface matrix in docs/unwired-capability-audit-2026-08-20.md.\n\nThe work is small -- reviewModel takes an OrmModel and an LlmClient, both of which the CLI already constructs for `barwise import transcript`. `barwise review \u003cfile\u003e [--focus ...]` mirroring the MCP tool's options is the obvious shape.\n\nNeeds a key to exercise, like every LLM surface. Decide alongside barwise-810 (merge): either the CLI gets both and the surfaces converge, or the divergence gets written down as deliberate. What should not persist is undocumented divergence, which is how the parity claim in CLAUDE.md survived unchallenged.\n\nClosed by docs/specs/cli-surface-parity.spec.md. `barwise review \u003cfile\u003e` ships with --focus, the same provider options as `import transcript`, and --format text|json.\n\nThe question this issue raised -- decide alongside merge, either the surfaces converge or the divergence gets written down -- was answered by doing both. Review now reaches all three surfaces.\n\nOne design call worth recording: review always exits 0 when the review completes, whatever it says. No --fail-on flag. The suggestions are model-generated advice, and a command that failed a build on them would put an LLM in the merge path, where a bad day for the provider becomes a red build for everyone. A user who wants a gate pipes --format json through jq, which keeps the policy in their hands.\n\nTested with a mock client, per the llm package convention that no test makes a real call. A key exercises it against a provider; it is not needed to verify the command works.","status":"closed","priority":2,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-08-20T19:30:00Z","created_by":"claude","updated_at":"2026-08-21T03:20:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-810","title":"merge reaches only the MCP surface","description":"registerMergeTool is in the MCP tool barrel. Neither the CLI nor the VS Code extension references mergeModels -- the narrowest reach of any shipped capability, one surface out of three.\n\nFound by the cross-surface matrix in docs/unwired-capability-audit-2026-08-20.md.\n\nUnlike review, merge needs no LLM and no key: it is pure core. `barwise merge \u003ca\u003e \u003cb\u003e` is cheap to add and testable offline, which makes it the easier half of the pair to settle.\n\nOpen question worth deciding first: is merge a modelling operation users want at the command line, or is it there for agent use through MCP specifically? If the latter, say so in the docs rather than leaving it looking like an oversight.\n\nClosed by docs/specs/cli-surface-parity.spec.md. `barwise merge \u003cbase\u003e \u003cincoming\u003e` ships with --output and --format yaml|json.\n\nThe open question -- is merge a modelling operation users want at the command line, or an agent affordance belonging to MCP alone -- resolved to the former. A merge only an agent can perform is a merge no human can review before it happens, and the CLI is where a human works a model into a state they are willing to commit. It is also the sibling of `barwise diff`, which already ships: the same comparison read in the other direction.\n\nTwo deliberate differences from the MCP tool, both recorded in the spec. The CLI does not write back to the base file -- defensible for an agent handed both paths, surprising for a command a human typed -- so the merged model goes to stdout and --output is how you ask for a file. And a merge producing structural errors writes nothing anywhere and exits 1, since a broken model on disk is worse than no model. Both have their own test.\n\nVS Code still lacks merge; the capability matrix in CLAUDE.md now records that as deliberate, because merge in an editor wants a diff view and per-delta accept/reject, which is a UI feature rather than a wiring job.","status":"closed","priority":3,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-08-20T19:30:00Z","created_by":"claude","updated_at":"2026-08-21T03:20:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-811","title":"CodeExtractionPrompt builds a prompt nothing sends to a model","description":"buildCodeExtractionPrompt is exported at packages/code-analysis/src/index.ts:31 and defined in prompt/CodeExtractionPrompt.ts:21. Its only other mention in any src/ tree is that export line. One test covers it. Nothing hands its output to a model.\n\ndocs/specs/prompt-optimization-harness.spec.md already scoped it out on exactly this ground: \"dead code today (built, exported at src/index.ts:31, never sent to a model). It joins the harness when it gains a call site.\" Two months on it has not gained one.\n\nThe decision is not really about the code, it is about the design: was the code-analysis import path ever meant to run an LLM pass over assembled code context, or was that abandoned and this left behind? Answer that and the disposition follows -- give it a call site, or delete it and drop the export.\n\nCarrying it is not free: it costs a test, it appears in the public export list where a reader will reasonably assume it is used, and it makes the next audit re-derive the same finding. Confirmed in docs/unwired-capability-audit-2026-08-20.md.","status":"open","priority":3,"issue_type":"chore","owner":"noreply@anthropic.com","created_at":"2026-08-20T19:30:00Z","created_by":"claude","updated_at":"2026-08-20T19:30:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-812","title":"Few-shot demo rendering is built, tested, and never exercised","description":"PromptDemo, the loader validation in loadArtifact.ts:69-83, and renderDemos in prompt/artifacts/render.ts are implemented and tested. Both shipped artifacts declare demos: [], so no demo has ever rendered outside a unit test.\n\nNothing is broken and no action is needed yet. Filed so the gap is visible rather than discovered later: the DSPy optimizer lane is specified to export demos, and it will be the first thing to exercise this path in earnest.\n\nWhat is unverified is behaviour under real content rather than fixture content -- specifically token budget. The extraction system prompt is already about 4,540 tokens; the harness spec flags demos carrying full JSON payloads as a grounding question for the exporter, possibly needing excerpt-truncation rules. That question stays open until something renders a real demo.\n\nFrom docs/unwired-capability-audit-2026-08-20.md, which recommends leaving this alone until the optimizer lane needs it.","status":"open","priority":4,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-20T19:30:00Z","created_by":"claude","updated_at":"2026-08-29T02:37:38Z","dependency_count":0,"dependent_count":0,"comment_count":0,"notes":"First real exercise of few-shot demos on real content (2026-08-29 sonnet bootstrap compile), and they HURT: against the same seed program, adding 2 bootstrapped demos moved misplaced_is_preferred from 5 to 60 occurrences, introduced population/uniqueness-violation (10) and population/value-constraint-violation (15) errors the zero-shot program did not produce, and floored 5 of 15 evaluations. The demos are collected from recorded traces, so their defects propagate as instructions -- a demo carrying a misplaced is_preferred teaches it. Any future demo rendering needs a conformance gate on the demo itself (a trace that would fail conformance must not become a demo), which is a design question this issue should carry."}
{"_type":"issue","id":"barwise-813","title":"Workstream 3 (promote rules into the default artifact) is deferred pending a sized measurement round","description":"Artifact resolution workstreams 1 and 2 shipped (PR #310). Workstream 3 -- promoting the model-agnostic rules from the variants into defaultExtractionArtifact -- is deliberately not done. It is gated on a measurement round the repo cannot run without an API key, and the analysis below changed what that round has to look like.\n\nRe-grounding against the actual prompt files found the spec's workstream 3 table does not describe what is in the variants. Corrections are recorded in docs/specs/artifact-resolution-in-production.spec.md under Implementation notes; the load-bearing ones:\n\n1. Five of the six named rules exist only in haiku45-2. Only frequency siding is in both variants. Promoting them is not consolidating agreed rules; it is applying Haiku-derived text to every tier, including ones with no variant at all.\n\n2. The spec omits five bullets that ARE byte-identical in both variants (the populations rewrite, complete-instances, enum-is-a-value-constraint, conflict-flags-ambiguity, and the \"populations are optional\" critical rule). By the spec's own logic those are the strongest promotion candidates.\n\n3. The Open decision \"sonnet5 retains objectification and ring guidance\" is wrong. sonnet5-3 differs from the default in exactly two lines, neither about objectification or rings. Promoting the union would leave both variants empty -- the real question is whether they get retired, not whether they stay thin.\n\nDecision held by the user pending a run with more rounds. See barwise-814 for the harness work that makes such a run interpretable.","status":"open","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-21T00:45:00Z","created_by":"claude","updated_at":"2026-08-22T20:40:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"notes":"Re-grounded 2026-08-22 now that barwise-814 has shipped. Three findings change what the round should be, and one changes whether the comparative round is needed at all. Recorded in docs/specs/artifact-resolution-in-production.spec.md under 'Workstream 3, revisited'.\n\n1. The motivating gap is not resolvable and has shrunk. The spec cites 0.760 vs 0.948 (gap 0.188). Two recorded rounds of the same comparison: n=3 gave 0.725 vs 0.828 (gap 0.103); n=5 gave 0.783 vs 0.855 (gap 0.072) against a 95% threshold of 0.090 -- z=1.56, p=0.12. The variant leads in both rounds and in neither by a resolvable margin. Resolving 0.03 needs n=45/config (~644 calls); 0.02 needs n=102 (~1442).\n\n2. Three of the nine promotable lines target failures deterministic code now prevents: frequency min\u003e=1 (conformance 5b, barwise-830) now costs 0.02 not 0.1; populations-satisfy-mandatory (sample:true, barwise-827) is moot for the mandatory half; enumerated-list-is-a-value-constraint is HALF absorbed -- conformance check 3 removes a population duplicating a value constraint already emitted, but nothing converts a population into one when none was emitted, so the prompt keeps that half.\n\n3. A comparative test of means is the wrong instrument. Scoring the seven recorded answer keys is free and deterministic: all seven come back full-rubric with ZERO validation errors and ZERO validation warnings, 1-3 conformance corrections each being the entire penalty. So every warning a live run incurs is addressable, warningsByRule names the rule, and each candidate maps to a named rule (completeness/fact-type-without-uniqueness for the uniqueness bullet, structural/binary-missing-inverse-reading for the both-readings bullet, both warnings at 0.05). One diagnostic run of the DEFAULT over both splits -- 10 cases x 5 = 50 calls -- prices each candidate at 0.05 x its count and says which bullet to promote. The 644-call comparative round returns one blended number that cannot tell them apart.\n\nRecommended order: barwise-835 (make the per-instance role-completeness check deterministic; no API calls to test), then the 50-call diagnostic round against the default, then promote only the lines whose rule actually fires. Still gated on the user authorizing the paid run."}
{"_type":"issue","id":"barwise-814","title":"promptlab: report per-case SD and the suite standard error so an eval run says whether n was enough","description":"runSuite records every sample but reports only means, so a run cannot tell you whether a difference it shows is real. Working the recorded runs by hand shows why that matters.\n\nAnalytic SE of the suite mean reproduces the run-to-run swing we observed empirically:\n  haiku45-2, n=5:  SE 0.0318  -\u003e two configs must differ by \u003e0.088 to be distinguishable at 95%\n  sonnet5-2, n=3:  SE 0.0398  -\u003e \u003e0.110\n\nThose thresholds are larger than every gap we have used to rank configurations, which is why n=3 could not rank them.\n\nThe sharper finding: the noise is one sample, not a spread. A single collapsed sample out of 35 carries 77% of all variance in the Haiku run (project-staffing, 0.000 among 0.98s); a single one of 21 carries 99% in the Sonnet run (university-enrollment, 0.167 among 1.000s). The suite mean is mostly an estimator of whether a collapse happened, not of prompt quality. Splitting collapse rate from quality-given-no-collapse tightens the Sonnet SE 13x, and drops the n needed to resolve a 0.02 regression from ~190 to single digits.\n\nProposed, smallest first:\n- Per-case sample SD and a suite-level standard error on SuiteReport, carried into the history entry. Pure arithmetic over samples already collected; no new calls.\n- Print the resolution alongside the mean, e.g. \"mean 0.916 +/- 0.062 (95%, n=5)\", so a reader cannot quote a mean without its error bar.\n- A decision rule in the report: a difference below 2x SE(difference) is inconclusive, stated as such rather than left to the reader.\n- Separately (needs a spec, involves a judgment call about the threshold): report collapse count per case, so the bimodal failure is counted rather than averaged away.\n\nWanted before the next sized run, since it is what makes that run self-diagnosing.\n\nClosed by PR #313. runSuite folds per-case sample SD and the suite standard error through src/stats/dispersion.ts; the CLI prints the 95% margin beside every mean including in `prompt history`, where old rows show '+/- ?'. Below two samples the SD is undefined rather than 0. The collapse-rate split stays deferred -- it needs a threshold, which is a judgment call wanting its own spec.","status":"closed","priority":1,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-21T00:45:00Z","created_by":"claude","updated_at":"2026-08-21T03:00:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-815","title":"Observability for the LLM surfaces: nothing that costs money is currently visible after the fact","description":"Placeholder for a spec, not a design. Filed while working barwise-814, because the same gap kept showing up: the system collects the signals and then drops them.\n\nWhat is already measured and then thrown away:\n- CompletionResponse carries usage.promptTokens, usage.completionTokens, and latencyMs. Every provider populates them. processTranscript passes them into DraftModelResult. No surface persists them -- not the CLI, not MCP, not VS Code. So there is no answer to \"what did that extraction cost\" or \"how slow is Haiku on a 16KB transcript\" without instrumenting a run by hand.\n- runSuite records `attempts` per run and the retry classification (FailureKind), which is the only place a rate-limit storm is visible, and it survives only until the process exits.\n- The score history is the one durable record, and it holds means and nothing operational.\n\nQuestions a spec should answer:\n- What is worth persisting, and where? The score history is per-eval-run and checked into git; operational telemetry is per-call and should probably not be.\n- Does this belong in a package, or is it a CLI/MCP concern? Determinism-in-core says the collection point is the outer packages, since it is all clocks, counters, and I/O.\n- Is there a single envelope that serves eval runs, production extractions, and the optimizer lane, or are those three different records that should not be forced together?\n- Cost needs per-model pricing, which is external data that goes stale. Record tokens and derive cost at read time rather than baking a number into a row.\n\nRelates to task #2 (model-tier economics), which cannot be answered without the token and latency numbers this would keep.\n\nSpec written as docs/specs/llm-call-observability.spec.md; workstream 1 (the record type and the withCallLog decorator) is implemented. Left open: the CLI sink (W2) and the usage report (W3), both gated on open decisions a reviewer should settle first -- chiefly whether recording is opt-in.\n\nAnswers to the questions this issue posed:\n\n- What to persist, and where: one record per completion -- model, tokens in and out, latency, outcome, error class -- as JSONL under $XDG_STATE_HOME/barwise/, matching where the gym already writes. Not in the repo: this is operational data with a different lifetime than promptlab's committed score history.\n- Package or surface concern: the recording seam is the LlmClient itself, via a decorator in @barwise/llm. The persistence is the caller's, following the same seam the no-clocks rule and the build provenance already use. processTranscript, reviewModel, and runSuite are all untouched, which is the test of whether the seam is right.\n- One envelope or three: one. Eval runs, extractions, and the optimizer lane differ in how calls are grouped, not in what is measured. An optional correlation id is the whole difference.\n- Cost: record tokens, never a price. The repo ships no rate table, since a stale price produces a confidently wrong number; a usage report takes --rates from the user.\n\nOne constraint the issue did not raise and the spec makes explicit: records carry sizes and identities, never prompt or response text, and errorKind is a classification rather than the provider's message. Transcripts are client material, and a telemetry log quietly accumulating them would be that mistake written to disk. Two tests assert it.","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-21T01:30:00Z","created_by":"claude","updated_at":"2026-08-22T22:05:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"notes":"2026-08-22: workstream 3 SHIPPED -- `barwise llm-usage` reads the log and reports per model: calls, failures by kind, token totals, median and p95 latency. All three workstreams now done; task #2 (model-tier economics) is answerable rather than merely unblocked.\n\nNo prices ship with the repo, per the open decision: --rates \u003cfile\u003e takes a user-maintained JSON of per-million rates. A checked-in table with a 'last verified' date is friendlier for one release and misleading for every release after.\n\nPercentiles rather than a mean, because a mean hides the tail and the tail is what a timeout is set against; undefined rather than 0 for an empty sample, the same distinction the dispersion module makes. The reader tolerates the file it actually gets: one log holds three record kinds and an append-only file can end mid-line, so non-call records are skipped on a field only a call carries and an unparseable line is skipped rather than fatal.\n\nStill deferred: promptHash on the record (hashPrompt lives in promptlab, which depends on llm and not the reverse; the honest fix is moving the hasher into llm rather than duplicating it).","closed_at":"2026-08-22T22:05:00Z"}
{"_type":"issue","id":"barwise-816","title":"Consider folding Ousterhout's design vocabulary into the architecture guidance","description":"A Philosophy of Software Design covers ground our design principles gesture at without naming, and the vocabulary is worth having because it makes a review comment land in one word instead of a paragraph.\n\nWhere it would attach. CLAUDE.md states orthogonality and composability as primary, DRY as secondary, and explicit-over-implicit throughout. Ousterhout's frame is compatible but sharper in places we actually argue:\n\n- Deep vs shallow modules. Our composability principle says \"narrow primitives that compose over wide ones that don't\", which is the same claim, but \"deep module\" names the target directly: a simple interface over substantial functionality. The FormatDescriptor registry and resolveArtifact are deep; a wrapper that renames three parameters is shallow. Useful for judging whether a proposed seam earns its place.\n- Complexity is what the *reader* pays. Change amplification, cognitive load, unknown unknowns. The third is exactly the failure the artifact-resolution spec kept hitting: nothing told a reader that the variant files had converged, or that the eval harness could not resolve the gaps it was being used to rank.\n- Define errors out of existence. Related to explicit-over-implicit but pointed the other way -- rather than requiring the caller to declare, remove the condition. Worth stating alongside, since taken alone \"explicit over implicit\" can argue for pushing work onto callers, and that is how a shallow interface gets justified.\n- Comments describe things the code cannot. Our comment convention is house style by imitation; the \"what the code cannot say\" test is a real criterion.\n- Strategic vs tactical programming, and design-it-twice. Design-it-twice is close to what the spec-writer skill's sensemaking step already asks for (hold two or three alternatives); naming the source would tie them together.\n\nWhere it would live: docs/ARCHITECTURE.md, or a short section in CLAUDE.md's Design Principles that cites the terms without restating the book. Prefer the smaller edit -- the value is a shared vocabulary, not a summary. A reader who wants the argument should be pointed at the book.\n\nWorth deciding explicitly whether this becomes normative (a reviewer may reject a change for being a shallow module) or descriptive (shared words, no new gate). Recommend descriptive first.\n\nClosed. Landed as a \"Shared vocabulary (Ousterhout)\" subsection under Design Principles in the root CLAUDE.md, kept descriptive per the recommendation: the terms give a reviewer shorter words, they do not add a gate.\n\nFive terms, each anchored to something already in this codebase rather than restated from the book: deep vs shallow modules (resolveArtifact and the FormatDescriptor registry as the deep cases), the three forms of complexity (with unknown-unknowns named as the one the last two specs kept hitting), define-errors-out-of-existence (explicitly as a counterweight to explicit-over-implicit, which taken alone argues for pushing work onto callers), comments-describe-what-code-cannot, and strategic-over-tactical with design-it-twice tied to the spec-writer skill's sensemaking step.\n\nPlaced in CLAUDE.md rather than ARCHITECTURE.md section 8, which holds decisions about this system rather than general design vocabulary, and because CLAUDE.md is what an agent actually reads before working.","status":"closed","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-21T02:05:00Z","created_by":"claude","updated_at":"2026-08-21T03:40:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-817","title":"splitModel's auction test is a latent CI flake: 480ms alone, 5714ms under coverage","description":"tests/project/splitModel.test.ts \"splits the auction model into a valid four-domain project\" ran 5714ms during a full `npm run test:coverage` and failed on vitest's 5000ms default timeout. The same test standalone takes 480ms. Coverage instrumentation plus twelve packages running in parallel is the difference.\n\nNot reproduced in CI -- the run on PR #313 was green -- which is exactly what makes it worth filing rather than waiting for. It surfaced on a loaded container, so it is a question of runner speed, and a slower or busier runner will hit it.\n\nThis is the second instance of the class. The first was loadSuite's seed-suite test in promptlab, fixed by giving that one test an explicit `}, 30_000)`. Same fix applies here.\n\nWorth deciding whether to keep patching per test or raise testTimeout once in the shared vitest config. Per-test is more honest about which tests are genuinely slow; a global default hides that a test got slower. Recommend per-test, consistent with the loadSuite precedent.\n\nTrap for whoever picks this up: `npm run test` and `npm run test:coverage` are different Turborepo tasks, and only the second is what CI runs. A local `npm run test` will not reproduce this.\n\nClosed. The recommendation recorded above -- patch per test, following the loadSuite precedent -- was wrong, and checking the repo rather than generalizing from one remembered instance is what corrected it.\n\nFive packages already set `testTimeout: 30_000` globally in vitest.config.ts (cli, diagram, diagram-ui, llm, mcp), with comments giving exactly this reason: \"the 5s default is too tight on a loaded CI runner\", \"under coverage instrumentation\". The per-test patch in promptlab was the anomaly, not the convention.\n\nThe cohort is also larger than one test. Under coverage with twelve packages in parallel, core alone has five splitModel tests between 2.8s and 11s, and diagram, llm, and cli each have tests over 5s that pass only because those packages already carry the global. Patching per test would have meant re-deriving this finding on every loaded runner.\n\nThe argument that per-test is \"more honest about which tests are slow\" also does not survive contact: a timeout is a failure threshold, and it fires on machine load rather than on a test getting slower. Vitest already reports per-test durations, which is how the cohort was found. Using the timeout as a slowness alarm is a misuse of it.\n\nFixed by giving the seven packages that lacked it the same `testTimeout: 30_000` the other five had, so all twelve now agree, and removing promptlab's now-redundant per-test override. Verified load-bearing: lowering core's to 300ms fails five tests, restoring it passes all twelve packages under test:coverage.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-21T02:10:00Z","created_by":"claude","updated_at":"2026-08-21T03:30:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-818","title":"Truncated LLM responses were scored as prompt failures, not excluded","description":"The first run against the dev split produced three consecutive near-zero scores on vendor-onboarding (0.000, 0.000, 0.133) that were not bad extractions at all. They were complete extractions cut off at the provider's 8,192-token output default, and nothing in the response said so.\n\nThree dev transcripts are 13-17 KB against the seed suite's 1.0-1.6 KB. At the densest observed payload/transcript ratio (9.69, from the seven recorded fixtures) they need 31,700 to 41,600 output tokens. Every one was two to five times over budget.\n\nThe failure was invisible because a truncated tool_use block still parses: it arrives as well-formed JSON holding whatever fields completed, so it raised no error and nothing marked it unscorable. It simply scored near zero, which reads exactly like a bad prompt. Both SDKs report this (Anthropic stop_reason=max_tokens, OpenAI finish_reason=length) and the code discarded it.\n\nSame category error as barwise-806, which stopped scoring failed calls as zeroes, and worse for being silent: a failed call at least reports an error.\n\nFixed per docs/specs/output-budget.spec.md. CompletionRequest.maxTokens overrides the client default per call; CompletionResponse carries stopReason and truncated; suggestMaxTokens derives a budget from transcript length, floored at the client default so every seed case and every recorded history row stays comparable; runSuite excludes truncated runs as failureKind 'truncated' and reports truncations apart from failures; barwise prompt eval gains --max-tokens. processTranscript derives the same budget and surfaces truncation as its first warning rather than shipping half a model silently.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-22T16:00:00Z","created_by":"claude","updated_at":"2026-08-22T16:40:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-819","title":"MAX_OUTPUT_TOKEN_CAP of 64,000 exceeds some models' own output limits","description":"suggestMaxTokens caps a derived budget at 64,000, sized so the largest transcript in the eval suite (vendor-onboarding, ~41,600 derived tokens) fits. A cap below that would leave the very cases that motivated barwise-818 silently truncated.\n\nThe consequence, accepted deliberately and recorded here so it is not rediscovered: a model whose own output ceiling is lower rejects the request outright. gpt-4o allows 16,384. That is a 400 carrying a status, an error type, and a request id, all three now recorded by runSuite, and the remedy is --max-tokens (or ProcessorOptions.maxTokens). Being told a budget is impossible beats being handed a fragment that scores like a bad prompt.\n\nNot fixed with a per-model table on purpose: the SDKs do not expose each model's output limit, and a hand-maintained table would go stale without anything noticing. Revisit only if a real user hits this on a default path, in which case the fix is probably for the provider to clamp and report the clamp, never to clamp silently.","status":"open","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-22T16:40:00Z","created_by":"claude","updated_at":"2026-08-22T16:40:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-820","title":"Consider a transcript cleanup pass before ORM extraction","description":"Idea from the user (2026-08-22): a lightweight pass over a raw transcript before it reaches the extraction prompt -- strip timestamps, speaker noise, filler, verbatim repetition -- so the model sees signal rather than transport.\n\nWorth noting up front that this sounds easier than it is, which is the user's own framing. Two hazards specific to barwise:\n\n1. Provenance. Every extracted element carries a SourceReference with line numbers and excerpts into the original transcript. A cleanup pass that renumbers lines breaks that unless it emits a line map back to the original, which is most of the work.\n2. Determinism. If cleanup is deterministic it belongs in core and is testable; if it is an LLM pass it doubles the calls and adds a second source of non-determinism upstream of the metric, which would make eval scores harder to attribute, not easier.\n\nThe measurable upside is real and now quantifiable: the dev transcripts are 13-17 KB against 1.0-1.6 KB for the seed cases, and input size drives both cost and the output budget (barwise-818). A deterministic pass that halved transcript size would halve both.\n\nNeeds a spec before any code. The eval harness is the right place to settle it: score the same cases with and without cleanup and see whether it moves the mean beyond the suite's resolvable difference. If it does not, it is cost reduction only, which is still a result worth having but a different justification.","status":"open","priority":3,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-08-22T16:45:00Z","created_by":"claude","updated_at":"2026-08-22T16:45:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-821","title":"Anthropic SDK refuses non-streaming calls above 21,333 max_tokens","description":"Fallout from barwise-818, found on the first dev-split run after it landed. All three cases came back FAILED, excluded with: 'Streaming is strongly recommended for operations that may token longer than 10 minutes.'\n\n@anthropic-ai/sdk 0.52.0 calculateNonstreamingTimeout throws when (3_600_000 * max_tokens) / 128_000 exceeds the 600_000ms default, i.e. for any max_tokens above 21,333. It throws before the request is sent, and no timeout argument escapes it in this version. The derived budget for a 17 KB transcript is roughly 41,600, so every dev case tripped it.\n\nThe retry classifier handled it correctly, for what it is worth: terminal, 1 attempt, no wasted backoff.\n\nFixed by streaming unconditionally in AnthropicLlmClient and taking finalMessage(). Not for incremental display -- nothing renders these tokens -- but so no call can reach the guard at all. Streaming only above a threshold was rejected: the threshold is a constant inside someone else's package and can move without warning, which is the failure mode this whole line of work keeps hitting. finalMessage() reassembles input_json_deltas into a complete tool_use block, so the rest of the provider sees exactly what the non-streaming call returned, partial input included.\n\nNote the mock cannot prove the SDK's real streaming behaviour; the live dev-split run is the proof.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T16:38:00Z","updated_at":"2026-08-22T16:45:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-822","title":"Cache the extraction prompt prefix instead of resending 5,780 tokens per call","description":"Measured while sizing the output budget: every extraction call sends a byte-identical 5,780-token preamble before a line of transcript -- 4,540 for the system prompt, 1,180 for the response schema (which reaches the wire as the extraction tool's input_schema), 58 scaffold. For the seven seed cases the transcript is 6% of what we send; the rest is our own prompt. A repeat=5 sweep over ten cases sends that preamble fifty times: 289,000 input tokens of pure repetition.\n\nFixed per docs/specs/prompt-caching.spec.md. CompletionRequest gains cacheSystemPrompt and cacheUserMessage; the Anthropic provider marks the corresponding blocks with cache_control. Two flags rather than one because their break-even conditions differ: a write costs ~1.25x and a read ~0.1x, so the preamble is worth caching whenever a run makes two or more calls, while a transcript is only worth caching when the same case is sampled again (runSuite iterates case-outer, repeat-inner, so repeats send identical transcripts back to back).\n\nNote the layout was already correct by accident: the API renders tools then system then messages, and the extraction call happens to put its stable content first. Nothing said so, so a later change moving the transcript earlier or interpolating a date into the system prompt would have cost the whole saving with no test failing. The ordering is now a stated invariant in the package CLAUDE.md.","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T16:46:00Z","updated_at":"2026-08-22T16:55:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-823","title":"Report cache hits in the eval output, so caching is confirmed rather than assumed","description":"barwise-822 turns caching on for eval sweeps but nothing reports whether it worked. The failure mode is silent by construction: below a model's minimum cacheable length, or after any byte change in the prefix, cache_creation_input_tokens is simply 0 and the request succeeds normally. The only symptom is a bill.\n\nCompletionResponse.usage carries promptTokens and completionTokens but has no field for cache_creation_input_tokens or cache_read_input_tokens. Adding them means touching the usage shape and every provider that fills it, which is why it was cut from the caching change rather than bundled in.\n\nWorth noting that input_tokens is the UNCACHED REMAINDER once caching is on -- total prompt size is input_tokens + cache_creation + cache_read. Any future cost reporting that reads promptTokens alone will under-report by the cached portion, which is most of it.\n\nRESOLVED per docs/specs/cache-reporting.spec.md. CompletionResponse.usage gains cacheReadTokens and cacheWriteTokens, populated by Anthropic (both counters) and OpenAI (reads only -- its caching is automatic and server-side, so there is no client-controlled write). SuiteReport.cache totals them, the verbose line shows reads live, and the CLI warns when caching was requested, more than one call was made, and nothing was read back.\n\nAbsent and zero are kept distinct throughout: a provider with no cache says nothing and gets no row; a provider that cached nothing says zero and gets one, because zero across a repeated prefix is the fault worth naming.\n\nHistoryEntry.tokens carries prompt/completion/cacheRead/cacheWrite. Scoped deliberately: unlike every other field in a row these say nothing about comparability (caching is score-neutral), but cost is a longitudinal question and four numbers let a later reader reconstruct it against whatever price table exists then. Stop reasons, request ids and HTTP statuses stay off the record -- they diagnose a run that is happening, and are useless three weeks later.","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T16:55:00Z","updated_at":"2026-08-22T18:15:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-824","title":"Investigate prompt caching as a default for interactive barwise usage","description":"From the user (2026-08-22): whenever the llm package is used, defaulting to cached prompts could save real money on interactive sessions.\n\nbarwise-822 deliberately scoped caching to the eval lane, on the reasoning that a one-shot processTranscript reads the cache zero times and pays the 1.25x write for nothing. That reasoning does not survive contact with interactive use, which is the point being made here: a VS Code chat session, an MCP client working through a model, or a user importing several transcripts in a sitting all make repeated calls inside the 5-minute TTL, and every one of them re-sends the same 5,780-token preamble at full price.\n\nWhat needs establishing before changing a default:\n\n1. Call frequency per surface. The break-even is two calls within the TTL. VS Code chat almost certainly clears it; a single CLI import almost certainly does not; MCP is unknown and depends on the client.\n2. Whether the preamble is actually identical across those calls. review and extraction are different surfaces with different prompts, so they are different cache entries; a session alternating between them may thrash rather than accumulate.\n3. Whether the default should be per-surface rather than global. Extraction repeats within a session more plausibly than review does.\n\nDepends on barwise-823: without cache-hit reporting there is no way to confirm a default is working rather than quietly costing 25% more on every first call. Do that first.","status":"open","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T16:55:00Z","updated_at":"2026-08-22T16:55:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-825","title":"Ollama provider: use native /api/chat so the context window is settable","description":"The Ollama provider talked to the OpenAI-compatible /v1 endpoint, which cannot set num_ctx. Ollama's own docs say so: 'The OpenAI API does not have a way of setting the context size for a model', with a Modelfile or a server-wide OLLAMA_CONTEXT_LENGTH as the documented workarounds -- neither reachable from a library.\n\nThat is disqualifying rather than inconvenient. Ollama defaults to a 4,096-token context and silently drops what does not fit; the extraction system prompt alone is ~4,540 tokens (barwise-822 measured it). On the default, the INSTRUCTIONS are truncated before the transcript is read, and the model is then scored on a prompt it never saw. Same class of silent corruption as barwise-818, entering through a different door.\n\nRewritten against native /api/chat over fetch. Gains options.num_ctx and options.num_predict, and native `format` which takes a JSON schema directly rather than the response_format/json_schema wrapper. Adds no dependency -- fetch is Node core -- so this provider now uses no SDK at all. suggestContextWindow derives a window per call from prompt length plus output budget; OllamaClientOptions.contextWindow overrides, reachable as `barwise prompt eval --context-window`.\n\nCorrection worth recording, because it was stated wrongly mid-investigation: the compat layer DOES report truncation. It passes done_reason through to finish_reason unchanged, and Ollama's vocabulary matches OpenAI's ('length' on num_predict or context exhaustion). The earlier claim that Ollama truncation was undetectable was wrong; describeOpenAiStop already handled it. num_ctx alone justifies the move.\n\nStreams, like the Anthropic provider and more acutely: a local model generating tens of thousands of tokens runs for many minutes, and a non-streaming request sends no headers until it finishes -- past Node's 300s header timeout. The NDJSON reader buffers partial lines.","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T16:56:00Z","updated_at":"2026-08-22T17:05:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-826","title":"Conformance passed single-role disjunctive constraints the validator rejects","description":"Found by the first dev-split run that completed without truncation. No case was truncated -- every one finished well inside its budget (12,604/41,640; 14,177/35,255; 10,509/31,700) -- so the remaining failures were ours.\n\nisValidArity in ExtractionConformance fell through to a permissive '\u003e= 1' for disjunctive_mandatory, exclusion and exclusive_or, while core's constraintConsistency rule rejects exactly those three below 2 roles. Conformance waved them through and the validator errored on every one, at 0.1 penalty each.\n\nincident-response scored 0.000 on seven such errors; subscription-billing lost 0.1 to one. Neither module was wrong on its own terms, which is why nothing caught it: they are separate by design but not independent, since conformance exists to hand the parser something the validator will accept.\n\nFixed by listing the three types explicitly at '\u003e= 2', matching the validator. Removal rather than repair: a single-role disjunction is a contradiction in terms, and converting it to a plain mandatory constraint would assert a rule the extraction never stated -- which is precisely what produced vendor-onboarding's separate failure (barwise-827). Removal also charges 0.02 as a conformance correction instead of 0.1 as a validation error, which is the right relative price.\n\nThe regression test asserts the agreement end to end rather than the removal alone: a module-local test would re-pin one side of a disagreement and a future relaxation of either module would pass it while re-creating the bug. Verified load-bearing -- reverting the fix fails 7 of 10.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T17:12:00Z","updated_at":"2026-08-22T17:20:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-827","title":"Extraction asserts mandatory constraints its own populations violate","description":"vendor-onboarding scored 0.023 on the dev split with: 'Mandatory constraint on role ... in fact type \"Vendor has VendorStatus\" is violated: \"Meridian Components\" appears in the model but does not play this mandatory role.'\n\nThe same extraction supplied both halves: an inferred mandatory constraint, and a population instance that does not satisfy it. Distinct from barwise-826 -- that was two modules disagreeing; this is one extraction contradicting itself.\n\nenforceConformance can see both halves, so it is the natural place to reconcile them. Three options, none obviously right, which is why this is filed rather than fixed:\n\n1. Drop the mandatory constraint. Safe, loses a rule the transcript may genuinely support.\n2. Drop the offending population instance. Keeps the rule, discards evidence -- and populations are what make a model checkable.\n3. Leave both and let it be a validation error. Status quo, and arguably honest: the model really is inconsistent and a human should look.\n\nWorth settling with data rather than argument: check how often the pattern occurs across the recorded payloads before choosing. If it is common, option 1 or 2 is worth the loss; if rare, the status quo is a correct signal, not a defect.\n\nNote the prompt angle too: a mandatory constraint over a role no instance plays may indicate the extraction inferred mandatory from a sample rather than from a stated rule.\n\nRESOLVED per docs/specs/sample-populations.spec.md, and none of the three options above was right. The defect was not that the extraction contradicts itself -- it was that nothing in the model said its populations are samples, so the validator assumed the stricter reading. Population gains a `sample` flag (default false, so every existing model is unchanged); sample instances are excluded from the object universe and still counted in valuesPlayedInRole. A sample is positive evidence only: it can satisfy a constraint, never raise the obligation that one be satisfied.\n\nThe data settled it: all seven seed answer keys produce zero population-rule errors, so the pattern does not exist in the train split. It is a scaling artifact of realistic-length transcripts, and it reached the product too -- anyone importing a long transcript in the editor saw the same violations.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T17:20:00Z","updated_at":"2026-08-22T17:45:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-828","title":"Investigate data profiling to derive domain understanding","description":"Idea from the user (2026-08-22): profile actual data to derive domain understanding, rather than relying only on transcripts.\n\nFits barwise's existing shape better than it might first appear. The metamodel already has the target concept -- Population -- and as of barwise-827 it distinguishes a significant population (complete, checkable as a closed world) from a sample. Profiled data is the first source that could legitimately produce SIGNIFICANT populations: unlike a transcript, a table really does contain every row.\n\nThat matters more than it sounds. Constraint discovery from data is exactly the closed-world inference the population rules already implement, run backwards: a column with no nulls suggests a mandatory role; a column with unique values suggests a uniqueness constraint; a bounded value set suggests a value constraint; a foreign key suggests a fact type. The validation engine can then check the inferred constraints against the same data that suggested them.\n\nOpen questions before any spec:\n\n1. Where does it live? Profiling reads data sources, which is I/O -- so a connector package on the @barwise/code-analysis or @barwise/dbt model, not core. The inference itself is deterministic and could be core.\n2. What sources? A dbt project already declares models and tests; the dbt connector may already reach most of what is needed without a database connection.\n3. How does it combine with transcript extraction? A transcript gives intent and naming; data gives structure and actual distributions. Merging the two is where the value is, and diffModels already exists to compare them -- a data-derived model diffed against a transcript-derived one would surface exactly the disagreements worth a human's attention.\n\nWorth noting the sequencing: it is more valuable AFTER the extraction quality work settles, because it gives a second, independent signal to check extraction against.","status":"open","priority":3,"issue_type":"feature","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T17:45:00Z","updated_at":"2026-08-22T17:45:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-829","title":"Output budget floor of 8,192 truncated a small train case; output does not track input size","description":"The first clean train sweep truncated university-enrollment at exactly 8,192 output tokens. Its transcript is 1,486 bytes -- the THIRD largest of the seven train cases, not the largest -- and its recorded answer key is only ~3,500 tokens. Live extraction generated more than twice that.\n\nMeasured across the whole suite, live output vs transcript size:\n\n  1.0-1.6 KB transcripts -\u003e 3.7k to 8.2k+ output (implied ratio 12 to 22+)\n  13.1-17.2 KB transcripts -\u003e 12.2k to 14.4k output (implied ratio 3 to 4)\n\nOutput barely moves across a seventeen-fold range of input. Two nearly identical train transcripts (1,589 B and 1,529 B) produced 7,220 and 4,735 tokens. Output is governed by how much MODEL the domain implies, not by how many bytes it took to describe -- university enrollment is a rich domain expressed briefly.\n\nSo the proportional-to-input model from barwise-818 is the wrong SHAPE, not merely mis-tuned, and it is wrongest at the small end where the required ratio is highest -- precisely where it was assumed safest.\n\nFixed by splitting MIN_DERIVED_OUTPUT_TOKENS (16,384) from DEFAULT_MAX_OUTPUT_TOKENS (8,192, still the provider default for a call that names no budget). The floor sits 14% above the largest output observed anywhere (14,393, subscription-billing).\n\nNote the old floor's justification was 'a case that fit before must behave exactly as it did', to preserve comparability with recorded history. That was empty: main had no recorded history at all (the rows live on optimization-evals-3 and predate barwise-826/827). The comparability argument was protecting nothing while the truncation was real.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T18:25:00Z","updated_at":"2026-08-22T18:35:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-830","title":"Conformance did not check frequency bounds the validator rejects","description":"Third instance of the barwise-826 pattern, found on the first clean train sweep: clinic-appointments lost 0.1 to 'Frequency constraint in fact type \"Appointment has FollowUpNote\" has min 0, which must be at least 1.'\n\nInferredConstraint carries min and max for frequency constraints. constraintConsistency rejects min \u003c 1 and a max below its min. enforceConformance checked ARITY for frequency constraints but never the bounds, so a min-0 frequency sailed through and became an unavoidable validation error at 0.1.\n\nbarwise-826 fixed this class for arity and its own test comment stated the general rule -- 'any rule the validator enforces on arity has to be enforced here too' -- but the fix was scoped to arity when the principle is broader. Worth remembering: the fix landed with a comment that described the general case and code that handled one instance of it.\n\nFixed with an invalid_bounds correction, removing rather than repairing: a frequency of 'at least 0' is not a weak constraint but no constraint at all -- every population satisfies it -- so nothing a reader could have relied on is lost. Same end-to-end invariant test as barwise-826: whatever survives conformance must not trip the validator.\n\nFollow-up worth doing deliberately rather than reactively: audit constraintConsistency for any REMAINING structural rule conformance does not mirror, instead of waiting for the next eval run to find the fourth instance.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T18:28:00Z","updated_at":"2026-08-22T18:35:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-831","title":"Audit constraintConsistency for structural rules conformance does not mirror","description":"Three times now a validator rule with no conformance counterpart has surfaced as an unavoidable eval penalty, each found by a live run rather than a test: barwise-826 (constraint arity), barwise-830 (frequency bounds), and the population closed-world assumption in barwise-827.\n\nThe pattern is structural, not coincidental. enforceConformance exists to hand the parser something the validator will accept, so every purely STRUCTURAL rule in core/src/validation/rules/constraintConsistency.ts needs a mirror in conformance or it becomes an error the extraction cannot avoid. Nothing enforces that correspondence, so the gaps are found one eval run at a time.\n\nDo this deliberately: enumerate every rule in constraintConsistency, classify each as structural (checkable on the extraction payload before model construction) or semantic (needs the built model), and confirm conformance mirrors every structural one. Removal is usually right for a malformed constraint -- it charges 0.02 as a correction rather than 0.1 as an error, which is the correct relative price.\n\nConsider whether the correspondence can be made mechanical rather than remembered. A test that feeds deliberately-malformed constraints of every type through conformance and asserts the model validates clean would catch the next gap without a live run, though it needs a generator that knows what malformed means per type.","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T18:35:00Z","updated_at":"2026-08-22T20:05:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-22T20:05:00Z","design":"barwise/docs/specs/constraint-conformance-audit.spec.md","notes":"Audit complete. 19 rule ids across 20 diagnostic sites in constraintConsistency, classified structural/semantic in the spec.\n\nOne real gap found: constraint/ring-different-players (error, structural, unmirrored). A ring whose two roles are played by different object types survived conformance and the parser and landed in the model as an unavoidable validation error -- the same shape as barwise-826 and barwise-830. Fixed by conformance check 5c, which removes it and charges 0.02 as a correction rather than 0.1 as an error.\n\nMost of the class turned out to be already closed, but by the PARSER rather than by conformance: seven invalid-role rules are unreachable because resolveRolesByPlayerName only returns ids drawn from the fact type it was handed, and the three cardinality rules are unreachable because the extraction vocabulary has no cardinality type. Recorded so a future auditor reading conformance alone does not add ten dead checks.\n\nTwo rules are correctly unmirrored: external-uniqueness-all-local and spanning-all-roles are modeling advisories, not defects.\n\nThe durable half is tests/ConstraintCorrespondence.test.ts: one malformed shape per type in the extraction vocabulary, asserting nothing surviving conformance produces a constraint/* ERROR (severity-scoped on purpose, so the two advisories are not forced into a wrong fix), plus a coverage test that fails if a new InferredConstraintType goes unswept. Verified by mutation: disabling the ring check fails 4 tests including the sweep entry."}
{"_type":"issue","id":"barwise-832","title":"Provenance called every run dirty because of untracked files","description":"resolveProvenance ran `git status --porcelain`, which lists untracked files as `??` entries. Any scratch note, editor swap file, or build output therefore marked a recorded eval run as having a modified tree, and `describeProvenance` printed `-dirty` on a checkout whose tracked source exactly matched the commit.\n\nThe compounding part is what makes it more than cosmetic: `history.jsonl` is neither tracked nor gitignored. So the FIRST run to record history created the untracked file that made every subsequent run report an unreproducible commit. The flag degraded to always-true the moment the feature it annotates started being used.\n\nWhat dirty is for is deciding whether the recorded commit reproduces the run, so it has to mean the tracked source moved. Fixed with `--untracked-files=no`.\n\nUntracked files are not entirely irrelevant -- an unversioned variant under --artifacts really would change what ran -- but promptHash already covers exactly that case by fingerprinting the prompt actually sent, rather than inferring it from the state of the working tree. Guessing from untracked files would be a worse instrument for the same question.\n\nReported by the user, who noticed dirty:true on a checkout where they had only untracked files and had changed nothing committed. Regression test builds a real git repo carrying the barwise marker and asserts clean, still clean with an untracked history.jsonl beside it, and dirty once a tracked file actually changes.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T19:00:00Z","updated_at":"2026-08-22T19:05:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-833","title":"Eval scores could not explain themselves: unnamed warnings, discarded collapse payloads","description":"The first repeat=5 baseline produced a mean it could defend statistically and not explain mechanically.\n\nuniversity-enrollment: mean 0.540, sd 0.307, 4/5 surviving, quality 0.675, [corrections=1.6 warnings=6]. Two distinct problems averaging together -- six warnings costing 0.30 on EVERY run (systematic), and one run in five missing the CourseOffering entity entirely (variance, 76% of all suite noise). The collapse-floor split is what separated them; without it one number would have produced the wrong diagnosis.\n\nNeither could be pursued further. scoreExtraction counted diagnostics by severity and discarded them, so the report could say six and not which six -- against seven completeness/* rules, none of which fire on the recorded answer keys, so the cost is addressable. And the payload of the collapsed run was scored and dropped, so there was no way to ask what it produced instead of CourseOffering.\n\nFixed per docs/specs/eval-diagnosis.spec.md: CaseScore.warningsByRule, a suite-level tally rendered under the report, CaseRun.payload kept ONLY for sub-floor and unscorable runs, and --save-payloads to write them.\n\nHistory gains per-case penalty counts and the suite warning tally. Scoping note: HistoryEntry.tokens needed an argument because caching is score-neutral; warnings need none, being score-constitutive and ~80% of everything lost. A row carrying a mean without them cannot say whether a score moved because warnings fell or because a rubric check began passing. Per-rule detail stays suite-level -- seven rules across seven cases is forty-nine numbers answering a question nobody asks per case.","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T19:10:00Z","updated_at":"2026-08-22T19:22:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-834","title":"Extend the conformance/validator correspondence audit beyond constraintConsistency","description":"barwise-831 audited core/src/validation/rules/constraintConsistency.ts and found one real gap (ring player identity), plus a mechanical sweep that now guards that module: llm/tests/ConstraintCorrespondence.test.ts.\n\nThat audit was scoped to one rule module because the issue scoped it there. The same argument applies to every other rule module the extraction path can trip -- a purely structural rule with no conformance counterpart becomes a validation error the extraction cannot avoid, charged at 0.1 instead of the 0.02 a dropped malformed constraint costs.\n\nEnumerate the other rule modules under core/src/validation/rules/, classify each rule structural or semantic the same way, and extend the sweep where a structural gap exists. Note that the constraintConsistency audit found most of its class already closed by the PARSER rather than by conformance -- check for that before adding a conformance check, or the result is dead code.\n\nSee docs/specs/constraint-conformance-audit.spec.md for the classification format and for why the sweep asserts on severity error rather than on diagnostics generally.","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T20:05:00Z","updated_at":"2026-08-22T22:05:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-22T22:05:00Z","notes":"Audit complete, and the headline is the ratio: 51 error-severity sites across eight rule modules, ONE gap. Four of the five earlier finds in this class were real gaps, so a reader arriving from that run would expect a mess and would be wrong.\n\nThe gap: structural/subtype-cycle. Both edges of a two-node cycle resolve perfectly well, so the parser builds them and the model carries a cycle it is then rejected for. Conformance now drops the edge that CLOSES the cycle, keeping every relationship declared before it.\n\nEverything else is unreachable, and by construction rather than luck: eleven of twelve structural rules are stopped because the parser resolves names against what it has already built and skips what it cannot; joinConstraintRules needs a joinPath nothing in llm/src ever builds; projectRules takes an OrmProject and extraction produces an OrmModel; population/cardinality needs ObjectType.cardinality, absent from the payload. Recorded so a future auditor does not add ~26 dead conformance checks.\n\nAlso worth knowing: the population/* modules are almost entirely WARNINGS, not errors -- a population contradicting a constraint is lint, not invalidity. That is why the population side is far less exposed than the constraint side, and it is not obvious from reading either module alone.\n\ntests/StructuralCorrespondence.test.ts extends the mechanical sweep. It asserts the OUTCOME (no error reaches the model) rather than the mechanism, so a change moving responsibility between parser and conformance still passes while a parser admitting unresolved names fails. Verified by mutation: disabling the cycle check fails 5 tests.","design":"barwise/docs/specs/validation-rule-audit.spec.md"}
{"_type":"issue","id":"barwise-835","title":"Conformance check: every population instance must supply a value for every role","description":"Both prompt variants carry the line 'Every instance MUST supply a value for EVERY role of the fact type', and nothing in code enforces it -- cleanPopulations checks for empty instances, orphaned fact types, and populations duplicating a value constraint, but never that an individual instance is complete.\n\nThis is the one promotion candidate from barwise-813 that is squarely a deterministic check rather than a modelling judgement. The fact type's roles are known, the instance's role_values are known, and completeness is a set comparison. The other candidates cannot move: code can DETECT a missing uniqueness constraint (completeness/fact-type-without-uniqueness) but cannot decide whether it is many-to-one or many-to-many, and can detect a missing inverse reading (structural/binary-missing-inverse-reading) but cannot safely generate the inverse wording.\n\nFollow the established shape: drop the incomplete instance (or the population, if that empties it) and record a correction, charging 0.02 rather than letting an invalid population reach the validator. Note the resolution nuance from barwise-831 -- role_values may be keyed by player name OR role name, so completeness must accept either spelling, the same way repairIdentifierPopulations already does when computing coverage.\n\nTestable with no API calls, which is the point: it removes one line from what the paid measurement round in barwise-813 has to settle. See docs/specs/artifact-resolution-in-production.spec.md, 'Workstream 3, revisited'.","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T20:40:00Z","updated_at":"2026-08-22T20:55:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-22T20:55:00Z","design":"barwise/docs/specs/population-instance-completeness.spec.md","notes":"Done, and the investigation found a second, larger defect underneath the one filed.\n\nAs filed: population/incomplete-instance is an ERROR in the validator and conformance never checked it, so an instance short a role value cost 0.1. Conformance now drops the instance and charges 0.02 -- fourth instance of the barwise-826 class.\n\nThe larger defect: a self-referencing fact type could not have a valid population by ANY spelling. parsePopulations resolved role_values keys by player name only and took rolesForPlayer(...)[0], so on 'Employee mentors Employee' the player-name spelling {\"Employee\":\"Alice\"} can only carry one key (JSON forbids duplicates) and lost a role to incomplete-instance, while the role-name spelling failed to resolve and was dropped with a warning. Every ring and self-referencing fact type -- mentoring, prerequisites, org hierarchies, part-of -- was structurally incapable of carrying an example. No prompt text could have fixed that: the model was being asked for something the parser would not accept.\n\nThe fix aligns the two resolvers in the same package. parse/helpers.ts already did this correctly for CONSTRAINTS (role name first case-insensitively, then player name, each match consuming a role); parse/populations.ts now does the same. That is why the recorded project-staffing RING CONSTRAINT worked while a population of the same fact type could not.\n\nVerified by mutation, not by being green: reverting the parser fails 2 tests, disabling the conformance check fails 4. All seven answer-key scores unchanged (the guard that mattered) and the full monorepo gate is green.\n\nConsequence for barwise-813: the prompt bullet 'every instance MUST supply a value for every role' is now partly redundant with code, which is one more line the promotion round does not have to settle."}
{"_type":"issue","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T21:30:00Z","updated_at":"2026-08-22T21:35:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"id":"barwise-836","title":"Record what the pipeline changed and what it could not fix","design":"barwise/docs/specs/pipeline-observability.spec.md","description":"Design proposal in docs/specs/pipeline-observability.spec.md. Sibling to barwise-815, which covers what a CALL cost; this covers what the pipeline DID to the payload.\n\nTwo concrete losses today.\n\n1. Conformance corrections are structured at the source -- ConformanceCorrection {category, description, element} -- and TranscriptProcessor.ts:126 flattens them to warnings: string[] prose one line after enforceConformance returns them. The category is unrecoverable downstream, so no surface can count corrections by kind and nothing can be aggregated.\n\n2. Validation diagnostics all carry a ruleId, and only the eval harness keeps any of it -- warningsByRule. Errors get a bare count in both the report and the history row. Errors cost 0.1, twice a warning, so the more expensive signal is the less diagnosable one.\n\nThe consequence was concrete: this session could not determine whether the ring-player fix (barwise-831) or the population fix (barwise-835) moved the recorded baselines, because the record counts errors without naming them. Not a measurement failure -- the runs were measured -- but the measurement was summed before it was stored.\n\nDesign constraint that decides the shape: corrections do NOT go on DraftModelResult. That type answers 'what model did this transcript produce and where did each part come from'; corrections answer 'what did a cleanup pass change'. observe/ is the component for the second, and it already exists. Note DraftModelResult already carries modelUsed/usage/latencyMs, which are call telemetry and belong in observe/ by the same argument -- deferred, not overlooked.\n\nExtraction does not validate (ValidationEngine is constructed at 13 downstream sites), so these are two moments and get two records rather than one half-empty one.","closed_at":"2026-08-22T21:35:00Z","notes":"Done, together with barwise-837 and workstream 2 of barwise-815 -- all three are the same seam and splitting them would have left the record half-written.\n\nobserve/extractionLog.ts is the sibling of callLog.ts: corrections recorded BY CATEGORY, parser warnings and skipped constraints as counts, plus what reached the model. processTranscript gained observer/now/correlationId and emits only when someone is listening.\n\nNothing was added to DraftModelResult, and a test asserts that directly rather than leaving the decision to review. The prose warnings are untouched -- surfaces render them to users, and a user who stopped being told their constraint was dropped would be worse off than before.\n\nThe no-content rule is tested over the SERIALISED record rather than field by field, because a field added later without thought is exactly how transcript text would leak.\n\nBoth import commands are wired, not one: batch.ts builds a client the same way, and wiring one of two is the divergence pattern this project keeps finding."}
{"_type":"issue","status":"closed","priority":1,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T21:30:00Z","updated_at":"2026-08-22T21:35:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"id":"barwise-837","title":"Errors are counted but never named: add errorsByRule alongside warningsByRule","description":"The smallest, highest-leverage piece of barwise-836, split out because it is independently shippable and blocks a decision that is waiting on it.\n\nscoreExtraction returns warningsByRule (which rule warned, how many times) and validationErrors (a bare count). history.jsonl records penalties {corrections, errors, warnings} -- three counts. So a recorded run can say it had two validation errors and cannot say which rules produced them.\n\ndocs/specs/eval-diagnosis.spec.md closed exactly this gap for warnings on the argument that 'the count alone says a run lost 0.30 without saying to what'. The identical argument applies to errors and they cost twice as much.\n\nBlocks the re-run decision: whether barwise-831 and barwise-835 moved the train/dev baselines is answerable from the record only if the record names the rule. Without this, the only way to find out is to spend money re-running, which is the wrong lever.\n\nMust land in BOTH scoreExtraction and the history row -- adding it to one repeats the original defect one level up.","closed_at":"2026-08-22T21:35:00Z","design":"barwise/docs/specs/pipeline-observability.spec.md","notes":"Done. errorsByRule landed in all four places -- CaseScore, the SuiteReport tally, the history row, and the CLI report -- because the spec's own risk note said both or neither: adding it to the scorer alone would have repeated the original defect one level up, which is exactly how the warning tally came to exist in one place and not the other.\n\nThe two tallies now share one helper in both the scorer and the runner. Two copies is how they came to differ in the first place.\n\nThe CLI prints errors above warnings, since errors cost twice as much and that line is what says whether a conformance fix actually landed.\n\nConsequence for the re-run question: whether barwise-831 and barwise-835 moved the baselines is now answerable from any FUTURE run's record. It is still not answerable for the runs already recorded -- those rows carry penalties.errors as a bare count and nothing can retro-fit the identity. That is the cost of having shipped the asymmetry, and it is bounded now."}
{"_type":"issue","id":"barwise-838","title":"The validation-side observability record was specced and not built","design":"barwise/docs/specs/pipeline-observability.spec.md","description":"docs/specs/pipeline-observability.spec.md Scope asked observe/ for a record of what validation found, carrying each diagnostic's ruleId and severity. barwise-836 shipped the extraction-side record and named validation findings in PROMPTLAB (errorsByRule in the scorer, suite report, history row and CLI) -- which covers the eval harness and nothing else.\n\nSo the case the spec used to argue for keeping two moments apart is the case still unserved: ValidationEngine is constructed at 13 call sites, and a `barwise validate` on a hand-written model records nothing. The spec's own words -- 'exactly as worth recording as one that came from a transcript'.\n\nWhat it needs: a ValidationRecord in observe/ (ruleId + severity, counts only -- diagnostic messages interpolate element names the transcript supplied, so the same no-content rule applies as for correction descriptions), plus a summarise helper, plus wiring at the surfaces that validate. Wire ALL of them or state which are deliberately out: wiring one of thirteen is the divergence pattern this project keeps finding, and barwise-836 wired both import commands rather than one for exactly that reason.\n\nFiled against myself: the scope item was not implemented and the implementation notes did not flag it until a re-read caught it. A scope item silently unimplemented is the same defect as a capability matrix asserting parity that does not hold, which is what this spec was written about.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T21:40:00Z","updated_at":"2026-08-22T22:05:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-22T22:05:00Z","notes":"Done. observe/validationLog.ts carries errorsByRule and warningsByRule split by severity, with summariseValidation and emitValidationRecord following the same seam as the other two records (caller supplies clock and sink; a throwing sink is swallowed).\n\nThe info tier is deliberately neither counted nor tallied: it carries no weight in any score and recording it would grow every row for a tier nobody acts on. A diagnostic with no ruleId is recorded as (unattributed) rather than dropped -- dropping it would make the tally disagree with the count, which is the one failure this record exists to prevent.\n\nWired at `barwise validate` and nowhere else, which the issue asked me to state rather than assume. Of the 13 sites constructing a ValidationEngine, most are INTERNAL GATES rather than operations an operator asked for: the export formats validate before writing, learn's check runners validate as part of grading, and scoreExtraction already records the same tallies through the eval harness. Logging those would turn one `barwise export` into several validation events -- noise, not observability. MCP is the one worth revisiting when a sink exists on that surface."}
{"_type":"issue","id":"barwise-839","title":"orphaned_reference_mode charges 0.02 for a condition core rates 'info', and mirrors no validator rule","description":"Surfaced by correctionsByCategory the moment it existed. Scoring the seven recorded answer keys gives 14 conformance corrections and ALL FOURTEEN are one category: orphaned_reference_mode. Those payloads carry zero validation errors and zero warnings, so at 0.02 each this single rule is the entire gap between them and 1.0 (their scores run 0.94-0.98).\n\nTwo things make it worth a decision rather than a shrug.\n\n1. It is DETECT-ONLY. checkOrphanedReferenceModes records a correction saying an entity has a reference_mode with no identifier fact type, and changes nothing. Every other category removes or repairs something; this one is charged the same 0.02 for noticing. 'Correction' is arguably the wrong word for it, and the weight is arguably the wrong price.\n\n2. It is a FLOOR under every case, so it is baked into every recorded baseline and into the barwise-813 promotion decision without anyone having chosen it.\n\nThe question is which of three: (a) the detection is right and the charge is right -- an entity with a reference mode and no identifier fact type really is an incomplete extraction, and the prompt should be taught to emit the identifier fact type; (b) the detection is right but detect-only findings should not be charged as corrections, in which case it wants its own tier; (c) the detection is too eager -- a reference mode without an identifier fact type may be perfectly acceptable in a draft -- and it should not fire at all.\n\nDo NOT change the weight before deciding which. Every recorded history row prices this rule at 0.02, so a reweight silently reinterprets the whole record -- the failure mode barwise-836 exists to stop. (a) needs no weight change at all and is testable against the answer keys for free.","status":"closed","priority":1,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-22T22:05:00Z","updated_at":"2026-08-23T13:40:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"notes":"INVESTIGATED 2026-08-23. The evidence points to option (c) -- the detection is too eager -- and more sharply than the issue anticipated. Four findings, each checked against the source:\n\n1. NO VALIDATOR COUNTERPART. Every other conformance check exists to prevent a specific validator error; that is the whole justification of the module. Nothing in core/src/validation/rules/ mentions referenceMode at all. This check mirrors nothing -- it invents a rule.\n\n2. CORE RATES THE NEAREST THING 'info', WHICH COSTS ZERO. completeness/missing-preferred-identifier is severity info and its message says the relational mapper will use a heuristic. So core's own position is that an entity without an explicit identifier is FINE. Conformance charges 0.02 for it -- more than core charges for the closest real diagnostic.\n\n3. THE PARSER MANUFACTURES A REFERENCE MODE FOR EVERY ENTITY. parse/objectTypes.ts does `ext.reference_mode ?? camelCase(name) + '_id'`, because ObjectType THROWS if an entity has none. A reference mode is mandatory, not optional metadata, so 'has a reference mode' is not evidence of anything.\n\n4. IT FLAGS WHAT THE NOTATION IS FOR. In ORM 2, Patient (medical_record_number) IS the shorthand for the identifier fact type. Every one of the 14 answer-key hits is an ordinary reference mode -- sku, employee_id, room_number -- on an entity modelled exactly as ORM intends. The check asks the extraction to spell out the thing the notation abbreviates.\n\nPlus the evidence that started this: it fired 0 times across 15 live dev runs and 14 times across 7 hand-curated answer keys. It is not measuring extraction quality.\n\nRECOMMENDATION: remove the check (option c). Consequence worth seeing before agreeing -- all 14 answer-key corrections are this rule, so removing it takes every recorded answer key from 0.94-0.98 to exactly 1.000. That is arguably the point: the reference payloads should score 1.0, and they do not solely because of a check that mirrors nothing.\n\nNOT DONE OVERNIGHT, DELIBERATELY. Any change here -- removing it, or keeping the detection while not charging it -- moves the answer-key pins in scoreExtraction.test.ts and makes every recorded history row incomparable, including the dev baseline measured hours earlier. That is the same 'do not land it between two runs being compared' hazard flagged on barwise-840, and this one is 14 corrections rather than 1 error. It needs a yes, not an inference from a sleeping reviewer, and it is a one-word decision now that the evidence is in.\n\nCLOSED 2026-08-23: option (c), removal, approved by the user and implemented. docs/specs/reference-mode-charge-removal.spec.md.\n\ncheckOrphanedReferenceModes, its call site and identifierFactTypeEntities (which nothing else read) are gone. All seven recorded answer keys now score exactly 1.000, up from 0.94-0.98.\n\nSUITE VERSION BUMPED 1.2.0 -\u003e 1.3.0. It changes no case, no weight and no floor; it marks a change outside the manifest with the same consequence, since every payload scored under 1.2.0 carried a charge that no longer exists. The version is the field a reader uses to decide whether two history rows can be compared, and after this they cannot.\n\nWORTH CARRYING FORWARD. Two tests were leaning on an answer key being imperfect: the weights-and-floor test needed three corrections to exist, and the penalties-reach-the-history-row test read the one correction every key happened to carry. Both now manufacture their own penalty. That dependency is exactly how this check became load-bearing without anyone choosing it -- the issue's own framing ('a floor under every case, baked into every recorded baseline') turned out to hold inside the test suite too.\n\nAlso: two collapse-floor tests used 0.999 as 'above every achievable score'. With clean answer keys that no longer holds, so both now use 1.001. A test that silently stops testing what it names is the barwise-840 failure mode.\n\nA regression test asserts the charge does NOT happen, carrying all four findings inline so the next reader does not re-derive the question and re-add the check.\n\nFull gate green: 1403 core, 418 llm, 154 promptlab, 219 cli, 179 mcp, 69 vscode.","closed_at":"2026-08-23T13:40:00Z","design":"barwise/docs/specs/reference-mode-charge-removal.spec.md"}
{"_type":"issue","id":"barwise-840","title":"Conformance counts role hints; the parser counts resolved roles, so duplicate hints bypass the arity check","description":"Found by the first diagnostic round with errorsByRule (2026-08-23 dev sweep, haiku-4-5, default artifact). One validation error in fifteen runs, and it is a rule conformance is supposed to make impossible: constraint/disjunctive-mandatory-too-few-roles on incident-response.\n\nReproduced deterministically. isValidArity checks ic.roles.length -- the number of role HINTS the model emitted. resolveRolesByPlayerName consumes each role it matches, so two hints naming the same player resolve to ONE role id. Conformance sees 2 and passes it; the parser builds a disjunctive_mandatory with a single roleId; the validator rejects it. 0.1 the extraction could not avoid.\n\n  roles: ['Incident', 'Incident']  -\u003e  conformance: length 2, ok\n                                    -\u003e  parser: 1 resolved role\n                                    -\u003e  validator: must reference at least 2 roles\n\nSixth instance of the barwise-826 class, and the first that is not a MISSING check but a check measuring the wrong quantity. My barwise-831 sweep missed it because every probe used distinct resolvable names -- the sweep tested the check's existence, not its arithmetic.\n\nFix: resolve the hints in conformance the way the parser does (role name first case-insensitively, then player name, each match consuming a role -- the discipline barwise-835 already aligned parse/populations.ts to) and apply the arity rule to the RESOLVED count. Applies to every arity branch, not just disjunctive_mandatory: ring (exactly 2), mandatory/frequency/value_constraint (exactly 1), exclusion and exclusive_or (at least 2).\n\nExtend StructuralCorrespondence/ConstraintCorrespondence with duplicate-hint variants; the existing sweeps pass today and would still pass after a wrong fix.\n\nCost is small -- 1 error in 15 runs, 1.9% of the dev penalty -- so this is a correctness fix, not a score fix. Note it WILL move recorded baselines slightly, so land it before the next measurement round rather than between two runs being compared.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-23T03:05:00Z","updated_at":"2026-08-23T03:30:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-23T03:30:00Z","design":"barwise/docs/specs/constraint-conformance-audit.spec.md","notes":"Fixed. The investigation found THREE bypass paths, not the one the issue described -- all producing the same unavoidable error, all invisible to the existing sweep:\n\n  A  ['Incident','Incident']          one role named twice (consuming resolution)\n  B  ['Incident','Customer']          check 4's name set is GLOBAL, not per fact type, so a\n                                      real object type that plays no role here passes it\n  C  ['originates from','Incident']   a role name and a player name selecting one role\n\nThe arity check now measures the RESOLVED count, and deliberately only for disjunctive_mandatory, exclusion and exclusive_or. That scope is exact, not approximate: every other type the parser skips outright when the resolved count is wrong, and a skipped constraint never reaches the validator -- so checking them here would charge 0.02 for constraints already dropped for free, which is a score change dressed as a correctness fix and would land on every recorded baseline at once. The two uniqueness types do build on a short resolution, but uniqueness over one role is valid ORM.\n\nOne resolver (resolveRoleIndices) now serves the arity check, the ring-player check and population completeness. Three copies of a consuming name match is precisely how this divergence grew in the first place.\n\nVerified by mutation: disabling the check fails 9 tests, 3 of them sweep entries. Ten hand-checked probes confirm the three paths close, valid constraints are untouched, uniqueness over one role still builds, and ring gains no new charge. All seven answer-key scores unchanged; full gate green (419 llm, 154 promptlab, 211 cli).\n\nWorth carrying: the sweep passed throughout because every probe used distinct resolvable names. It asserted the check EXISTED and never that its arithmetic was right -- the same failure mode one level in from the one barwise-831 was written about."}
{"_type":"issue","id":"barwise-841","title":"prompt eval has never run without an API key, so every line it prints is unverified","description":"barwise prompt eval is the one command in the repo that has never been held to the determinism preference: every line it prints has only ever executed with an API key in the environment. promptlab/tests/runSuite.test.ts covers the runner with a fixture client, but nothing covers what the CLI does around it -- artifact resolution and its version line, the incomplete-run warning and exitCode 1, the truncation warning, --save-payloads, the history append and its provenance, and createLlmClient from flags with the budget it derives. Each of those is a line an operator reads while a sweep is running, or a file they rely on afterwards, and a wrong one costs the round that discovered it.\n\nInstrument: a loopback ollama server, not an injected mock client. The CLI builds its own client from its own flags, and that construction is part of what is untested -- injecting past it would test a path no operator takes while leaving the real one uncovered, and would put a production seam in place solely for a test. The ollama provider already takes --base-url, speaks a documented NDJSON protocol over fetch, and returns the payload as bare content, which is exactly the shape the recorded fixtures hold. So nothing in barwise is stubbed; the fake is the server.\n\nSeven train cases run in about half a second, against roughly twenty minutes and real money for the same sweep against Anthropic.\n\nSee docs/specs/offline-eval-rehearsal.spec.md.","status":"closed","priority":1,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-23T04:00:00Z","updated_at":"2026-08-23T04:20:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-23T04:20:00Z","design":"barwise/docs/specs/offline-eval-rehearsal.spec.md","notes":"Implemented 2026-08-23. Eight tests in cli/tests/commands/promptEvalOffline.test.ts over a fake server in cli/tests/workspace/fakeOllama.ts. No production code changed -- if the spec had required any, the design would have been wrong.\n\nMutation-checked: seven deliberate breakages, each caught by exactly one test. One-for-one is the result worth recording; a mutation tripping four tests says they overlap and one tripping none says the assertion was decorative.\n\nThe sharpest of the seven: resolve the variant and then never pass it to runSuite. The 'Using artifact version' line stays intact and the default prompt goes on the wire -- a comparative round that reports the variant it never ran. Only reading the system prompt back off the wire catches it, which is why that assertion is there rather than a check on stderr.\n\nTwo hazards found by building it, both recorded in the spec because the symptom points nowhere near the cause:\n1. Node's fetch pools connections, so server.close() waits on a socket that is never coming back and the test process hangs after the assertions pass. The first probe hung four minutes. Teardown must call closeAllConnections().\n2. historyPathFor is dirname(manifest)/history.jsonl, so a test running the packaged suite would append to the repository's own recorded history. Every test copies evals/ to a temp dir first. A score record a test can write to is not a record.\n\nIncidental confirmation of barwise-839: the rehearsal reproduces the answer-key means exactly (0.98 0.96 0.96 0.94 0.98 0.96 0.94) and reports 'fixed: orphaned_reference_mode x14' as the entire penalty."}
{"_type":"issue","id":"barwise-842","title":"prompt eval resolves the artifact from flags, not from the client, so omitting --provider silently runs the default prompt","description":"Confirmed deterministically against the built resolver (2026-08-23):\n\n  --provider anthropic --model claude-haiku-4-5   -\u003e haiku45-2\n  --provider anthropic --model claude-sonnet-5    -\u003e sonnet5-3\n  (no --provider)     --model claude-haiku-4-5    -\u003e DEFAULT ARTIFACT\n\nprompt eval passes opts.provider and opts.model -- the FLAGS -- into resolveArtifact. When --provider is omitted the CLI still builds a working client, because createLlmClient auto-detects the provider from env vars, but resolveArtifact is handed provider: undefined and no variant can match its 'provider: anthropic' clause. The run then uses the default prompt while the operator believes it used the variant they passed --artifacts for.\n\nThe client already knows the answer. LlmClient declares provider and model precisely so a variant can be resolved before the call -- that is the documented reason the fields exist (packages/llm/CLAUDE.md, 'A client declares its own identity'), and processTranscript resolves from the client. The eval command resolves from the flags instead, so the two disagree about what provider is in play. Same shape as the rest of this session's findings: a value the system had already computed, discarded at a boundary in favour of a weaker one.\n\nMitigating: the command does print 'Using the default prompt artifact.' and that line is truthful. It is easy to miss at the head of a sweep that then runs for twenty minutes.\n\nNOT a silent failure to fix casually. Resolving from the client would also change a run that passes neither --provider nor --model: today that gets the default artifact, and afterwards it would get whichever variant matches the anthropic default model. That is a real behaviour change to a plausible command line, so it wants a decision rather than a patch, and it must not land between two runs being compared.\n\nFound by barwise-841's mutation check, which asks exactly this question: does the command send the variant it says it resolved? The mutation was hypothetical; this is the same failure reachable through ordinary flag usage.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-23T04:25:00Z","updated_at":"2026-08-25T00:15:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"notes":"Fixed 2026-08-25 as part of barwise-850. Resolution moved to AFTER the client is built and now keys on client.provider / client.model rather than opts.provider / opts.model. createLlmClient auto-detects the provider and every provider resolves its own default model (AnthropicLlmClient.model is `readonly model: string`, defaulted), so a client built with neither flag knows both -- which is the documented reason LlmClient carries them.\n\nGrounding corrected the issue's scope: the bug was worse than filed. Omitting --provider was not the only way to get the default -- prompt eval never consulted builtinArtifacts under ANY flags, because resolution sat behind `if (opts.artifacts)`. See barwise-850.\n\nRegression test: promptEvalOffline 'resolves the variant from the client's model, not from the --model flag'. Ollama defaults to llama3.1, which no flag mentions, so only a client-side resolution finds a variant matching modelPrefix llama3. Mutation-checked.","closed_at":"2026-08-25T00:15:00Z"}
{"_type":"issue","id":"barwise-843","title":"Build the DSPy optimizer lane (harness workstream 3), never grounded since 2026-08-08","description":"Workstream 3 of docs/specs/prompt-optimization-harness.spec.md, carried as 'provisional: not yet grounded' since 2026-08-08 and never built. Requested directly.\n\nThe lane: a Python project under barwise/optimizer/, uv-managed, outside the npm workspace, never imported, never in CI, never on the runtime path. It crosses to TypeScript only through two subprocess calls -- barwise prompt schema and barwise prompt score -- which already exist as ordinary commands. No shared library, no FFI, no second copy of the schema or the scorer.\n\nGrounding the harness spec asked for, now measured. (1) dspy 3.3.1 installs with BootstrapFewShot, MIPROv2, GEPA, COPRO. (2) DSPy renders its own field protocol around the instructions, which production never emits -- so the compiled program's score is a SEARCH SIGNAL and the accepted number always comes from a real barwise prompt eval. (3) Demos do not fit naively: the system prompt is ~4,540 tokens and demo payloads run 1,103-3,851 (mean 1,984), so BootstrapFewShot's 4-16 default is a 3x-8x prompt paid on every call of every candidate. The exporter budgets and truncates; the compile step caps demos at 2.\n\nSee docs/specs/dspy-optimizer.spec.md.","status":"closed","priority":1,"issue_type":"feature","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-23T14:00:00Z","updated_at":"2026-08-24T16:10:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-23T15:30:00Z","design":"barwise/docs/specs/dspy-optimizer.spec.md","notes":"Implemented 2026-08-23. 55 tests, about a minute, no API key and no network.\n\nTHE RESOLUTION PROBLEM IS BUILT IN RATHER THAN ARGUED ABOUT. At repeat=5 the suite's resolvable difference is ~0.086, and DSPy is the technique most sensitive to that. So: --max-calls and --samples-per-candidate are REQUIRED (no library default for anything that costs money), samples below 2 is refused because the run could not then report a spread, and every run writes a delta report that leads with whether the margin cleared the noise band and labels one inside it as not evidence. The metric also records errorsByRule / warningsByRule / correctionsByCategory per evaluation, so the report says WHICH NAMED RULE MOVED -- a count from the same calls, legible where a 0.03 shift in a mean is not.\n\nFOUR BUGS FOUND WHILE BUILDING, none visible from reading the code:\n\n1. MIPROv2 reads dspy.settings.lm in its constructor and raises without one; BootstrapFewShot constructs happily and fails much later. The same ordering mistake read as 'mipro is broken'. build_optimizer now requires an LM uniformly.\n2. GEPA requires a separate reflection_lm. --proposer-model was already recorded in provenance but never wired to the optimizer that needs it -- a value computed and dropped at a boundary, the same class as everything else this week. Now wired, and gepa without one is refused at config time so it costs a flag rather than a run.\n3. The exported instructions carried the 11.5 KB schema (candidate: 20 KB), duplicating in every request what production sends as the tool's input_schema, and handing MIPROv2 a schema to paraphrase. Schema moved to the output field description.\n4. THE CANDIDATE HAD NO match BLOCK. resolveArtifact filters to artifacts declaring one, so it loaded without complaint and was silently skipped -- the operator would read 'Using the default prompt artifact.' while believing they had gated the candidate they paid to compile. Same failure class as barwise-842 one layer out. match is now required and derived from the target model.\n\n3 and 4 were found by SMOKE-RUNNING run() offline, and 4 specifically by round-tripping the export through the real prompt eval --artifacts loader rather than asserting on the exporter's own YAML, which would only have proved the package agrees with itself. Both are now tests.\n\nMutation-checked: eight deliberate breakages, each caught; the last two only by the smoke test and the loader round trip.\n\nNOT DONE, deliberately: running a real compilation. The lane is built; spending money on it is a separate human act, and the run report exists so that when someone does, the result says what it can support.\n\nTWO REAL COMPILATIONS RUN (2026-08-24), bootstrap on claude-haiku-4-5, 32 calls each.\n\nRUN 1, --seed-from minimal. Both arms floored at 0 (penalties ~0.98 and ~1.02/run against a rubric fraction of at most 1.0). Uninterpretable as a comparison, but it produced two design corrections (the saturation guard and --seed-from itself) and one real result: demos fixed format adherence, unparseable 5 -\u003e 0 across 15 evaluations. That is barwise-812's question answered on real content.\n\nRUN 2, --seed-from default (haiku45-2). Clean: floored 0, unparseable 0.\n  baseline  mean 0.870   10 corrections, 35 warnings -\u003e 0.130/run penalty\n  candidate mean 0.840   20 corrections, 40 warnings -\u003e 0.160/run penalty\n  margin -0.030 against a resolvable difference of 0.0999 -- INSIDE the noise band.\n\nBoth arms reconcile to the digit, which means rubricPassed == rubricTotal on every evaluation: haiku passes the whole rubric on all three held-out cases every time, and the entire gap from 1.0 is penalty rather than missing content.\n\nVERDICT: do not adopt. The candidate is nominally worse and the difference does not resolve. The demos drove arity_mismatch 5 -\u003e 20 (a count, not a mean, so a real pattern) while fixing duplicate_constraint 5 -\u003e 0. Bootstrap's demos are the model's own outputs, so it propagates whatever the traces got wrong -- the same feedback loop as run 1, milder.\n\nTHE REMAINING TARGET IS ONE RULE. completeness/fact-type-without-constraints at 30 occurrences = 0.100/run = 77% of the entire remaining penalty. That is an instruction problem, so it wants mipro rather than bootstrap; bootstrap cannot touch instructions at all.\n\nINCIDENTAL, FOR BARWISE-813. Against the earlier dev diagnostic on the DEFAULT artifact (same 3 cases, same 5 samples): fact-type-without-uniqueness 18 -\u003e 0, fact-type-without-constraints 50 -\u003e 30. Warning counts are unaffected by the 839 correction removal, so they compare across the suite bump. Direct evidence that haiku45-2 beats the default on exactly the rules its bullets target. CAVEAT: the earlier numbers came through the production path and these through DSPy's adapter, which renders differently -- suggestive, not clean. A `prompt eval --artifacts packages/llm/prompts --provider anthropic --model claude-haiku-4-5 --split dev --repeat 5` would settle it through the real renderer for ~15 calls and append a comparable history row."}
{"_type":"issue","id":"barwise-844","title":"Eval penalties are counted, not rated, so a bigger model is charged more for the same defect rate","description":"scoreExtraction computes rubricPassed/rubricTotal (bounded to [0,1]) minus sum(weight * occurrences) (unbounded, growing with model size). The reward side is normalised and the penalty side is not, so the same defect RATE costs a large model several times what it costs a small one.\n\nMeasured from the recorded compilations: on the dev split, structural/binary-missing-inverse-reading occurred at a mean of 10.67 per scored run; at validationWarning 0.05 that one rule costs 0.53. The train reference models hold a median of 3 fact types in total, so the same rule at a 100% defect rate costs a median train case 0.15. Same rate, 3.5x the charge, decided entirely by transcript length.\n\nPast 1.0 the clamp in scoreExtraction takes over and the damage compounds: floored arms all report 0.000 however different they were, and the identical values collapse the sample SD, shrinking resolvableDifference so a meaningless margin can clear a threshold the flooring itself manufactured. Three of the four recorded compilation arms floored.\n\nFix: penalty(rule) = weight * occurrences / max(elementCount, 1), with elementCount = objectTypes + factTypes recorded on CaseScore. The weight then means the cost of a model in which every element carries that defect. Weights must be re-fitted in the same change and the suite bumps to 2.0.0.\n\nThe answer-key invariant survives untouched (zero occurrences over any denominator is zero, so all seven keys still score 1.000) -- which means it CANNOT detect a wrong denominator. Mutation-check by inverting it, and pin at least one non-perfect score.\n\nWorkstream 1 of the spec.\n\nDECIDED 2026-08-24: weights are validationWarning 0.4, validationError 0.8, conformanceCorrection 0.2, read as 'a model in which every element carries a warning loses 0.4'. Checked against the recorded dev run first: at an estimated 24 elements the two dominant rules cost about 0.27 together under these weights against 0.81 under today's, close to the 3x distortion being removed. ambiguityExcess stays at 0.02 and stays UNRATED -- it is already charged against a per-case authored budget, so rating it would divide twice.\n\nOne denominator for every rule, not per-rule denominators. The approximation was measured: across the seven reference models fact types are 33-50% of total elements and the dominant rules all fire on fact types, so the denominator is wrong by a near-constant factor, which a weight absorbs. Per-rule denominators would make every new validator rule a promptlab change. Retrofitting them later costs a second re-baselining and that was accepted knowingly.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-24T21:30:00Z","updated_at":"2026-08-30T13:51:58Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/specs/eval-split-stratification.spec.md","closed_at":"2026-08-30T13:51:58Z","close_reason":"Shipped and verified. scoreExtraction.ts rates every penalty by model size rather than counting it -- the `rated()` helper at line 187 divides each occurrence count, and the comment at 161 states the rule ('Penalties are rated by model size, not counted'), with the ambiguity-excess term deliberately left unrated because it is charged against a per-case authored budget. Landed in PR #336, whose branch is literally claude/barwise-844-penalty-rates, at suite 2.0.0.\n\nFound stale by the barwise-912 class-C pass. eval-split-stratification.spec.md's Status has said 'Workstream 1 (rate-based penalties, suite 2.0.0) implemented 2026-08-24 in PR #336' since that merge; the issue was simply never closed.\n\nSecond instance of this shape in two passes -- barwise-cb6 was the first. Both were closed by verifying the artifact, not by trusting either the spec or the store. That is now two data points for the barwise-912 caution: deriving a spec's Status from the beads store inherits whatever the store has not been told."}
{"_type":"issue","id":"barwise-845","title":"The three realism eval cases have no reference model, so the dev split carries zero forbids_population checks","description":"The train and dev splits do not grade the same thing. Across the ten cases:\n\n  forbids_population   train 14 total   dev 0\n  requires_ambiguity   train 0          dev 2\n  requires_element     train 2-3/case   dev 4-5/case\n  reference model      train all 7      dev none\n\nforbids_population is the only check that tests whether a constraint actually forbids what it should -- the evaluator derives a counterexample population from the reference and requires the candidate to reject it. It exists in train and nowhere else. So train grades constraint semantics and dev grades element recall, and a change improving one is under no obligation to improve the other.\n\nThis is why the mipro run's reading was wrong: its own metric rose 50.89 -\u003e 52.97 while every held-out figure fell, and that was recorded as the split catching overfitting. The split may have caught that; it cannot be used as evidence of it, because the two numbers never measured one quantity.\n\nThe cause is mechanical rather than an oversight: references are generated by running a recorded payload through parseExtractionFromJson, no payload has ever been recorded for these three (vendor-onboarding.eval.yaml:7-9 says so), loadSuite refuses a forbids_population check without a reference, and the rubrics were authored around the gap.\n\nFix: record a payload for vendor-onboarding, subscription-billing and incident-response; generate their references; add forbids_population where the transcript settles a constraint, and a requires_ambiguity check to incident-response, which has none despite being a realism case.\n\nWorkstream 2 of the spec. Separable from the re-split and worth landing first -- it is the cheapest step that makes the two halves comparable at all.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-24T21:30:00Z","updated_at":"2026-08-28T00:59:08Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/specs/eval-split-stratification.spec.md","closed_at":"2026-08-28T00:59:08Z","close_reason":"Closed at suite 2.5.0 from the 2026-08-27 haiku45-2 sweep's saved payloads. All three dev cases now carry generated references (best payload per case: vendor-onboarding-run3 at 1.000, subscription-billing-run4 at 0.994, incident-response-run2 at 0.960), fifteen forbids_population checks where the transcripts settle constraints, incident-response's requires_ambiguity plus ambiguityBudget 10, and the suite's first irreflexive ring check with a genuine answer key. Both halves now grade constraint semantics. Answer keys pinned in scoreExtraction.test.ts (two pin below 1.000 over recorded-payload conformance defects, deliberately)."}
{"_type":"issue","id":"barwise-846","title":"Stratify the eval splits so train and dev are two samples of one population, then re-baseline at 2.0.0","description":"Even with rubric parity (barwise-845) the splits stay confounded on transcript shape: train is 154-243 words of single-expert interview, dev is 2,163-2,967 words of multi-party disagreement. Every property that made the three realism cases worth adding is also a property that distinguishes dev from train, because eval-transcript-realism workstream 2 added them and made them the entire dev split in one change.\n\nFix: author new realistic transcripts so each split carries cases at each size class, then reassign splits so neither half is identifiable from its rubric or its transcript length. Then re-baseline both splits at repeat \u003e= 5 for the default artifact and both committed variants, write the first committed rows of evals/history.jsonl, and re-fit collapseFloor against that distribution rather than the seven-case evidence it was fitted to.\n\nTIMING: evals/history.jsonl does not exist in the repository. historyPathFor resolves it beside the manifest, provenance.ts:57 records that it is untracked, and no run has ever written one here. The continuity that eval-metric-readiness cited as the reason to keep the seven in train lives only in operators' local files, so the re-split is nearly free today and gets monotonically more expensive the moment the first committed rows land. Re-check that before starting -- it is the anchor most likely to have gone stale.\n\nWorkstreams 3 and 4 of the spec. Blocked on barwise-844: the number of new cases needed depends on dispersion measured after the penalties are rated.\n\nDECIDED 2026-08-24: three new long transcripts, giving 7 short and 6 long -- train 4 short + 3 long, dev 3 short + 3 long. One new long case is the minimum that removes the confound; the extra two buy a six-case dev half over a four-case one. If the dev error bar is still wide at six cases, raise repeat rather than authoring a seventh: samples cost API calls and cases cost authored domains.\n\nAlso decided: 'no client-derived material in evals/' is now a stated rule in packages/promptlab/CLAUDE.md, written when the decision was made rather than deferred to this workstream.","status":"open","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-24T21:30:00Z","updated_at":"2026-08-28T15:48:36Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/specs/eval-split-stratification.spec.md","notes":"Workstream 4 RECORDED 2026-08-28: all eight arms (both variants + default, both models, both splits, repeat 5) in the first committed history.jsonl rows; full analysis in docs/prompt-baseline-2.5.0-2026-08-28.md. Collapse floor deliberately NOT re-fitted: zero collapses at 0.3 in 120 samples, and the observed minima are barwise-890-distorted scores -- re-fit after the 2.6.0 correspondence bump, against undistorted minima. Verdicts: haiku45-2 +0.097 on dev (resolvable, the record's one proven variant win); sonnet5-3 shows no resolvable value over the default at repeat 5 (may flip at 2.6.0). Remaining in this issue: workstream 3 (transcripts, per eval-difficulty-calibration spec); re-baseline again after the re-split as originally planned."}
{"_type":"issue","id":"barwise-847","title":"reviewModel never resolves a prompt artifact, so the review surface cannot be versioned, hashed or evaluated","acceptance_criteria":"reviewModel resolves a surface: \"review\" artifact from builtinArtifacts using the client's declared provider and model, with an optional artifact override on ReviewOptions; a golden test pins the default rendered prompt byte-for-byte against the pre-wiring literal; parseReviewResponse rejects a suggestion whose category or severity falls outside its enum; the three call sites (CLI, MCP, VS Code) are unchanged.","description":"PromptSurface is declared as \"extraction\" | \"review\", loadArtifact accepts both, and resolveArtifact is generic over the pair -- but every artifact in packages/llm/prompts/ declares surface: extraction and reviewModel never calls the resolver. Its prompt is a string literal returned by buildReviewSystemPrompt(). The type says two surfaces are supported; one is.\n\nSame shape as barwise-811, and the more expensive instance: review is the one LLM capability reachable from all three surfaces (CLI, MCP and VS Code all call reviewModel), so everything the extraction lane learned about prompt quality is unavailable to the surface with the widest reach.\n\nThe call sites need no change. LlmClient carries provider and model precisely so a variant can be chosen before the call, and TranscriptProcessor.ts:116 already resolves from the client rather than from its caller; review can do the same in the same place.\n\nAlso in scope, because the metric depends on it: parseReviewResponse casts obj.suggestions to ReviewSuggestion[] without validating category or severity against their enums. A model answering category: \"style\" produces a typed lie today and would silently match no planted defect once scoring exists.\n\nAcceptance criterion: a golden test asserting the default rendered prompt is byte-identical to the current literal.\n\nWorkstream 1 of the spec.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-24T21:30:00Z","updated_at":"2026-08-24T22:15:56Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/specs/review-surface-evals.spec.md","assignee":"Claude","started_at":"2026-08-24T22:05:00Z","closed_at":"2026-08-24T22:15:56Z","close_reason":"Workstream 1 of docs/specs/review-surface-evals.spec.md. reviewModel now resolves a review artifact from builtinArtifacts using the client's own provider and model, falling back to defaultReviewArtifact (the historical literal); ReviewOptions gains an optional artifact, and an artifact of the wrong surface is rejected before the LLM call. parseReviewResponse validates each suggestion against the declared category and severity enums (and the required string fields) and drops the malformed ones rather than casting the array. A golden fixture pins the default prompt's 2,609 bytes; every new assertion was mutation-checked. The three call sites needed no change, as the spec's Inventory promised. No review artifact was added, so the resolution is a no-op today. Full monorepo build, test and lint green."}
{"_type":"issue","id":"barwise-848","title":"Add ReviewEvalCase and scoreReview: seeded-defect recall with a clean control for precision","description":"The review surface has no metric. Grading advice looks like it needs a judge, which the harness refuses on principle. It dissolves once the question changes from 'was this good advice' to 'did it find the thing that is wrong': plant a known defect in a fixture model and either a suggestion names that element in that category or none does. ReviewSuggestion already carries element and category, so the payload has both fields a match needs.\n\nRecall alone is winnable by volume. The extraction lane hit exactly this with requires_ambiguity and answered with ambiguityBudget; the same answer works here, and the second half is cheaper still -- run the reviewer on the UNMUTATED model, where every suggestion is a false positive by construction and no authoring is needed at all.\n\nThat makes the clean control a case with an empty defect list rather than a special mode: one case shape, no branch, graded by the same fold.\n\n  score = found / max(defects, 1)\n          - suggestionExcess * max(0, suggestions - defects - budget)\n\nA case with no defects scores on the excess term alone. Matching is case-insensitive containment on element plus exact category; a suggestion with no element matches nothing.\n\nReview cases get their own manifest and version -- one version: cannot describe two metrics, and the version is the field a reader uses to decide whether two history rows compare.\n\nWorkstream 2 of the spec. Independent of barwise-847: the scorer grades a payload and does not care how the prompt was built.\n\nDECIDED 2026-08-24: excess is CHARGED as one scalar, matching ambiguityExcess -- reporting recall and suggestion count as a pair hands every consumer the collapse decision, the shape the sibling spec rejects for the extraction score.\n\nMatching rule: a defect is found when some suggestion IN ITS CATEGORY names the element case-insensitively, in `element` or -- when that field is absent -- in `description`. A suggestion naming the element nowhere matches nothing. Strict field-only matching would score a model that names the element in prose as having missed the defect, which measures formatting; category-only matching would let a vague summary claim every find.\n\nSuite selection: the manifest declares `surface: review` and `barwise prompt eval` reads it. No --suite flag and no second subcommand -- a flag and a manifest can disagree about what a suite is, and a manifest alone cannot. A manifest with no declared surface means extraction, which is what every existing manifest means today.","status":"open","priority":2,"issue_type":"feature","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-24T21:30:00Z","updated_at":"2026-08-24T22:05:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/specs/review-surface-evals.spec.md"}
{"_type":"issue","id":"barwise-849","title":"Author the review eval seed corpus, baseline it, and decide whether the review prompt is worth optimizing","description":"Paired fixtures: a clean base model per domain and a defective twin per review category, plus a control case per pair. The pairing is the design -- twins differ only by the planted defects, so a difference between the two cases cannot be attributed to size, domain or serialization.\n\nFixtures are authored rather than generated. Mechanical mutation was considered and set aside: four of the five categories mechanise cleanly and normalization does not (a missing subtype or a redundant fact type is a judgment about the domain, not an edit), so a new subsystem would buy four-fifths of the corpus. @barwise/core has no mutation machinery today; counterexample/ and diff/ are the closest and neither seeds defects.\n\nOpen before authoring: how many planted defects a case can carry before they interfere. A model with a stripped definition and a dropped constraint on the same element may draw one suggestion naming both, and whether that is one find or two is a question the fixtures answer rather than the design. Until it is answered, plant at most one defect per element.\n\nThen baseline each supported provider/model pair at repeat \u003e= 5 and READ THE RESULT BEFORE WRITING ANY OPTIMIZER PROGRAM. If the shipped literal already finds nearly every planted defect, this lane is a regression detector and not an optimization target, and saying so is a result. A first baseline near 100% recall is more likely a sign the corpus is too easy.\n\nWorkstreams 3 and 4 of the spec.\n\nDECIDED 2026-08-24: two domains, each with one clean base model, five defective twins (one per review category, each the base with a single edit) and one control case scoring the clean base with an empty defect list. Twelve cases in all. The authoring cost is dominated by the shared base, so this costs barely more than one defective model carrying all five categories and yields recall PER CATEGORY -- weak normalization recall and weak definition recall call for different prompt changes. Four categories are a mechanical edit; normalization needs a modelling judgment.\n\nAlso decided: NO prompt caching on the review call. Measured rather than deferred -- the review system prompt is 2,609 characters, about 650 tokens, against Haiku 4.5's 4,096-token minimum cacheable prefix, so a breakpoint is inert on the model the suite runs against. Revisit only if the prompt grows past 4,096.","status":"open","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-24T21:30:00Z","updated_at":"2026-08-24T22:05:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/specs/review-surface-evals.spec.md"}
{"_type":"issue","id":"barwise-850","title":"prompt eval never consulted builtinArtifacts, so the shipped variants have never been evaluated","description":"Artifact resolution in `prompt eval` sat behind `if (opts.artifacts)` (prompt.ts:124). Without a directory the command never looked at `builtinArtifacts` at all -- it passed `artifact: undefined` to runSuite, which renders the default directly (runSuite.ts:352). `--provider` and `--model` reached the client but never the artifact query.\n\nSo the same two flags gave two answers from one tool:\n\n  prompt artifact --provider anthropic --model claude-haiku-4-5  -\u003e haiku45-2\n  prompt eval     --provider anthropic --model claude-haiku-4-5  -\u003e 1.0.0 (default)\n\nThe shipped variants are compiled into builtins.generated.ts, so haiku45-2 and sonnet5-3 -- the prompts production actually sends -- had never once been measured by the command built to measure them. Every recorded row measured the default while naming a provider and a model.\n\nNo error, ever: resolution returning undefined is indistinguishable from an operator deliberately choosing the default, and the stderr line said only 'Using the default prompt artifact.'\n\nFIXED alongside barwise-851 and barwise-842: one `artifactCandidates` module builds the set for both commands, and the fallback message now names the provider/model that matched nothing.","status":"closed","priority":0,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-25T00:15:00Z","updated_at":"2026-08-27T11:29:52Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-27T11:29:52Z","close_reason":"Fixed by artifactCandidates (cli/src/workspace/promptArtifacts.ts): prompt eval now resolves over builtinArtifacts plus any --artifacts dir, so the shipped variants are measurable. Per docs/specs/artifact-resolution-parity.spec.md"}
{"_type":"issue","id":"barwise-851","title":"prompt artifact double-loads the builtins when --artifacts points at the directory they are generated from","description":"`prompt artifact` built `[...builtinArtifacts, ...loadArtifactsFromDir(dir)]`. Point --artifacts at packages/llm/prompts/ -- the very files builtins.generated.ts is generated FROM -- and the resolver sees haiku45-2 twice and refuses:\n\n  Error: Ambiguous prompt artifacts ... version haiku45-2 (provider=anthropic, modelPrefix=claude-haiku); version haiku45-2 (provider=anthropic, modelPrefix=claude-haiku)\n\nA comment claimed order settled it -- 'built-ins first, then the directory, so a local variant can win against a shipped one of equal specificity rather than colliding with it'. resolveArtifact compares specificity and knows nothing of order, so the claim was never true.\n\nThis is the same defect as barwise-850 seen from the other side: the two commands built their candidate set differently, one ignoring the builtins and one double-counting them.\n\nFIXED: `artifactCandidates` de-duplicates by version with the directory winning. A directory entry sharing a builtin's version IS that builtin, edited. Two variants at equal specificity under DIFFERENT versions stay ambiguous and still throw.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-25T00:15:00Z","updated_at":"2026-08-27T11:29:52Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-27T11:29:52Z","close_reason":"Fixed by artifactCandidates: directory entries de-duplicate builtins by version (a local file sharing a builtin version IS that builtin, edited), so pointing --artifacts at packages/llm/prompts/ no longer refuses as ambiguous"}
{"_type":"issue","id":"barwise-852","title":"university-enrollment is bimodal, and it manufactures 88% of the suite's resolvable difference","description":"First 2.0.0 baseline, train split, haiku 4.5, repeat=5, default artifact:\n\n  university-enrollment  0.080  0.961  0.154  0.070  0.906   sd=0.457\n\nTwo clusters, not a spread. The rubric output names the mechanism: the model either invents `CourseOffering` or it does not, and when it does not, three requires_element and two forbids_population checks fail together as one correlated block -- five of six checks flipping on a single modelling decision.\n\nThe run reports '88% of that noise is university-enrollment alone'. Suite resolvable difference is 0.087. Removing that variance takes SD to about 0.35 of current and resolvable to roughly 0.030 -- a 3x gain in discriminating power from one case.\n\nThat matters because docs/specs/dspy-optimizer.spec.md concluded the MEASUREMENT was the binding constraint: resolvable ~0.10 against 0.13 of headroom on the shipped prompt. At 0.03 the optimizer has real room.\n\nNot yet diagnosed: whether the transcript underdetermines CourseOffering (an authoring bug, fix the transcript) or whether it is genuinely hard and the model is genuinely inconsistent (a real signal, keep it and raise repeat). Read the saved payloads for the three collapsed runs before deciding -- do not 'fix' a case that is correctly reporting a hard problem.\n\nNOTE: collapseFloor 0.3 is working exactly as designed here -- ok=2/5, quality=0.934+/-0.039 -- so it separates the two modes cleanly and does NOT need re-fitting on this evidence.","status":"closed","priority":1,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-25T00:15:00Z","updated_at":"2026-08-27T12:57:14Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/specs/eval-split-stratification.spec.md","closed_at":"2026-08-27T12:57:14Z","close_reason":"Workstreams 1-2 of docs/specs/eval-name-licensing.spec.md merged in PR #357: university-enrollment licenses CourseOffering/Offering, both check families resolve through the licence, suite bumped to 2.1.0. WS3 (alias-recording prompt compliance) stays open in the spec as provisional."}
{"_type":"issue","id":"barwise-853","title":"elementCount is recorded on CaseScore but nothing consumes it, so the denominator-inflation tripwire is unwired","description":"eval-split-stratification.spec.md names the risk that rating penalties leaves volume unpunished: a model inventing forty elements, half defective, now scores like one inventing four with two defects. The stated guard is that `elementCount` is recorded on CaseScore, so 'an arm inflating its denominator is visible in the record', with the tripwire 'if a candidate's mean elementCount rises materially and its score rises with it, the denominator is being gamed'.\n\nThe field exists. Nothing reads it. The one place that compares two arms is the optimizer's MetricLog, and `CaseScore.from_json` (optimizer/barwise_optimizer/barwise_cli.py:164) does not carry it -- explicit .get() lookups, so no break, just silence. The delta report has no denominator column.\n\nThe guard was specified, the field was added, and the consumer was never built. Same shape as the defects both eval specs were written to fix.\n\nFix: carry elementCount through CaseScore.from_json into MetricLog, report mean elementCount per arm in render_delta_report, and say so when it moves with the score.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-25T00:15:00Z","updated_at":"2026-09-06T13:32:44Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/specs/eval-split-stratification.spec.md","closed_at":"2026-09-06T13:32:44Z","close_reason":"Merged in PR #422 (main e53f6ab): CaseScore.element_count read from elementCount; MetricLog.mean_element_count -\u003e summary meanElementCount; export._denominator_lines warns on a \u003e=25% rise with a positive margin (ELEMENT_COUNT_RISE); tests in test_metric.py and test_export.py."}
{"_type":"issue","id":"barwise-854","title":"resolveArtifact does not rank a narrower modelPrefix above a broader one, and its error tells the operator to do what they already did","description":"`specificity()` scores which match FIELDS are present -- provider 1, modelPrefix 2 -- and never how specific a prefix is. So a variant matching `claude-haiku-4-5-2026` ties with one matching `claude-haiku`, and the pair is refused:\n\n  Error: Ambiguous prompt artifacts ... version haiku45-2 (modelPrefix=claude-haiku); version local-narrow-1 (modelPrefix=claude-haiku-4-5-2026). Narrow the match blocks so exactly one variant wins.\n\nThe advice is the one thing an operator in this position has already done. This is the first shape anyone testing a per-release variant against a shipped one will hit, and --artifacts exists precisely for that workflow.\n\nFound while writing tests for barwise-850/851; asserted in packages/cli/tests/workspace/promptArtifacts.test.ts as documented-not-endorsed so it is not rediscovered.\n\nFix options: rank by matched prefix LENGTH so the narrower wins (a longer prefix is strictly more specific and the tie is artificial); or leave the tie and rewrite the message to name the real remedy. The first is recommended -- the tie is not a question an operator can meaningfully answer.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-25T00:15:00Z","updated_at":"2026-09-06T13:32:44Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-09-06T13:32:44Z","close_reason":"Merged in PR #422 (main e53f6ab): resolveArtifact specificity is [fields, prefixLength] compared lexicographically; llm test 'ranks a longer modelPrefix above a shorter one' and the cli promptArtifacts test flipped from pinned refusal to resolution; optimizer smoke test and optimizer/CLAUDE.md updated for --artifacts selecting the candidate."}
{"_type":"issue","id":"barwise-855","title":"The review surface is artifact-driven but only half-reachable from the CLI","description":"Found auditing PR #338 for the defect it was fixing. barwise-847 wired reviewModel to the artifact seam; the CLI has caught up in one place and not the others.\n\nFIXED in #338: `prompt artifact --surface review` refused with 'extraction only'. The one command whose job is 'which prompt would this configuration send' could not answer for the surface that had just started having an answer. Note that an EXISTING TEST asserted the refusal was correct -- the gap was locked in by a passing test, which is worth remembering as a shape.\n\nSTILL OPEN, both the same shape:\n\n1. `prompt schema --surface review` refuses with 'no schema export yet (extraction only)'. buildReviewResponseSchema is private to reviewModel.ts and not exported from @barwise/llm. barwise-848 needs it -- scoreReview grades a payload against that schema's shape -- so export it and wire the command.\n\n2. `barwise review` has no `--artifacts` and never reports which artifact it used. Extraction can be pointed at a candidate directory and says what it resolved; review can do neither, so there is no way to try a review variant without editing packages/llm/prompts/ and rebuilding, and no way to tell from the output which prompt produced a given review.\n\nBoth are small. Filed rather than folded into #338 because (2) overlaps the review-eval workstreams (barwise-848/849), which will want the same override to gate a candidate.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-25T00:25:00Z","updated_at":"2026-08-27T11:29:53Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/specs/review-surface-evals.spec.md","closed_at":"2026-08-27T11:29:53Z","close_reason":"Item 1 shipped: buildReviewResponseSchema exported and prompt schema --surface review wired. Item 2 resolved differently than filed (artifact-resolution-parity.spec.md, option 2): production commands deliberately gain no --artifacts; barwise prompt run --surface review --artifacts sends a candidate once, and runs record promptHash so the artifact used is recoverable"}
{"_type":"issue","id":"barwise-856","title":"parseExtractionFromJson has no production caller, so the reference-generation rule is a procedure nobody automates","description":"Exported from @barwise/llm, exercised only by tests/TranscriptProcessor.test.ts. Nothing in src anywhere calls it.\n\nNot dead code -- a DOCUMENTED PROCESS WITH NO IMPLEMENTATION. packages/promptlab/CLAUDE.md states as a standing rule: 'References are generated, not hand-written. The seed references were produced by running the recorded payloads through parseExtractionFromJson and serializing -- they cannot drift from what the pipeline actually builds.' That rule is why the seven train references can be trusted, and nothing automates it, so the guarantee rests on whoever last did it by hand having done it right.\n\nLive work, not archaeology: barwise-845 requires generating references for the three dev cases and that IS this procedure. Whoever picks it up either writes the script or does it by hand and leaves the next person in the same position.\n\nFix: a script (scripts/regen-references.mjs, following scripts/regen-tutorial.mjs) or a CLI subcommand that takes a recorded payload and writes the .reference.orm.yaml. Then the standing rule has an implementation and a drift test can assert the references match what the pipeline builds today.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-25T00:45:00Z","updated_at":"2026-08-25T00:50:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/unwired-capability-audit-2026-08-25.md","closed_at":"2026-08-25T00:50:00Z","notes":"Implemented 2026-08-25. renderReference + withDeterministicIds in @barwise/promptlab, `npm run regen:references`, and tests/referenceDrift.test.ts.\n\nTHE RULE HAD ALREADY BEEN BROKEN. The regenerator's first run found freight-corrections missing a `sample: true` the parser has emitted since the sample-populations work. Nothing caught it because a reference is consumed by forbids_population, which asks about constraints and never reads populations -- so the stale field changed no score and produced no symptom. That is the argument for the guard in one sentence: the drift was real, silent, and had been there for an unknown length of time.\n\nThe rendering lives in promptlab src rather than in the script so the drift test and the script cannot disagree -- two copies that diverge would give a passing test over a file the script writes differently, which is the failure mode the test exists to prevent, one level up.\n\nwithDeterministicIds restores the previous generator in a finally: setIdGenerator is process-wide, and a leak would leave every later test in the worker minting fixture ids while still passing.\n\nMutation-checked, one-for-one: dropping the finally-restore, making ids non-deterministic, and reverting freight-corrections to its drifted state each fail exactly the test that should catch them.\n\nRegenerating changed ids across all seven references (now 00000000-0000-70NN-..., obviously synthetic). Inert: all 168 promptlab tests pass, including the pinned answer-key scores.\n\nUNBLOCKS barwise-845: the three dev cases need exactly this procedure, and the script names them as the cases still lacking a payload."}
{"_type":"issue","id":"barwise-857","title":"computeLayoutMetrics is exported, tested, and computed by nothing","description":"Exported from @barwise/diagram, exercised only by tests/layout/metrics.test.ts. Reports nodeOverlapCount and the other layout quality measures to no caller.\n\nSame shape as the eval metric before promptlab existed: a measurement built without the thing that reads it. Related to docs/specs/diagram-layout-aesthetics.spec.md.\n\nEither wire it (a diagram quality report, or a layout regression gate that fails when overlap count rises) or delete it. Deleting is a legitimate answer and cheaper than the third rediscovery -- what is NOT acceptable is leaving it exported so the next audit finds it again.","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-25T00:45:00Z","updated_at":"2026-09-06T13:32:44Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/unwired-capability-audit-2026-08-25.md","closed_at":"2026-09-06T13:32:44Z","close_reason":"Merged in PR #422 (main e53f6ab): computeLayoutMetrics / LayoutMetrics removed from packages/diagram/src/index.ts; the corpus tolerance test stays as the internal consumer; note in diagram-layout-aesthetics.spec.md."}
{"_type":"issue","id":"barwise-858","title":"ParseLevel declares an \"llm\" tier that is never produced, in the one package that cannot produce it","description":"packages/core/src/sql/types.ts:\n\n  export type ParseLevel = \"sqlglot\" | \"regex\" | \"llm\";\n\n'llm' is never produced by any code and never branched on anywhere. The module docstring describes 'the SQL parsing cascade (sqlglot sidecar -\u003e regex -\u003e LLM fallback)' -- a three-tier cascade with two tiers implemented.\n\nWorse than merely unused: this type lives in @barwise/core, where the determinism rule forbids exactly the thing the member describes. Core could never produce it. So the docstring reads as a description of the system and is a description of an intention.\n\nFix: drop the member and correct the docstring, or state where the LLM tier would live if it is still wanted -- one layer out, in a connector package, per the same rule that keeps the sqlglot subprocess in @barwise/formats rather than core.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-25T00:45:00Z","updated_at":"2026-09-06T13:32:44Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/unwired-capability-audit-2026-08-25.md","closed_at":"2026-09-06T13:32:44Z","close_reason":"Merged in PR #422 (main e53f6ab): ParseLevel in packages/core/src/sql/types.ts is now \"sqlglot\" | \"regex\"; SqlCascadeParser header no longer promises an LLM tier (ImportFormat.enrich is the seam)."}
{"_type":"issue","id":"barwise-859","title":"Review the test suite for tests that assert the wrong behaviour is correct","description":"Requested 2026-08-25. A new audit axis, and the one the unwired-capability sweep is least able to close.\n\nPR #338 fixed `prompt artifact` refusing --surface review, wrong since barwise-847 made review artifact-driven. The gap survived a green suite because a test ASSERTED THE REFUSAL WAS CORRECT:\n\n  it('rejects a surface it cannot print', ...)\n    expect(stderr).toContain('extraction only');\n\nThat is worse than an untested gap. An untested gap is invisible; this one was visible, described, and pinned as a requirement, so any reviewer reading it would conclude the behaviour was intended.\n\nWHAT WORKS (tried in the 2026-08-25 audit): grep expected error strings for 'yet', 'only', 'not supported', 'unsupported'. 'Yet' is the strongest tell -- it means temporary, and a test pinning a temporary state is the shape. Four hits, one real (prompt schema --surface review, barwise-855). THE DISCRIMINATOR is whether src PRODUCES the string or the test SUPPLIES it: three of four hits were the test feeding its own fixture text.\n\nWHAT THAT MISSES, and why this needs a real pass rather than a grep: it only catches tests pinning an ERROR. It cannot catch a test pinning a wrong VALUE, a wrong fallback, or an assertion that passes for the wrong reason. barwise-840 was the second kind -- mutating CaseScore.floored to always return false was caught by NOTHING, because the report tests supplied the count as a literal and so asserted the consumer and never the computation.\n\nMethod for the real pass: sample-mutate the PRODUCER of any value a test feeds in by hand, and audit every test whose name begins 'rejects', 'refuses', 'throws' or 'does not' for whether the refusal is a requirement or a limitation. Prioritise packages where a capability seam exists: llm (artifacts, surfaces), cli (commands, flags), promptlab (scorer), core (format registry).","status":"closed","priority":1,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-25T00:45:00Z","updated_at":"2026-08-25T19:05:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/unwired-capability-audit-2026-08-25.md","closed_at":"2026-08-25T19:05:00Z","assignee":"Claude","close_reason":"Audit complete: barwise/docs/test-suite-assertion-audit-2026-08-25.md (PR #341). Both passes run over main at 24663b1 -- Pass 1 (refusal vocabulary grep + name audit over all 254 rejection-named tests), Pass 2 (mutation testing at the llm/promptlab/cli/mcp capability seams). Four findings, all fixed in that PR: (1) `prompt schema --surface review` refused a surface with a real schema (barwise-855 shape, fixed by exporting buildReviewResponseSchema); (2) --max-tokens/--context-window were wired but unassert-able, fixed with a request-body assertion in promptEvalOffline.test.ts; (3) MCP tool registration was pinned for 4 of 17 tools, fixed by asserting the full sorted list with toEqual in serverSpawn.test.ts; (4) llm-usage and the call-log emitter agreed only by convention, fixed with a withCallLog round-trip test. Re-verified 2026-08-25: all four fixes present on current main, full monorepo build+test green (24 turbo tasks, core 1403/1403, cli 237/237).\n\nWatch items filed as follow-ups since the audit's session lacked `bd`: barwise-860 (VS Code ToolRegistration has no unit pin, same shape as Finding 3), barwise-861 (runSuite's review-artifact refusal is correct today but becomes the next Finding 1 once review-surface-evals.spec.md workstreams 2-4 route review prompts through runSuite -- confirmed still open on re-check). The stale fixture string in OrmYamlAnnotator.test.ts (frequency constraints not yet supported) is not a defect -- fixture-supplied, not src-produced -- and needs no issue.\n\nThe assertion-audit skill (.claude/skills/assertion-audit/SKILL.md) was added in the same PR so the method and authoring rules are reusable for the next audit."}
{"_type":"issue","id":"barwise-860","title":"VS Code ToolRegistration has no unit pin on the registered-tool list","description":"Watch item from the 2026-08-25 test-suite assertion audit (barwise/docs/test-suite-assertion-audit-2026-08-25.md, barwise-859), filed now because the prior session's container had no `bd`.\n\n`packages/vscode/src/mcp/ToolRegistration.ts` is exercised only by the integration harness; nothing in a fast unit test fails if a registration is dropped. This is the same shape as Finding 3 of the audit (MCP's `registerReviewTool`/`registerMergeTool` were unregistered and 179 tests stayed green because `serverSpawn.test.ts` sampled only 4 of 17 tool names) -- the fix there was pinning the complete sorted tool list with `toEqual`.\n\nNot mutation-tested in the original audit (the vscode integration harness was not runnable in that pass); still true as of this filing (no test file under packages/vscode/tests references ToolRegistration).\n\nWork: add a fast unit test asserting the complete, sorted list of tool names ToolRegistration registers, mirroring the pattern in packages/mcp/tests/serverSpawn.test.ts.","status":"open","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-25T19:05:00Z","updated_at":"2026-08-25T19:05:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/test-suite-assertion-audit-2026-08-25.md","dependencies":[{"issue_id":"barwise-860","depends_on_id":"barwise-859","type":"discovered-from","created_at":"2026-08-25T19:05:00Z","created_by":"claude","metadata":"{}"}]}
{"_type":"issue","id":"barwise-861","title":"runSuite's review-artifact refusal becomes a pinned-limitation risk once review evals land","description":"Watch item from the 2026-08-25 test-suite assertion audit (barwise/docs/test-suite-assertion-audit-2026-08-25.md, barwise-859), filed now because the prior session's container had no `bd`.\n\n`packages/promptlab/src/run/runSuite.ts:337` throws `Prompt artifact surface \"...\" cannot drive transcript extraction.`, pinned at `packages/promptlab/tests/runSuite.test.ts:226`. This is a correct requirement today (confirmed still true 2026-08-25: workstreams 2-4 of docs/specs/review-surface-evals.spec.md, which would route review prompts through this same shape, are still open) -- the suite's cases are transcripts scored as extractions, and a review prompt genuinely cannot drive that pipeline yet.\n\nBut if workstreams 2-4 route review evals through `runSuite`, this test becomes the next Finding 1 (the barwise-855 shape): a refusal wording per-surface, not per-capability, defended by a test that no longer describes a limitation.\n\nWork: no code change now. Whoever wires review evals into `runSuite` (review-surface-evals.spec.md workstream 2 or later) must treat runSuite.test.ts:226 as theirs to change, not as intent to preserve -- grep for this refusal string before shipping that workstream and delete or rewrite the test in the same change, per assertion-audit rule 6.","status":"open","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-25T19:05:00Z","updated_at":"2026-08-25T19:05:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/test-suite-assertion-audit-2026-08-25.md","dependencies":[{"issue_id":"barwise-861","depends_on_id":"barwise-859","type":"discovered-from","created_at":"2026-08-25T19:05:00Z","created_by":"claude","metadata":"{}"}]}
{"_type":"issue","id":"barwise-862","title":"Dependency gate cannot see @barwise/* subpath imports; graph copies omit diagram-ui","description":"tsconfig.depcruise.json maps '@barwise/*' with a wildcard that swallows subpath specifiers, so @barwise/diagram-ui/server and every other subpath import resolves to a nonexistent path and dependency-cruiser silently DROPS the edge rather than reporting couldNotResolve. Verified: the depcruise JSON for packages/cli/src/commands/diagram.ts lists three dependencies; the file's imports of @barwise/core/annotation and @barwise/diagram-ui/server are absent. 80 subpath imports across the workspace are invisible to the gate, and packages/core/CLAUDE.md MANDATES subpath imports, so the documented house style is exactly what the gate cannot see.\n\nLive consequence: the root CLAUDE.md dependency graph (cli and mcp rows) and .dependency-cruiser.cjs (a hand-copied third instance of the graph) both omit diagram-ui, while both packages declare it in package.json and import it (cli/src/commands/diagram.ts:10, mcp/src/tools/diagram.ts:7). npm run depcruise passes clean today. Also stale: .dependency-cruiser.cjs:2 cites docs/specs/architecture-analysis.spec.md, now under docs/specs/archive/.\n\nFix per spec workstream 1: per-subpath paths entries (or a strip-subpath resolver), correct both graph copies, fix the archived spec path, and add a hostile-import probe proving the gate now reports a forbidden subpath edge. Audit finding A1 -- do this first; every other parity claim's verification story sits on this gate.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-26T23:35:00Z","updated_at":"2026-08-26T23:12:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/specs/duplication-drift-guards.spec.md (workstream 1); barwise/docs/logic-duplication-audit-2026-08-26.md (A1)","closed_at":"2026-08-26T23:12:00Z","close_reason":"Fixed in PR #350. Per-subpath paths entries in tsconfig.depcruise.json (1236 to 1319 edges cruised); diagram-ui added to the cli/mcp rows in .dependency-cruiser.cjs INTENDED and the root CLAUDE.md graph; stale spec path corrected. scripts/check-depcruise-gate.mjs (npm run check:depcruise-gate, in CI next to depcruise) guards the gate: derives expected entries from package.json exports and probes that a forbidden subpath import is reported; both failure modes mutation-verified. No other graph violations were hiding among the 83 revealed edges."}
{"_type":"issue","id":"barwise-863","title":"Default extraction prompt has diverged from the YAML variants it is the fallback for","description":"EXTRACTION_INSTRUCTIONS in packages/llm/src/prompt/systemPrompt.ts (the default artifact, a TS literal unlike every YAML-authored variant) no longer agrees with packages/llm/prompts/extraction.sonnet5.prompt.yaml. Verified missing from the default: the every-role instance rule ('Every instance MUST supply a value for EVERY role...'), the enumerated-list-is-a-value-constraint rule, and the frequency-siding paragraph (systemPrompt.ts:108 truncates at 'exactly 1 entry.'; the variant continues with the Shipment/Warehouse siding rule).\n\nselectArtifact falls back to defaultExtractionArtifact for any client with no matching variant, so this is the LIVE prompt for OpenAI, Ollama, and unmatched Anthropic models -- those runs are instructed without rules the conformance layer downstream enforces, and nothing reports it. The golden test pins the default's bytes against a frozen fixture: it asserts the default never changes, not that it agrees with anything.\n\nThis is a decision, not just a sync: either the default becomes YAML-authored and regenerated like every variant (removing the hand-maintained copy), or the divergence is intended model-tiering and must be stated in packages/llm/CLAUDE.md. Audit finding B1.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-26T23:35:00Z","updated_at":"2026-08-27T01:01:53Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/specs/extraction-default-parity.spec.md; barwise/docs/logic-duplication-audit-2026-08-26.md (B1)","closed_at":"2026-08-27T01:01:53Z","close_reason":"Fixed in PR #353, implementing docs/specs/extraction-default-parity.spec.md (both workstreams). W1: both surface defaults re-homed to matchless prompts/*.default.prompt.yaml compiled in by regen:builtins; the hand-maintained TS literals deleted; byte-identical rendering proven by the golden tests passing unmodified. W2: the extraction default regained the every-role instance rule, the enumerated-list-is-a-value-constraint rule, the full frequency-siding paragraph, and the variants' shared Populations intro, at artifact version 1.1.0 with the two extraction goldens updated as the change marker. Live prompt change for OpenAI/Ollama/unmatched-Anthropic extraction runs, visible via promptHash."}
{"_type":"issue","id":"barwise-864","title":"dbt import mines compiled SQL: the findSqlFiles walk descends into target/ and logs/","description":"packages/dbt/src/DbtImportFormat.ts:53-59 skips node_modules, .git, target, dbt_packages, logs. packages/dbt/src/DbtSqlCompiler.ts findSqlFiles (approx :190) skips only node_modules, .git, dbt_packages -- so the .sql walk of the same project root during the same import descends into target/, where dbt writes its COMPILED sql, and into logs/. Compiled artifacts get mined as if they were sources. A third sibling list in packages/formats/src/sql/SqlImportFormat.ts:80-84 differs again (defensible for a generic SQL tree, but part of the same unshared decision).\n\nShare one skip-list within the dbt package (same package, no orthogonality cost) and add the target/ regression fixture. Audit finding B3.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-26T23:35:00Z","updated_at":"2026-08-26T23:36:48Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/logic-duplication-audit-2026-08-26.md (B3)","closed_at":"2026-08-26T23:36:48Z","close_reason":"Fixed in PR #352. One shared walker (packages/dbt/src/projectWalk.ts) owns the never-source skip-list (node_modules, .git, target, dbt_packages, logs) for both the SQL and YAML walks; regression test pins that a project without models/ yields only root-level models, nothing from target/ or logs/."}
{"_type":"issue","id":"barwise-865","title":"SQL type to conceptual type has three mappings; TIMESTAMP already disagrees between the live two","description":"packages/formats/src/ddl/DdlImportFormat.ts:485-525 (regex chain, live) maps TIMESTAMP to 'datetime'; packages/dbt/src/dbtMapping/naming.ts:11-41 (SQL_TYPE_MAP, live) maps timestamp to 'timestamp'. The same column imported through the DDL path and the dbt path yields a different data_type in the .orm.yaml and therefore different re-exported DDL. Further spread: DdlImportFormat matches CHARACTER and maps SERIAL/AUTOINCREMENT/IDENTITY to auto_counter; naming.ts alone knows 'character varying' and the timestamp_ntz/ltz/tz family.\n\npackages/formats/src/sql/SqlImportFormat.ts:99-114 _mapSqlType is a third copy with ZERO references in src or tests -- dead, but it reads as a live third opinion for the next person adding a SQL type; delete it regardless of the main fix. Remedy: one mapping (per-dialect where needed) consumed by both connector packages, or a parity test over the shared subset. Audit finding B2.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-26T23:35:00Z","updated_at":"2026-08-27T11:29:44Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/logic-duplication-audit-2026-08-26.md (B2)","closed_at":"2026-08-27T11:29:44Z","close_reason":"Merged to main in PR #355 (e7b36b1): one SQL-type-to-conceptual mapping in core; TIMESTAMP maps to timestamp"}
{"_type":"issue","id":"barwise-866","title":"query and describe answer constraint-keyword matching differently, inside core","description":"packages/core/src/query/evaluate.ts:502-505 matchesConstraintType answers with an open substring rule; packages/core/src/describe/describers.ts (approx :257-290) answers with a closed whitelist of hard-coded keyword checks. query_model and describe_domain disagree for any keyword outside the whitelist -- one matches, the other silently matches nothing. Same package, same conceptual vocabulary, no shared function, no test comparing them. Share one matcher in core (no coupling cost). Audit finding B4.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-26T23:35:00Z","updated_at":"2026-08-27T11:29:44Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/logic-duplication-audit-2026-08-26.md (B4)","closed_at":"2026-08-27T11:29:44Z","close_reason":"Merged to main in PR #355 (50d7684): one constraint-keyword matcher shared by query and describe"}
{"_type":"issue","id":"barwise-867","title":"Derive the surface format lists from the registry; add extension to FormatDescriptor; fix dangling list_formats","description":"The format registry already knows the answers; each surface restates them by hand and has drifted:\n- packages/cli/src/commands/import/model.ts:12 (help) and :35 (error) advertise 'ddl, openapi, norma' while the call site resolves through getImporter, whose registry holds 8+; barwise import model --format dbt works today, undiscoverable, and the failure text lies. The same list is restated in mcp/src/tools/importModel.ts (z.enum + prose + error), vscode package.json enum, and a vscode TS union -- seven statements total.\n- Export extension is answered three ways: mcp extensionForFormat maps ddl to .sql; the CLI project export uses the format NAME as the extension (.ddl, .openapi); vscode commands hardcode .sql/.avsc. FormatDescriptor has no extension field, so there is no shared answer to consult.\n- mcp/src/tools/exportModel.ts:41 and :107 direct callers to a list_formats tool that is NOT registered anywhere -- an agent following the error calls a tool that does not exist.\n- vscode package.json closes the export enum at [ddl, openapi, dbt, avro], omitting norma, which the registry serves to CLI and MCP.\n- vscode Export{Ddl,Avro,Dbt}Commands bypass the registry and call core renderers directly: no dialect routing, no strict mode, no annotations, no population INSERTs -- same menu label as the CLI, materially different SQL. Possibly deliberate; nothing records it. Decide.\nRemedy: export runtime arrays / derive enums and help from listImporters()/listExporters(), add extension to FormatDescriptor, register or un-reference list_formats. Audit findings B5, B6; also covers the SqlDialect and provider-name restatement fans (audit class D).","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-26T23:35:00Z","updated_at":"2026-08-27T11:29:44Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/logic-duplication-audit-2026-08-26.md (B5, B6, D)","closed_at":"2026-08-27T11:29:44Z","close_reason":"Merged to main in PR #355 (ca7c801): surface format/provider/dialect lists derived from their owners"}
{"_type":"issue","id":"barwise-868","title":"Implement the parity manifest and check:parity (drift-guards spec workstream 2)","description":"parity.manifest.json + scripts/check-parity.mjs + CI wiring. Whole-file members compare by normalized text; symbol members are extracted with the TypeScript compiler API and compared by printed AST text; an unresolvable member is an error (a stale manifest is itself drift). Seed with the audit's class C sets that are byte-identical today: the three surface idGenerators, the SqlglotBridge pair (leading-docblock ignored), quoteIdent, both toPascalCase pairs, summarizeDiff, the analyze extractModel pair, the diff delta projection. formatReview pends the spec's open decision 2 (share vs check). Replace the two 'Must match' comments with manifest pointers. No source-logic changes in this workstream. Acceptance criteria in the spec.","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-26T23:35:00Z","updated_at":"2026-08-27T11:29:44Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/specs/duplication-drift-guards.spec.md (workstream 2)","closed_at":"2026-08-27T11:29:44Z","close_reason":"Merged to main in PR #355 (cd8d476): parity manifest and check:parity implemented"}
{"_type":"issue","id":"barwise-869","title":"assertNever in core; guard the vocabulary unions (drift-guards spec workstream 3)","description":"The repo has no assertNever helper and zero uses of satisfies; the only live exhaustiveness mechanisms are Record literals and TS2366, which cannot fire in void switch contexts. Export assertNever from @barwise/core and apply it (or a typed Record / derivation) where exhaustive handling is the intent: the ring validation switch (rules/population/ring.ts), CounterexampleGenerator, NormaXmlParser.normalizeRingType (REJECT an unrecognized ring type instead of coercing to irreflexive -- that is a silent semantic rewrite and a behavior change needing a fixture), derive the InferredConstraintType ALL array and promptlab's gym-check-kind array from Record(Union, true) so completeness is type-checked, drop conceptualTypeToSql's unreachable TEXT default. Partial-on-purpose handlers (ModelToGraph draws 6 of 16 constraint kinds; lineage labels 11 of 16) are the spec's open decision 1, not silent conversions. Site list is provisional -- re-ground each switch's intent during implementation. Audit class D.","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-26T23:35:00Z","updated_at":"2026-08-27T11:29:44Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/specs/duplication-drift-guards.spec.md (workstream 3); barwise/docs/logic-duplication-audit-2026-08-26.md (D)","closed_at":"2026-08-27T11:29:44Z","close_reason":"Merged to main in PR #355 (369f041): assertNever and derived member lists guard the vocabulary unions"}
{"_type":"issue","id":"barwise-870","title":"One regenerator and a real drift test for examples/output (drift-guards spec workstream 4)","description":"Two regenerators exist and disagree: scripts/regen-example-output.sh (npm run regen:examples) prepends version headers; packages/llm/tests/Pipeline.integration.test.ts under UPDATE_GOLDEN=1 writes the same files with no header and a different diagnostics format. The committed files are the vitest form, so the DOCUMENTED command produces a diff on a clean tree by construction, and examples/README.md's version-stamp claim is false for every committed file. No test reads these files back, yet four sites cite the drift test as existing (builtins.test.ts:7, learn tutorial drift.test.ts:4, regen-builtin-artifacts.mjs:18, modeling-tutorial.spec.md:202) -- the precedent everyone copies points at the one case that never implemented it. Collapse to one writer, add the byte-compare drift test, fix the README claim and the four citations. Audit finding A2.","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-26T23:35:00Z","updated_at":"2026-08-27T11:29:44Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/specs/duplication-drift-guards.spec.md (workstream 4); barwise/docs/logic-duplication-audit-2026-08-26.md (A2)","closed_at":"2026-08-27T11:29:44Z","close_reason":"Merged to main in PR #355 (6a109ac): one regenerator and a drift test for examples/output"}
{"_type":"issue","id":"barwise-871","title":"audit:duplication detection script and the baseline ratchet (drift-guards spec workstream 5)","description":"scripts/audit-duplication.mjs: the sweep's deterministic detection half -- cross-file duplicate string-literal extraction, union-restatement candidates with their guard shapes, the parity-claim grep vocabulary over docs and CLAUDE.md files, the generated-file inventory with regeneration script and guard-test presence, and the jscpd run with pinned flags. Same tree, same bytes; it assigns no verdicts. audit-baseline.json holds every known candidate, stably keyed (file plus symbol or content hash, never line numbers), each carrying its verdict: accepted-benign, or tracked plus the bd issue id. --check diffs report against baseline and fails both directions (new unclassified candidate; stale entry for a resolved finding), so the tracked entries always enumerate exactly the detectable findings still open. Cadence and detector scope for CI is the spec's open decision 5 (recommended: crisp detectors per PR, full sweep on a schedule). Also shrink the duplication-audit skill's method section to the judgment layer once this lands.","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-26T23:35:00Z","updated_at":"2026-08-27T11:29:44Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/specs/duplication-drift-guards.spec.md (workstream 5)","closed_at":"2026-08-27T11:29:44Z","close_reason":"Merged to main in PR #355 (90e611c): audit:duplication detection script and baseline ratchet"}
{"_type":"issue","id":"barwise-872","title":"Write the must-agree rule into CLAUDE.md conventions and the spec-writer gate (drift-guards spec workstream 6)","description":"Add the invariant to root CLAUDE.md Conventions -- a copy that must agree with other code is shared, derived, registered in parity.manifest.json, or drift-tested in the same commit; a 'must match' comment is not a check -- and a matching item to the spec-writer pre-push design gate. Point both at the duplication-audit skill. Last of the spec's workstreams because it cites the mechanisms the others create.","status":"closed","priority":3,"issue_type":"chore","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-26T23:35:00Z","updated_at":"2026-08-27T11:29:44Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/specs/duplication-drift-guards.spec.md (workstream 6)","closed_at":"2026-08-27T11:29:44Z","close_reason":"Merged to main in PR #355 (934da35): must-agree rule written into CLAUDE.md conventions and the spec-writer gate"}
{"_type":"issue","id":"barwise-873","title":"Doc parity cleanup: AGENTS.md duplicate, ARCHITECTURE.md, aggregator CLAUDE.md tables, promptlab history.jsonl, MCP.md/CLI.md","description":"The prose-restates-code findings from the audit (B8), cheapest first:\n- AGENTS.md holds two copies of the session-completion procedure that disagree (bd sync at :38 vs bd dolt push at :85 and in root CLAUDE.md); the hand-written half sits above a generated beads block that will be overwritten, so the divergence is structurally permanent -- delete the hand-maintained duplicate.\n- packages/promptlab/CLAUDE.md:37 lists history.jsonl as a checked-in record; docs/local-eval-runbook.md:17 states outright it does not exist. Fix the CLAUDE.md.\n- docs/ARCHITECTURE.md is mandated reading (root CLAUDE.md) yet describes a three-package repo with NORMA compatibility as a non-goal -- NORMA is a shipped bidirectional format. Re-date it as a historical design record and soften the pointer, or rewrite its structural sections.\n- The aggregator CLAUDE.md files (cli, mcp, vscode, llm) understate dependency lists (3 stated vs 8-9 actual), list roughly half their command/tool files, and vscode/CLAUDE.md documents command ids under an orm. prefix the extension abandoned. Root CLAUDE.md:114 carries a fifth hand-maintained CLI command list (9 of 19), contradicting the capability matrix below it. Generate the three aggregator dependency tables from package.json with a drift test (spec open decision 3) and correct the rest by hand.\n- docs/MCP.md documents 8 of 17 registered tools; docs/CLI.md documents 12 of 19 commands. Complete or trim.\nLeaf-package CLAUDE.md files verified accurate; the rot concentrates in the aggregators.","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_by":"claude","created_at":"2026-08-26T23:35:00Z","updated_at":"2026-08-27T11:29:44Z","dependency_count":0,"dependent_count":0,"comment_count":0,"design":"barwise/docs/logic-duplication-audit-2026-08-26.md (B8)","closed_at":"2026-08-27T11:29:44Z","close_reason":"Merged to main in PR #355 (b0729f7): prose restating code reconciled with the current tree"}
{"_type":"issue","id":"barwise-874","title":"Revisit blocks-only dependency_count/dependent_count once conditional-blocks or waits-for appears","description":"beads-crud.mjs (barwise/scripts/beads-crud.mjs, spec: barwise/docs/specs/beads-issue-crud-scripts.spec.md) infers dependency_count/dependent_count as counting only type:blocks edges -- verified against every non-zero example in the live issues.jsonl at spec time (36 blocks-typed examples, zero exceptions), but conditional-blocks and waits-for (both in bd's DEPTYPE vocabulary per check-beads.sh) never appear in this file's history so the rule is unconfirmed for them. Revisit once one shows up: check whether the real bd binary counts it toward these fields, and update blocksDepCount() in beads-crud.mjs if not.","status":"open","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-26T22:56:15Z","created_by":"claude","updated_at":"2026-08-26T22:56:15Z","design":"barwise/docs/specs/beads-issue-crud-scripts.spec.md","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-875","title":"Residual small cross-surface twins: analyze extractModel pair, counterexample renderer pair, extraction tool-prompt fragment","description":"Small byte-near-identical pairs left after the parity manifest seeding (barwise-868), each excluded there for a concrete reason and tracked via audit-baseline.json:\n- extractModel duplicated between cli/src/commands/analyze.ts and mcp/src/tools/analyze.ts: identical logic, but the guard message deliberately diverges in its surface-specific remediation clause, so byte-parity cannot hold; the right fix is sharing it (likely into @barwise/code-analysis, which owns the domain) with the remediation hint as a parameter. Its shared strings ('no deterministic import format detected...', 'Model name for the extracted domain', 'Branch, tag, or commit to analyze') show up in audit:duplication.\n- Counterexample renderer pair: cli/src/workspace/format.ts formatCounterexamples vs the inline block in mcp/src/tools/verbalize.ts (same header 'Counterexamples (what the constraints rule out):', two-space indent; differ by blank lines). Share or align and register.\n- The extraction tool-use prompt fragment 'Extract a structured ORM model from the transcript analysis.' duplicated between llm/src/providers/anthropic.ts and vscode/src/llm/CopilotLlmClient.ts: a prompt fragment two providers must agree on; move to a shared constant in @barwise/llm (vscode already depends on llm).","status":"open","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-27T03:47:45Z","created_by":"claude","updated_at":"2026-08-27T03:47:45Z","design":"barwise/docs/logic-duplication-audit-2026-08-26.md (C8, C12)","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-876","title":"PR steward skill: repo-specific guidance for agents driving barwise PRs","description":"A thin .claude/skills/steward/SKILL.md that a PR-driving agent reads before acting on CI or review events. Carries only the repo-specific mechanics the generic drive-to-green rules cannot know: resolving .beads/issues.jsonl conflicts via beads-crud + check:beads (never hand-editing markers), fixing generated files with their regenerators (regen:builtins, regen-example-output.sh, UPDATE_GOLDEN) never by hand, the audit:duplication ratchet firing on your own change and its remedy, the local pre-push gate in CI order plus the stale-dist trap, and the tracker-closure-after-merge convention. Points at CLAUDE.md/AGENTS.md/scripts as authorities rather than restating them (must-agree prose rule). Spec: docs/specs/steward-skill.spec.md","status":"closed","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-27T11:11:06Z","created_by":"claude","updated_at":"2026-08-27T11:29:44Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-27T11:29:44Z","close_reason":"Landed on main in 5f27a87: .claude/skills/steward/ added per docs/specs/steward-skill.spec.md"}
{"_type":"issue","id":"barwise-877","title":"Multi-sample transcript import: run-to-run disagreement becomes declared ambiguity","description":"barwise import transcript --samples N (and an MCP samples param): extract N times, correspond the samples with core diffModels, output the medoid sample, and report every element the samples disagree on as an ambiguity with counts. Explicitly NOT best-of-N by score: production has no rubric, and the penalty-only signal prefers sparser models (the 2.0.0 university-enrollment collapse was penalty-clean). Spec: docs/specs/multi-sample-import.spec.md","status":"closed","priority":2,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-08-27T12:21:41Z","created_by":"claude","updated_at":"2026-08-27T15:36:10Z","design":"barwise/docs/specs/multi-sample-import.spec.md","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-27T15:36:10Z","close_reason":"Implemented per docs/specs/multi-sample-import.spec.md (all three workstreams), landing via PR #364: computeSampleAgreement (pure fold over diffModels, medoid + presence/shape/naming disagreements as ambiguities), sampleTranscript (sequential draws, failure exclusion, shared correlation), barwise import transcript --samples, MCP import_transcript samples, capability matrix row. Open decisions resolved as recommended; explicitly not best-of-N by score."}
{"_type":"issue","id":"barwise-878","title":"Audit the reference models against their transcripts; add rubric checks for settled-but-untested constraints","description":"References are generated from recorded LLM payloads (barwise-856), so they bless the recorded extraction's choices; the rubric only tests the constraints its author named. Known coverage gap from the 2.0.0 eval doc: university-enrollment's grade attachment (reference has the ternary Student receives LetterGrade for CourseOffering; the 1.000 run objectified Enrollment instead) is a real structural fork no check sees. Verbalize each committed reference, read it against its transcript, and add forbids_population/requires_element checks for every constraint the transcript settles that no check currently guards. Fold the same verbalize-and-read step into the split-spec WS2 procedure when pinning the three dev references (barwise-845). Suite bump per the 2.1.0 precedent if any rubric changes.","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-27T12:21:41Z","created_by":"claude","updated_at":"2026-08-27T13:53:12Z","design":"barwise/docs/adr/0002-constraint-coverage-policy.md (the policy); barwise/docs/specs/constraint-extraction-coverage.spec.md (the living inventory and closing workstreams); barwise/docs/specs/eval-split-stratification.spec.md (WS2, where the dev references land)","dependency_count":0,"dependent_count":0,"comment_count":0,"notes":"2026-08-27, sharpened by barwise-863: PR #353 restored the Ring, Subset, and Equality instruction blocks to the DEFAULT extraction prompt (the live prompt for OpenAI, Ollama, and unmatched Anthropic models; the YAML variants already carried them). References inherit the capability envelope of the prompt that produced their payloads, so audit each transcript specifically for settled ring/subset/equality rules the recorded payloads may have missed. Current state: only project-staffing's reference carries a ring (acyclic mentoring) and its rubric tests it via forbids_population, so the check machinery is proven; the other six references carry none. Sequence rubric additions BEFORE the split-spec WS4 paid re-baseline so one suite bump and one baseline cover both -- the re-baseline also newly measures the post-863 default (its promptHash changed).\n\n2026-08-27 inventory (first audit pass, executed in-session): across the ten transcripts, seven references, seven payloads, and the one gym exercise --\n- Ring: ONE instance total. acyclic on project-staffing (transcript settles it, reference carries it, rubric tests it). incident-response's transcript explicitly settles IRREFLEXIVE ('an incident cannot be a duplicate of itself', stated twice plus the facilitator's summary) but it is a dev case with no reference yet -- when barwise-845 pins it, check whether the recorded payload captured the ring; if not, that is prompt-headroom evidence, and add the forbids_population ring check either way. The other six ring types (asymmetric, antisymmetric, intransitive, symmetric, transitive, purely_reflexive): ZERO representation anywhere.\n- Set-comparison (subset/equality/exclusion): ZERO everywhere -- no transcript settles one, no reference carries one, no payload emitted one. And forbids_population cannot test them: ConstraintKind is {internal_uniqueness, mandatory, value, frequency, ring} (learn CLAUDE.md states set-comparison unsupported). Closing subset coverage needs data AND either a check-kind extension or requires_verbalization as the interim check.\n- Machinery envelope exceeds evidence envelope: schema carries all 11 inferred types, the parser maps subset/equality, population validation covers ring and setComparison, and CounterexampleGenerator derives counterexamples for ALL EIGHT ring types -- so a reference carrying any ring type is immediately testable.\n- Coverage target (comparative instrument, not certification): 2 instances per common type -- one train, one dev, different domains, different phrasing (irreflexive, acyclic, symmetric, subset, exclusion). Rare types (antisymmetric, intransitive, transitive, purely_reflexive, equality) belong in a didactic gym exercise, not in realism-governed eval transcripts. Write the common-tier budget into the split-spec WS3 authoring brief so new instances land in transcripts being authored anyway.\n\n2026-08-27, audit EXECUTED for the seven train cases (docs/reference-rubric-audit-2026-08-27.md): ten checks added across five rubrics, one misdescribing hint corrected, suite bumped to 2.2.0, every answer key verified at 1.000 offline. Findings filed: barwise-879 (payload defects F1-F4, incl. employee-hierarchy asserting an unsettled constraint while missing the settled one), barwise-880 (forMandatory anchor gap, two settled mandatories untestable), barwise-881 (objectification-blind correspondence, the grade rule stays unguarded on purpose). Ring/subset lens: no new instances in the train transcripts; common-tier coverage must come from split-spec WS3 authoring, per the coverage spec. Remaining in this issue: the dev-case half rides barwise-845 (verbalize-and-read while pinning the three dev references).","closed_at":"2026-08-27T13:53:12Z","close_reason":"Audit executed and merged in PR #360 (docs/reference-rubric-audit-2026-08-27.md): ten checks added across five rubrics, one misdescribing hint corrected, suite 2.2.0, every answer key verified at 1.000 offline. Findings filed as barwise-879 (payload defects incl. the employee-hierarchy unsettled-constraint assertion), barwise-880 (forMandatory anchor gap), barwise-881 (objectification-blind correspondence). The dev-case verbalize-and-read half rides barwise-845 per constraint-extraction-coverage.spec.md workstream 1."}
{"_type":"issue","id":"barwise-879","title":"Re-record the payloads the reference audit found defective or incomplete","description":"Findings F1-F4 of docs/reference-rubric-audit-2026-08-27.md, all payload defects the audit could not fix (references are generated; hand-edits are destroyed by regen). employee-hierarchy: Manager-manages IUC is on the Department role (unsettled) while the settled Manager-side IUC is absent, and the settled DepartmentName-role uniqueness (no two departments share a name) is missing. order-management: settled OrderStatus enumeration and the contains-\u003e=1-product mandatory absent. clinic-appointments: settled AppointmentStatus enumeration absent. university-enrollment: settled LetterGrade enumeration absent. conference-reviews' ReviewScore enumeration proves the pipeline carries value constraints end to end, so these are prompt-headroom evidence too. Fold into the split-spec WS4 re-record round: pick new best payloads under the current prompt, regen references, then add the checks these constraints unlock (suite bump per ADR 0002's companion-spec rule).","status":"open","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-27T13:32:33Z","created_by":"claude","updated_at":"2026-08-27T13:32:33Z","design":"barwise/docs/reference-rubric-audit-2026-08-27.md (F1-F4)","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-880","title":"forMandatory has no anchor when the player appears in one fact type; two settled mandatories are untestable","description":"Finding F5 of docs/reference-rubric-audit-2026-08-27.md. core's forMandatory (CounterexampleGenerator) expresses a mandatory violation as an instance that exists in another fact type but never plays the mandatory role; when the player has no second fact type (Employee in employee-hierarchy, Review in conference-reviews) findAnchorRole returns nothing and forbids_population fails with 'unsupported constraint shape'. Extend the anchor: a player with a reference scheme exists by identification alone, and a subtype's supertype fact types are also existence witnesses. The two withdrawn checks (Employee works in Department mandatory; Review has ReviewScore mandatory) should land with the fix.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-27T13:32:33Z","created_by":"claude","updated_at":"2026-08-27T15:13:34Z","design":"barwise/docs/reference-rubric-audit-2026-08-27.md (F5)","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-27T15:13:34Z","close_reason":"Merged in PR #362 (docs/specs/mandatory-existence-witness.spec.md): buildObjectUniverse and findAnchorRole share one identificationSharingAncestry function, so a subtype instance witnesses its supertype's existence across all four population rules and the mandatory counterexample; the withdrawn employee-hierarchy works-in check is restored at suite 2.3.0 with its answer key at 1.000. The objectification half (Review has ReviewScore) is deferred to barwise-881 -- a pair instance names no review_id, so witnessing WHICH Review exists needs the instance-identity design that issue owns."}
{"_type":"issue","id":"barwise-881","title":"Population correspondence cannot see through objectification, so shape-equivalent models score differently","description":"Finding F6 of docs/reference-rubric-audit-2026-08-27.md. 'At most one grade per (student, offering)' is expressible as the reference's ternary or as an objectified Enrollment carrying the grade -- both correct ORM -- but populationMapping corresponds fact types by player multiset and cannot map a ternary onto an objectified pair-plus-attachment. A check on the ternary would pin the reference's shape against the gym's grade-by-semantics rule, so the settled rule stays unguarded. Design objectification-aware correspondence (when the reference fact type's players include an objectified fact type's players, consider the candidate's objectified form) before adding the university-enrollment grade check. Same family as the set-comparison check gap (constraint-extraction-coverage spec workstream 3).","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-27T13:32:33Z","created_by":"claude","updated_at":"2026-08-27T15:36:10Z","design":"barwise/docs/reference-rubric-audit-2026-08-27.md (F6)","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-27T15:36:10Z","close_reason":"Implemented per docs/specs/objectified-correspondence.spec.md, landing via PR #364: an append-only expansion tier in populationMapping folds absorbed reference roles into one deterministic synthetic value, so the settled grade rule is guarded at suite 2.4.0 and both correct shapes pass. The reverse direction and existence-witnessing through objectification are recorded as out of scope in the spec, each waiting on a driving case."}
{"_type":"issue","id":"barwise-882","title":"prompt eval/artifact need an --artifact-version flag; the default is reachable only by a shadow-directory trick","description":"docs/local-eval-runbook.md's 'Gap worth closing', now tracked. prompt eval has no way to say 'this model, the default prompt': with builtins always in the candidate set, forcing the default while holding the model fixed requires shadowing the builtins with match-less copies (the runbook's /tmp/default-only recipe). An operator who does not know the trick will compare default vs variant by changing the model, confounding exactly what the comparison separates. Add --artifact-version \u003cversion|default\u003e to prompt eval and prompt artifact, resolving within the candidate set and erroring on an unknown version. Wanted before the split-spec WS4 re-baseline, which runs the default arm on both models.","status":"closed","priority":2,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-08-27T17:05:17Z","created_by":"claude","updated_at":"2026-08-28T02:23:55Z","design":"barwise/docs/local-eval-runbook.md (Gap worth closing)","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-28T02:23:55Z","close_reason":"Implemented as --artifact-version \u003cversion|default\u003e on prompt eval and prompt artifact (PR #367). One shared artifactByVersion selector beside artifactCandidates; unknown versions fail before a client exists, listing what would have worked; both commands announce a forced version on stderr. Runbook default-arm recipe now uses the flag; shadow-directory trick retired. Design recorded as a dated extension in docs/specs/artifact-resolution-parity.spec.md."}
{"_type":"issue","id":"barwise-883","title":"Conformance duplicate-constraint key still ignores values, frequency bounds, and subset targets","description":"constraintKey in ExtractionConformance.ts dedupes on type|fact_type|sorted-roles plus (since suite 2.5.0) ring_type. The ring case was found live: a recorded incident-response payload carried irreflexive AND acyclic on 'Incident is duplicate of Incident' and the acyclic one was silently dropped as a duplicate. The same key still conflates: two value constraints on the same role with different value lists, two frequency constraints with different min/max, and two subset constraints with different superset_fact_type/superset_roles. Each pair is either independent content (drop = silent loss, the ring case) or a contradiction (drop = hiding a defect the eval should charge for). Decide per type which fields belong in the key, mirroring what constraintConsistency accepts as distinct, and extend the ring_type test in ExtractionConformance.test.ts. Found during the barwise-845 close.","status":"open","priority":3,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-28T00:59:17Z","created_by":"claude","updated_at":"2026-08-28T00:59:17Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-884","title":"Verbalizer renders a subset constraint's superset role as a raw UUID","description":"barwise verbalize on the 2.5.0 vendor-onboarding reference prints 'If Vendor then 00000000-0000-701e-b0b7-bec5ccd3dae1.' for the subset constraint whose superset role is the Vendor role of 'Vendor has VendorStatus'. The subset verbalization resolves the subset side to a player name but emits the superset role id verbatim instead of naming its player and fact type. Repro: npx barwise verbalize packages/promptlab/evals/vendor-onboarding.reference.orm.yaml. Found during the barwise-845 verbalize-and-read pass.","status":"open","priority":3,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-28T00:59:29Z","created_by":"claude","updated_at":"2026-08-28T00:59:29Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-885","title":"Extraction schema cannot express a disjunctive mandatory across fact types","description":"InferredConstraint names one fact_type and roles within it, so the settled incident-response rule 'an incident has an alert, or a customer report, or both, and never neither' has no encoding: it spans roles in 'Incident originates from Alert' and 'Incident originates from Ticket'. The recorded haiku45-2 payload attempted it anyway, as a disjunctive_mandatory with one role plus the subset fields carrying the other fact type, and conformance rightly dropped that as arity_mismatch -- so the answer key pins the miss as a penalty (see scoreExtraction.test.ts, incident-response row). Core's constraint model does support cross-fact-type disjunctive mandatories; closing this needs a schema shape for them, parser support, and a conformance mirror. Until then the rule stays prompt-headroom evidence, recorded in the incident-response.eval.yaml comment.","status":"open","priority":3,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-08-28T00:59:29Z","created_by":"claude","updated_at":"2026-08-28T00:59:29Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-886","title":"Undici stream-drop 'terminated' was classified terminal, killing runs unretried","description":"classifyFailure (promptlab/src/run/retry.ts) fell through to terminal for undici's bare mid-stream connection-drop messages ('terminated', 'other side closed', 'fetch failed') -- no HTTP status, no pattern match. Measured on the 2026-08-28 default-haiku dev arm: 6 of 15 calls died unretried on ~2-minute streaming responses; incident-response kept 1 of 5 samples and the runner rightly refused to record. Fixed by adding the three messages to TRANSIENT_PATTERNS (terminal auth wording still wins first); tests pin both directions.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-28T13:15:26Z","created_by":"claude","updated_at":"2026-08-28T13:15:59Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-28T13:15:59Z","close_reason":"Fixed in the same change that filed it: terminated/other-side-closed/fetch-failed added to TRANSIENT_PATTERNS with tests both directions (auth wording stays terminal)."}
{"_type":"issue","id":"barwise-887","title":"prompt eval runs every call serially; add bounded concurrency","description":"An eight-arm re-baseline at repeat 5 is ~2 hours of wall clock on ~2-minute haiku/sonnet calls run one at a time. runSuite's serial loop is deliberate where caching is concerned -- the first call writes the prompt-cache entry the rest read (system prompt once per run, user message once per case) -- but nothing else requires it. Design: a --concurrency N flag threading to runSuite; per-case first call runs solo to warm that case's user-message breakpoint (and the run's first call overall warms the system prompt), then the case's remaining repeats fan out; retries/backoff and progress events must stay correct under interleaving; default stays 1. Spec to cover interaction with rate limits and the payload-durability fix (barwise-888).","status":"closed","priority":2,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-08-28T13:17:21Z","created_by":"claude","updated_at":"2026-08-28T13:26:29Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-28T13:26:29Z","close_reason":"Implemented (PR #368): --concurrency \u003cn\u003e on prompt eval; cases run as parallel chains via a pure boundedAll pool, repeats stay serial per case, and the run's first call completes alone behind a gate to warm the system-prompt cache. Default 1 is byte-identical to the old serial loop. Spec: docs/specs/eval-run-concurrency.spec.md."}
{"_type":"issue","id":"barwise-888","title":true,"description":"Operator observation on the 2026-08-28 arm: every file in /tmp/eval-payloads/\u003carm\u003e carries the same mtime -- savePayloads(report, dir) runs after runSuite returns, so payloads accumulate in the report and a crash or kill late in a sweep loses everything the run paid for. The per-case best/worst selection is the reason for batching, and the case boundary is therefore the natural durable point: flush each case's selected payloads when the case completes rather than when the suite does. Design rides the concurrency spec (barwise-887) since both touch runSuite's event flow.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-28T13:17:21Z","created_by":"claude","updated_at":"2026-08-28T13:26:29Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-28T13:26:29Z","close_reason":"Implemented (PR #368): runSuite gained onCaseComplete, and --save-payloads writes each case's retained payloads the moment the case finishes -- a crash late in a sweep now keeps everything already paid for. Same spec as 887."}
{"_type":"issue","id":"barwise-889","title":"prompt eval --verbose is silent until the first call completes; say the warm call started","description":"Operator experience on the first concurrent sweep (2026-08-28): 'Using artifact version...' printed, then nothing for the length of one call, because progress events fire on sample COMPLETION and the concurrency gate deliberately runs the run's first call alone (~2 minutes on dev-sized transcripts). An operator cannot tell warming from hanging. Emit one verbose line when the warm call starts (and possibly when the gate opens), e.g. 'warming prompt cache with the first call...' -- a progress event kind or a direct stderr line behind --verbose; renderer + a test.","status":"open","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-28T13:53:16Z","created_by":"claude","updated_at":"2026-08-28T13:53:16Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-890","title":"forbids_population cannot map a reference fact type onto a candidate with extra roles (projection tier)","description":"Third correspondence gap, surfaced by the first recorded 2.5.0 haiku sweep and now visible in three cases at once. The flat tier needs an exact player multiset and the expansion tier only folds declared objectifications, so a candidate that carries the same rule in a WIDER fact type never corresponds: conference-reviews runs 1-3 (0.857 mode, new today) pass requires_element factTypeBetween Reviewer/Paper but fail 'no fact type matching Reviewer reviews Paper' -- the candidate connects them through a fact type with extra roles; vendor-onboarding 0/5 matched the 5-ary contact fact type (a Contact-entity shape would also fail the multiset); freight run 2 missed 'Shipment contains Product in Quantity' the same way. Design sketch: a projection tier, tried after expansion -- find a candidate fact type whose players are a SUPERSET of the reference's, map the shared roles, and give each extra role fresh distinct values per injected instance; an IUC spanning only the shared roles then rejects the counterexample while an IUC spanning everything does not, which is exactly the semantic question. Needs the same declared-never-inferred care as the expansion tier and a spec before code: a superset match can be a false correspondence (two unrelated fact types sharing players). Until then these misses score as absent relationships, conflating 'modeled it wider' with 'did not model it'. Verify against the saved payloads in /tmp/eval-payloads/haiku45-2-{dev,train} before speccing.","status":"closed","priority":2,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-08-28T14:32:56Z","created_by":"claude","updated_at":"2026-08-28T18:22:53Z","dependency_count":0,"dependent_count":0,"comment_count":0,"notes":"Spec drafted for review: docs/specs/wider-shape-correspondence.spec.md. Two tiers after the expansion tier, both rescue-only: PROJECTION (reference players a strict sub-multiset of a wider candidate fact type's; extra roles get per-instance-distinct fresh values -- the vacuity guard that keeps a spanning IUC failing) and ENTITY-FOLD (absorbed reference value roles fold into one entity role evidenced by the candidate's own attribute binaries). Anchor propagation repairs itself through the shared mapForbiddenPopulation path. Carrier-aware failure messages replace the blanket 'does not carry'. Riders on the 2.6.0 bump: 891 per-mode payload retention, 892 suspen stem. The committed 20260828-0936 payloads become the integration fixtures. Post-landing: free offline re-score of all eight arms; the sonnet5-3 verdict is the cell expected to move.","closed_at":"2026-08-28T18:22:53Z","close_reason":"Projection and entity-fold tiers shipped in learn's populationMapping with the carrier-aware failure message and attempt loop; anchor propagation repairs through the shared path as specced. Rescue-only pinned by unchanged 2.5.0 answer keys; integration pins on the recorded sonnet clinic and haiku vendor payloads. Suite bumped to 2.6.0; offline re-read of all eight arms appended to docs/prompt-baseline-2.5.0-2026-08-28.md (clinic +0.500 both prompts, vendor ~+0.09 every arm, university unblinded as a new finding). The audit also found seven stale payload files (README added) and filed barwise-894 for the check's blunt rejection predicate."}
{"_type":"issue","id":"barwise-891","title":"Payload retention keeps best and worst only, losing a trimodal case's middle mode","description":"keepDiagnosticPayloads retains each case's best and worst scored runs (plus unscorable). conference-reviews on the 2026-08-28 recorded haiku run was trimodal -- 0.986 best, 0.684 worst, and a 0.857 mode on three of five runs -- and the middle mode's payloads were never written, which left the barwise-890 verification inferential for exactly the mode that mattered. Consider retaining one payload per score mode (cluster by score equality after rounding, keep the earliest of each), or a --save-payloads=all escape hatch for diagnosis rounds. Cheap either way; the current policy predates multi-modal cases.","status":"closed","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-28T14:51:40Z","created_by":"claude","updated_at":"2026-08-28T18:22:53Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-28T18:22:53Z","close_reason":"keepDiagnosticPayloads retains one payload per distinct score mode (3-decimal rounding, per the wider-shape spec's resolved open decision) plus unscorable runs; unit-tested on the trimodal shape. Shipped with suite 2.6.0."}
{"_type":"issue","id":"barwise-892","title":"vendor-onboarding requires_ambiguity token 'suspend' misses the noun form 'suspension'","description":"The matcher is honest case-insensitive substring over ambiguity descriptions, and 'suspension' does not contain 'suspend' (suspen-d vs suspen-s). Sonnet dev runs 1-2 (2026-08-28) reported exactly the parked item -- 'The post-suspension handling of in-flight purchase orders is explicitly unresolved... pending a policy decision from the steering group' -- and failed the check because the text never uses the verb form, while haiku's phrasing ('when a vendor is suspended') happens to match. Fix: change matches: [suspend] to a stem both forms contain ([suspen]), landing with the barwise-890 suite bump (any rubric loosening is a version bump). A lexical false miss of the same family the name licence fixed for object types.","status":"closed","priority":3,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-28T15:08:30Z","created_by":"claude","updated_at":"2026-08-28T18:22:53Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-28T18:22:53Z","close_reason":"vendor requires_ambiguity token is now the stem 'suspen', matching suspend/suspended/suspension alike; suite 2.6.0 changelog names it. The 2.6.0 offline re-read shows the affected vendor payloads rising accordingly."}
{"_type":"issue","id":"barwise-893","title":"barwise prompt compare: resolvability verdicts between recorded history rows","description":"The 2026-08-28 baseline round's core deterministic analysis was done by hand from run footers: pick two rows (artifact, model, split), subtract means, compare the gap against the combined resolvable difference, repeat per case. Every input is already recorded -- history rows carry mean, per-case stats, and dispersion, and src/stats/dispersion.ts owns the arithmetic -- but nothing computes a COMPARISON, so the runner prints 'gaps below X are not resolvable' and the operator does the subtraction. Add barwise prompt compare (or prompt history --compare A B): select two rows by artifact/model/split (or row index), print suite and per-case deltas each with a resolvable/unresolved verdict from the recorded dispersions, and refuse pairs whose suiteVersion differs (the incomparability the version field exists to mark). Pure read over history.jsonl + dispersion math; no calls, no clock. The judgment half of baseline analysis (payload forensics, attribution, prioritization) stays human -- this automates exactly the arithmetic layer.","status":"closed","priority":2,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-08-28T15:48:36Z","created_by":"claude","updated_at":"2026-08-29T18:38:26Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-29T18:38:26Z"}
{"_type":"issue","id":"barwise-894","title":"forbids_population rejection counts any population error, making passes vacuous under candidate mandatories","description":"Payload-verified during barwise-890 WS2: candidateRejects counts any error-severity population/* diagnostic, so on a candidate that declares mandatory constraints touching the injected players, ANY injection is rejected -- the check passes whether or not the constraint under test exists. Mutation evidence: deleting the Meridian IUC from the vendor answer key (flat tier) or from haiku45-dev/vendor-onboarding-run1 (entity-fold tier) leaves the check passing, because the synthetic/minted players trip 'every Contact/Vendor has X' mandatories. Pre-existing and suite-wide (not introduced by the 890 tiers -- verified on the flat tier first). A fix must not break the mandatory check kind, whose rejection IS an unrelated-population mandatory firing (the anchor). Likely direction: filter rejections to diagnostics attributable to the constraint kind and carrier under test, with a spec of its own. Out of scope for 890 per its spec (no change to which constraints are checked).","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-28T17:57:04Z","created_by":"claude","updated_at":"2026-08-29T20:03:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-29T15:48:51Z","notes":"Blast radius, measured 2026-08-29 at zero API cost (mutation sweep over all ten answer keys, scored offline):\n\n16 of 24 forbids_population checks whose SUBJECT is a uniqueness constraint PASS on a key with every uniqueness constraint deleted. 31 of 43 pass in the mandatories-only world. Affected: clinic-appointments, incident-response, order-management, subscription-billing, university-enrollment, vendor-onboarding. Clean: conference-reviews, freight-corrections, project-staffing.\n\nTwo controls ruled out the innocent readings. Not vacuity-in-general: with ALL constraints stripped, 0/43 pass. Not redundancy among uniqueness constraints: deleting the whole uniqueness class still leaves 31 passing, so the mandatories do the rejecting. The single-deletion figure alone (30 of 43) would have supported 'the suite is 70% vacuous', which is false.\n\nFIXED at suite 2.8.0 by docs/specs/attributable-rejection.spec.md. Attribution needed the corresponding FACT TYPE, not the role player: rule kind alone left 12 checks still passing, rule kind plus player left 8, rule kind plus corresponding fact type left 0. forbids_population now discriminates 43/43 under the sharpened per-fact-type mutation, with no answer-key regressions; 40 of 115 recorded payloads fell, none rose."}
{"_type":"issue","id":"barwise-895","title":"Own populations blind forbids_population mandatory checks: candidateRejects filters by elementId the mandatory rule never carries","description":"Payload-verified on the opus probe and reproduced synthetically: a candidate with the mandatory DECLARED passes the check when it has no populations and fails the identical check the moment it carries one sample population. candidateRejects switches to its hadOwnPopulations branch, which counts only error diagnostics whose elementId is an injected population id -- and mandatory violations attach elsewhere, so every forMandatory rejection becomes invisible. The extraction prompt instructs models to capture example populations, so the evaluator punishes exactly the models that obey. Blast radius in the 20260828-1647 round: 29 saved payloads carry populations (all 5 default-sonnet subscription runs, 4/5 vendor runs on both sonnet arms, 3 default-sonnet incident runs, freight runs in three arms); the baseline's 'genuine constraint-drop' reading of the sonnet dev cells is partially this bug, and opus's subscription 0.698 under-credits three declared mandatories. The freight answer key (the one populated key) still passes its full rubric, so its rubric evidently exercises no mandatory forbids check through this branch -- the keys did not pin this. Fix direction needs care: the filter exists so a candidate's own-data violations do not count as rejection, and the naive fix (count all errors when populations are samples) must not break that; likely a spec of its own plus a suite bump, since scores rise where models were blindly punished. Offline re-score of the committed round is free once fixed.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-29T01:58:46Z","created_by":"claude","updated_at":"2026-08-29T02:13:11Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-29T02:13:11Z","close_reason":"candidateRejects judges by caused errors (before/after multiset keyed by rule+element+message); synthetic repro and opus shape pass, own-data violations still cancel, unpopulated path byte-identical, answer keys pinned. Suite 2.7.0; offline re-read shows 11 rises / 0 falls on the recorded round."}
{"_type":"issue","id":"barwise-896","title":"Vendor entity-fold evidence fails on undeclared synonyms: EmailAddress/PhoneNumber vs ContactEmail/ContactPhone","description":"The opus vendor probe modeled the exact Meridian shape the entity-fold tier was built for -- ternary 'Vendor in Region has primary Contact' with IUC over [Vendor, Region] and Contact attribute binaries -- and still scored 'does not yet carry', because the fold requires every absorbed reference name evidenced by a candidate binary and opus named the value types EmailAddress and PhoneNumber where the reference says ContactEmail and ContactPhone. The transcript's own words are 'Name, email, phone number' (no Contact prefix), so both namings are faithful; this is the 2.1.0 lesson again, resolved by declaration: extend vendor-onboarding's vocabulary with [ContactEmail, EmailAddress] and add PhoneNumber to the existing [ContactPhone, ContactPhoneNumber] set. Rubric change, so it rides the next suite bump (with barwise-895's fix). Note ContactName matched only by luck of opus's naming; consider whether the set should also license the bare transcript nouns, per the declare-only-what-the-transcript-uses rule.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-29T01:58:56Z","created_by":"claude","updated_at":"2026-08-29T02:13:11Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-29T02:13:11Z","close_reason":"Vendor vocabulary widened per the transcript's own words: EmailAddress joins ContactEmail, PhoneNumber joins the ContactPhone set. Rides suite 2.7.0."}
{"_type":"issue","id":"barwise-897","title":"DSPy compile is silent between tqdm ticks: per-call progress feedback for the optimizer lane","description":"Operator feedback from the first real sonnet compile (2026-08-29): between tqdm ticks (~82s each at sonnet latency) nothing says what the lane is doing -- which example, which phase, metric score of the trace just collected, running call/spend count against --max-calls. The eval runner solved the same silence with --verbose per-sample lines and a retry event (barwise-889 files the warm-call gap); compile.py should emit the equivalent: a line per metric/LLM call with example id, phase (bootstrap/candidate N), score, and calls-used-of-budget, so an operator can tell a healthy 80s/call run from a hung one and can abort early when scores are flat.","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-29T02:06:48Z","created_by":"claude","updated_at":"2026-09-06T02:17:04Z","dependency_count":0,"dependent_count":0,"comment_count":0,"notes":"Implemented 2026-08-29 alongside 899. make_metric gained an optional progress callback reporting every evaluation (case id, score, phase, calls spent/ceiling) to stderr -- stdout carries the run's JSON result. Four phases are labelled: baseline, shipped, compile, candidate. All four metric return paths report, including the two that score zero without reaching the scorer, since silence on the failure paths is what made the silence dangerous; a throwing callback is swallowed, matching the observability sinks' rule that telemetry must not fail the paid run it observes.","closed_at":"2026-09-06T02:17:04Z","close_reason":"Verified on main: metric._report + ProgressReporter in compile.py; uv run --frozen --extra dev pytest optimizer/tests/test_metric.py test_compile.py test_verdict.py -\u003e 47 passed"}
{"_type":"issue","id":"barwise-898","title":"prompt eval --case \u003cid\u003e: run one case for targeted probes","description":"The thinking-budget probe design (docs/specs/thinking-budget-dimension.spec.md) wants conference-reviews x repeat 5 alone -- 5 calls a leg -- but eval's narrowest selector is --split, so a targeted probe costs a whole half-suite (35 train calls). A repeatable --case \u003cid\u003e filter (validated against the manifest, refusing history recording or recording with the case subset named -- a one-case mean must not masquerade as a split mean) makes failure-targeted probes seven times cheaper. Runbook's thinking-dimension section references this issue.","status":"open","priority":3,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-08-29T02:17:44Z","created_by":"claude","updated_at":"2026-08-29T02:35:19Z","dependency_count":0,"dependent_count":0,"comment_count":0,"notes":"Power analysis (2026-08-29) raises this from convenience to blocker for the thinking experiment. From the 2.6.0 record: a PERFECT conference fix moves haiku's train mean +0.009 against a ~0.025 two-leg threshold, and at case level the best-case +0.063 sits against a ~0.176 combined margin at repeat 5. Resolving it needs ~40 samples per leg: 280 calls per leg through --split, versus 40 with --case. The thinking probe is deferred until this or the barwise-846 WS3 harder cases land; the runbook's thinking-dimension section carries the arithmetic."}
{"_type":"issue","id":"barwise-899","title":"Optimizer compile report labels the seed program 'baseline', inviting the reading that it is the shipped prompt","description":"The 2026-08-29 sonnet bootstrap run reported baseline 0.380 / candidate 0.355 on the dev split. Both are the 90-word SEED_INSTRUCTIONS program (zero-shot and +2 demos); the shipped default scored 0.814 on the same three dev cases at repeat 5 in the 2.6.0 record. Nothing in the report says so, and 'baseline' is exactly the word a reader takes to mean 'what we ship' -- so a run that lost to the shipped prompt by ~0.45 reads as a near-tie against a baseline. program.py's own docstring has it right ('The shipped prompt is the baseline to beat, not the starting point'); the report contradicts it by omission. Fix: score the shipped artifact for the target model on the same dev subset in the same run and emit it as a third block (shipped/baseline/candidate), so 'did the compile beat what we ship' is answered in the report rather than by a reader who remembers a number from another document. Cheap: the shipped-artifact evaluation is the same metric path, and its cost is one dev sweep.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-29T02:37:26Z","created_by":"claude","updated_at":"2026-09-06T02:11:59Z","dependency_count":0,"dependent_count":0,"comment_count":0,"notes":"Implemented 2026-08-29. compile.py scores a third arm (the shipped instructions for the target model, via the existing default_instructions seam) on the same dev cases, skipped only when --seed-from default already makes baseline the shipped prompt; the report gains 'shipped mean', 'margin over shipped', and a verdict line separate from the seed margin (beats / ties / loses, with 'do not spend a keyed arm' on a loss). Also fixed a second defect found while in there: the report's own gating command read '--artifacts packages/llm/prompts', which widens the candidate set with the directory the SHIPPED builtins are generated from -- following it measures a shipped variant while the reader believes they are gating the candidate (barwise-850 reproduced in an instruction). It now names the candidate's own directory and forces --artifact-version. Both pinned by tests; all three arms carry a caveat that they compare to each other and not to a recorded history row.","closed_at":"2026-09-06T02:11:59Z","close_reason":"Implemented 2026-08-29 per notes: compile.py scores the shipped arm, report emits shipped mean / margin over shipped / verdict; gating command corrected; pinned by tests"}
{"_type":"issue","id":"barwise-900","title":"Nothing catches a red optimizer test: the lane's only gate is 'executed by hand', and it failed for weeks","description":"test_the_candidate_resolves_through_the_production_loader -- the test dspy-optimizer.spec.md singles out as the one that found the missing match block, because it round-trips a candidate through the REAL loader rather than asserting the exporter agrees with itself -- was red from the day the haiku45-2 variant shipped until 2026-08-29, when it was noticed by accident while working on barwise-899. Two independent staleness causes had accumulated in it: the candidate's derived match block now collides with a shipped variant, so `--artifacts` alone is correctly ambiguous; and the asserted string had a trailing period the CLI stopped emitting when barwise-850 made it echo the resolved client.\n\nThe defect is not the test. It is that nothing anywhere reports the optimizer suite is red. The lane is deliberately outside Turborepo and CI (optimizer/CLAUDE.md: 'never run in CI'), and dspy-optimizer.spec.md states the whole guard in one clause: 'The Python lane has its own pytest run, executed by hand.' A guard executed by hand is executed when someone remembers, and the failure mode is silent -- the next person to remember is the next person to spend money through the path the red test covers.\n\nNot obviously 'add it to CI': the suite takes ~3.5 minutes, needs a Python toolchain plus a built CLI (the metric tests shell out to the real `barwise prompt score`), and CI-ing a dev-time lane cuts against the dependency rule that keeps it out of the graph. Options worth weighing rather than picking here: (a) a CI job gated on paths optimizer/**, accepting the toolchain cost; (b) compile.py refusing to spend without a recent green pytest marker, which puts the gate where the money is; (c) a scheduled weekly run; (d) explicitly accept the rot and say so in the spec, dropping the claim that hand execution is a gate. Spec first per the house rule.\n\nFound while doing barwise-899 (PR #379), which fixed the test itself and pinned both halves of the ambiguity.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-29T14:07:23Z","created_by":"claude","updated_at":"2026-08-29T20:03:00Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-29T20:02:10Z","notes":"Closed 2026-08-29 by option (a): a CI job gated on the lane's paths.\n\nOf the four options weighed, (a) won on measurement rather than argument. The cost objection was the real one, and it turned out small: uv installs from pip in seconds, 'uv sync --extra dev --frozen' audits the committed lock in under a second, and the suite runs 95 tests in 3m29s. Gated on changes under optimizer/ or packages/cli/, most PRs never pay it.\n\npackages/cli/ is in the filter deliberately: the metric tests shell out to the real 'barwise prompt score' rather than mocking it, so a CLI change can break this lane without touching a Python file. That is also why the step runs after npm run build -- the seam reads dist.\n\n--frozen rather than a plain sync, for the reason the lock is committed at all: a compile costs money, so pin what produced it. A stale lock now fails CI instead of silently resolving something else. uv comes from pip, not a third-party setup action: this workflow uses first-party actions/* only and widening that belongs to the repo owner.\n\nOption (b) -- compile.py refusing to spend without a green marker -- is NOT closed by this. CI catches a red test on a PR; it does not catch an operator compiling from a tree whose tests were never run. compile-runner.sh does that half by running pytest as preflight."}
{"_type":"issue","id":"barwise-901","title":"subscription-billing requires_ambiguity token 'cancel' passes on an unrelated ambiguity (false pass)","description":"Mirror image of barwise-892. That issue fixed a false MISS (the token 'suspend' did not match the noun 'suspension'); this is a false PASS from a token that is too weak.\n\nThe check is `matches: [cancel]` with the hint 'The room parked what happens commercially when an annual subscription is cancelled mid-term.' The subscription-billing answer key reports nine ambiguities, and at least two contain the substring 'cancel':\n\n  1. 'Mid-term annual subscription cancellation policy' -- the intended one\n  2. 'Subscription status values and edge cases' -- lists 'trialing, active, past due, and cancelled'\n  3. 'Paused subscription status' -- parenthesises the same status list\n\nVerified by mutation, zero API calls: delete ONLY the intended ambiguity from the answer key and the check still passes, reporting\n\n  An ambiguity matching \"cancel\" was reported: \"Paused subscription status: The\n  system treats 'paused' as a separate boolean flag independent of the four core\n  statuses (trialing, active, past due, cance...\"\n\n-- a boolean-flag ambiguity, matched because 'cancelled' appears in a list of status values. Deleting every remaining 'cancel' mention finally fails it, which confirms the check is live but keyed on the wrong discriminator.\n\nWhy it matters more than a single check: 'cancelled' is a status value the transcript uses, so ANY competent model will mention it somewhere in its ambiguities, and the check therefore passes for essentially every candidate regardless of whether it parked the commercial policy question. It is a guaranteed point, not a measurement.\n\nFix direction: a token that discriminates the policy question from the status vocabulary -- 'mid-term' is the obvious candidate (it appears in the hint and nowhere in the status lists), possibly with 'cancel' retained as a conjunct rather than an alternative. That is a rubric loosening/tightening either way, so it rides a suite bump.\n\nThe two other requires_ambiguity checks (incident-response, vendor-onboarding) were mutation-checked in the same sweep and DO discriminate: deleting the intended ambiguity fails each. 2 of 3 sound.\n\nFound by the 2026-08-29 zero-call rubric audit (see docs/prompt-optimization-log.md).","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-29T14:38:30Z","created_by":"claude","updated_at":"2026-08-30T00:45:28Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-30T00:45:28Z","close_reason":"Fixed: matches is conjunctive (terms.every), so [cancel, mid-term] requires both substrings in the SAME ambiguity. 'mid-term' appears in the intended policy ambiguity and in neither status-vocabulary list, so it discriminates exactly. Verified by the mutation that found the bug: deleting only the intended ambiguity now FAILS the check ('None of the 8 reported ambiguities match cancel + mid-term'), with the two other cancel-bearing ambiguities still present. audit:rubric now reports requires_ambiguity 3/3 and its baseline row is removed. Suite bumped 2.9.0 -\u003e 2.10.0, since a candidate that passed on the weak token now fails."}
{"_type":"issue","id":"barwise-902","title":"must_validate has never failed: the corrector repairs upstream, so the check is a constant and the validationError weight is dead","description":"One `must_validate` check per case, 10 of the suite's 90. Evidence from the 2026-08-29 zero-call audit, two independent lines:\n\n**It has never failed on real output.** All 192 committed payloads from the two recorded rounds (eval-payloads/20260828-0936 and -1647), re-scored offline: 192 passed, 0 failed. In the same sweep `validationErrors` was 0 on every single payload.\n\n**No mutation of an answer key fails it either.** Tried: every object type deleted; every fact type deleted; both (a wholly empty model); a fact type with zero roles; a duplicated object type; every role player rewritten to a nonexistent type; the literal payload `{\"object_types\": \"not an array\"}`; and `{}`. All pass. On `{}` the case scores 0.125 with rubric 1/8 -- and the one passing check IS must_validate. The only check a completely empty extraction passes is the one asserting the model is sound.\n\n**Mechanism.** `enforceConformance` runs ahead of validation and repairs or discards what would otherwise be an error, so validation is handed a model with nothing left to report. The evidence is the complement: on the 115 payloads of the 1647 round, conformance corrections fire on 61 and validation WARNINGS fire on 78 (distribution 1:30, 2:15, 3:13, 4:4, 5:2, 6:1, 7:7, 8:3, 9:2, 19:1) -- the pipeline is plainly active. Only errors are absent, because errors are what the corrector eliminates.\n\nTwo consequences, and they are separable:\n\n1. **Every rubric fraction is inflated by a constant.** `rubricPassed` includes a check that cannot fail, so each case carries a guaranteed 1/N of credit -- 12.5 points on an 8-check case -- including for a candidate that returned nothing.\n2. **The heaviest weight in the manifest has never applied.** `validationError: 0.8` is the largest of the four and has multiplied zero in 192 payloads. It is not wrong, it is unreachable, and a reader fitting weights would reasonably assume it does work.\n\nFix direction, spec first: either run the validity check on the PRE-correction model (which makes it measure what its name claims and gives the 0.8 weight something to price), or accept it as a constant and remove it from the rubric denominator so scores are not inflated by a fixed term. These are different decisions -- the first changes what is measured, the second only how it is reported -- and picking between them wants the spec, not this issue.\n\nNote this is NOT the barwise-894 shape. 894 is a check that passes for the wrong reason; this is a check that has no reachable failure path at all.\n\nFound by the 2026-08-29 zero-call rubric audit (docs/prompt-optimization-log.md).","status":"open","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-29T14:38:50Z","created_by":"claude","updated_at":"2026-08-29T14:38:50Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-903","title":"Rubric discrimination ratchet: audit:rubric, so a check that cannot fail is caught by CI rather than by an audit that runs once","description":"Shipped. Recorded because the baseline file and the CI step need an issue to point at, and because the reasoning for the design belongs somewhere a reader can find it.\n\nThe 2026-08-29 hand audit found three rubric defects (barwise-894, -901, -902) at zero API cost, and was about to be left as a throwaway script with a note in the log saying nothing re-runs it. That note would have been the whole guard, and the project already has a rule about exactly this shape: a copy that must agree is never guarded by a comment. A finding is the same -- land the check or it decays.\n\nscripts/audit-rubric.mjs sweeps all 90 checks, applying the mutation each KIND is about: delete each object/fact type for requires_element, each ambiguity for requires_ambiguity, the whole declared constraint class for forbids_population, and a structural-corruption battery for must_validate. A check that survives its own mutation has no reachable failure path.\n\nCurrent state: 61/90 discriminate. requires_element 34/34 clean; forbids_population 25/43; requires_ambiguity 2/3; must_validate 0/10. The 29 open entries carry tracked: verdicts in rubric-baseline.json.\n\nNote the script is STRICTER than the hand audit, which called forbids_population 27/43. The hand pass keyed on uniqueness-subject checks only; the script uses each check's own declared ConstraintKind and so also catches mandatory-subject checks whose class deletion does not fail them. Two more findings, same evidence, no extra work -- which is the argument for the script over the sweep.\n\nRatchet semantics copied from audit-duplication.mjs deliberately: --check fails on a non-discriminating check missing from the baseline AND on a baseline entry that now discriminates, so fixing 894/901/902 forces those rows out rather than leaving them to rot. Both directions were mutation-verified, exit codes included (a guard that prints an error and exits 0 is the defect class this whole audit is about).\n\nRuns in CI after build, with the same docs_only guard, because it scores through the built promptlab.\n\nLimit, stated in the script header: discrimination is existential -- SOME mutation makes the check fail -- so it catches a check that cannot fail, never one that fails for the wrong reason. barwise-894 is visible only because its checks survive whole-class deletion; a subtler wrong-reason pass would not be.","status":"open","priority":2,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-08-29T14:45:47Z","created_by":"claude","updated_at":"2026-08-29T14:45:47Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-904","title":"forbids_population infers enforcement from diagnostics instead of asking the constraint; expose a per-constraint enforcement predicate","description":"Design question raised 2026-08-29: should each fact type (each constraint) come with a way to evaluate that it is enforced, rather than the evaluator inferring enforcement from validation side effects?\n\nIt should, and the current design is the argument for it. barwise-894 shipped THREE layers of compensation for one indirect measurement: a map from constraint kind to the population rule ids that count, an attribution step deciding whether a diagnostic names the right fact type, and a before/after multiset delta separating caused errors from pre-existing ones. Each layer is there because the question actually being asked -- 'does THIS constraint forbid THIS population' -- is answered by inspecting a flat, model-wide Diagnostic[] in which the answer is not distinguishable from every other rule firing at once.\n\nCore already asserts the pairing this wants, in Counterexample's own docstring: 'the deterministic inverse of population validation -- feeding the forbidden populations back through populationValidationRules reports a violation of the very constraint this counterexample was generated for.' Nothing can execute that claim. It is a prose invariant with no API, which is the shape the 2026-08-29 rubric audit kept finding.\n\nProposed: a per-constraint enforcement predicate in core -- given a constraint, its fact type, and a model carrying a population, does THAT constraint reject it. The population rules already contain the per-constraint logic; they are written as (model) =\u003e Diagnostic[] and iterate constraints internally, so the logic exists and only the entry point is missing.\n\nWhat it would delete from @barwise/learn: REJECTING_RULES and its drift test, namesTheCarrier, and the kind-to-rule mapping entirely. What it would NOT delete: correspondence. Which candidate constraint answers to a reference constraint is irreducible -- the candidate is a different model -- so correspondingFactTypes and the tiers stay. This narrows the indirection, it does not remove the hard part.\n\nAlso worth pricing in the same spec: Diagnostic carries elementId = 'c.id ?? ft.id', which is lossy by construction -- a caller cannot tell a constraint id from a fact type id, and namesTheCarrier has to search the model to find out. A structured constraintId would make attribution exact for anyone who still needs the diagnostic path.\n\nNot urgent: barwise-894 is fixed and measured (43/43 discriminating, zero answer-key regressions). This is the strategic version of the same fix, and it wants its own spec and a core API review, since core changes reach every downstream package.","status":"closed","priority":2,"issue_type":"feature","owner":"noreply@anthropic.com","created_at":"2026-08-29T15:48:51Z","created_by":"claude","updated_at":"2026-08-30T02:20:05Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-30T02:20:05Z","close_reason":"Workstreams 1 and 2 shipped (PRs #399 and #400). Workstream 3 (structured constraintId on Diagnostic) is not undone work: Open decision 2 resolved it as conditional -- only if a consumer needs it -- and workstream 2 removed the only caller that had to disambiguate 'c.id ?? ft.id'. It gets its own issue if a consumer appears. Gates: 43/43 still discriminate, rubric-baseline unchanged, prompt rescore moves zero of 192 payloads, no suite bump. Follow-up: barwise-911."}
{"_type":"issue","id":"barwise-905","title":"Root gate scripts have no test harness; check-shell's cwd-independence is unguarded","description":"barwise/scripts/*.mjs are CI gates run directly by npm scripts. `turbo run test` fans out to packages only, so none of them has a test. This is not theoretical: check-shell.mjs silently linted 6 scripts under `npm run check:shell` (cwd barwise/) and 7 when run by hand from the repo root, because `git ls-files '*.sh'` resolves its pathspec relative to cwd. .claude/hooks/session-start.sh was never linted in CI, the gate reported OK both times, and the discrepancy was noticed only by running it from two directories by accident. Fixed in 19189e6 by anchoring the listing to `git rev-parse --show-toplevel`, but that fix is guarded by construction and a comment -- delete `cwd: ROOT` and the gate goes quiet again. Per the repo convention, a finding is not closed by a document.","acceptance_criteria":"A test harness exists for barwise/scripts/*.mjs that runs under the monorepo test task, and check-shell.mjs has a test asserting its file list is identical when invoked from the repo root and from barwise/. The test fails if the cwd anchor is removed.","status":"closed","priority":2,"issue_type":"chore","owner":"noreply@anthropic.com","created_at":"2026-08-29T21:10:33Z","created_by":"claude","updated_at":"2026-08-29T22:48:03Z","dependency_count":0,"dependent_count":0,"comment_count":0,"notes":"Adjacent gates with the same exposure: check-no-nul.mjs (uses git ls-files), audit-duplication.mjs, audit-rubric.mjs. ci-local.mjs is already anchored -- it resolves ../.github/workflows/ci.yml from import.meta.url -- and that is the model to follow. Note that beads-crud create silently ignores --notes; it is an update-only field.","labels":["process","gate-coverage"],"closed_at":"2026-08-29T22:48:03Z","close_reason":"Closed by barwise/scripts/tests/gates.test.mjs, run as npm run test:scripts -- wired into ci.yml (24 gates, so ci:local and pre-push pick it up) and appended to the monorepo test script. node:test rather than vitest: the scripts are plain .mjs with nothing to compile and vitest is not a dependency at that level. check-no-nul and check-shell are each asserted to report IDENTICAL output from the repo root, from barwise/, and from a deep package directory. Mutation-verified: deleting cwd: REPO_ROOT from scripts/lib/tracked.mjs fails exactly those two tests and no others."}
{"_type":"issue","id":"barwise-906","title":"Verifications keep passing for reasons unrelated to what they verify","description":"Three instances, one shape: the probe was green, and the green meant nothing. (1) A NUL-gate probe written as an UNTRACKED file -- check-no-nul enumerates via git ls-files, so it never saw the probe and the gate printed OK. (2) The pre-push hook's exit code read through 'echo | sh hook | tail', which puts echo in PIPESTATUS[0]; the hook's real status was invisible and two readings came back 0. (3) 'python3 -m pytest' failing was concluded as 'this container cannot run the suite' and written into a commit message as fact; uv was installed and 'uv run pytest' gave 95 passed. In every case a green reading was reported to the user before anything had been shown to go red.","acceptance_criteria":"A gate or check is not considered verified until it has been shown to FAIL on a deliberate defect and PASS without it, with the defect placed where the gate actually looks (staged/tracked, not merely on disk) and the status read without a pipeline in between. Landed as a required step in the assertion-audit and session-review skills, so the next session starts from it rather than rediscovering it.","status":"closed","priority":1,"issue_type":"chore","owner":"noreply@anthropic.com","created_at":"2026-08-29T21:23:07Z","created_by":"claude","updated_at":"2026-08-29T22:48:03Z","notes":"The three share a root: I confirmed the outcome I expected instead of establishing that the instrument could report the other outcome. Red-then-green is the discipline; the two concrete traps worth naming in the skill are (a) a probe the enumerator cannot see, and (b) a status read through a pipe.","labels":["process","verification"],"dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-29T22:48:03Z","close_reason":"Closed, and by an executable check rather than the rule the acceptance criteria asked for. gates.test.mjs proves each gate RED on a defect planted where that gate actually looks -- staged into a throwaway git repo, never into this one -- and GREEN without it: a tracked NUL byte, a tracked shell script with SC2164, and root-script drift in both directions. It also pins the blind spot that caused the finding: an UNTRACKED NUL file must NOT trip the gate, asserted so nobody repeats that verification and believes the green. Mutation-verified: blinding check-no-nul's detection fails exactly the tracked-NUL test. The rule also landed as required steps in the assertion-audit and session-review skills, as the AC named."}
{"_type":"issue","id":"barwise-907","title":"cwd drift: repeated broken commands, and one wrong claim to the user","description":"Five occurrences in one session. Most are self-correcting (npm run from the repo root fails with ENOENT, loudly). The dangerous kind is silent: a gate that SUCCEEDS with different coverage depending on where it ran. That produced a wrong claim -- 'check-shell: 7 scripts, OK' quoted to the user as CI's number when CI's number was 6 -- and a live hole in check-no-nul, which scanned 1426 of 1483 tracked files in CI. The gate half is fixed structurally by scripts/lib/tracked.mjs. This issue is the operator half.","acceptance_criteria":"Decide and land one of: (a) a root package.json whose scripts forward into barwise/, so 'npm run X' works from either directory -- note CLAUDE.md currently states the root holds no package.json, so this needs the owner's agreement; (b) an explicit rule that every command that depends on cwd uses an absolute path or 'npm --prefix'; or (c) accept it and say so. Option (b) is discipline, not a mechanism, and must be marked as such if chosen.","status":"open","priority":2,"issue_type":"chore","owner":"noreply@anthropic.com","created_at":"2026-08-29T21:23:08Z","created_by":"claude","updated_at":"2026-09-06T02:19:20Z","notes":"Related: barwise-905 (no test harness for root gate scripts). The shared helper makes correct enumeration the easy path but nothing yet fails when a new script spells out 'git ls-files' directly.\n\nRepeat 2 (2026-09-06, session claude/todo-priorities-rwsmct): two broken commands in one session from cwd -- 'node scripts/beads-crud.mjs' run from the repo root instead of barwise/ (the root CLAUDE.md spells the path with the barwise/ prefix and the eye dropped it), and a 'cd packages/core \u0026\u0026 ...' whose cwd persisted into the next call so the next 'node scripts/...' resolved under packages/core. Both loud, neither wrong-claim-producing. The operator half was closed with skill text as the landing and the text did not hold. The form that removes the choice is a PreToolUse hook rejecting a bare 'cd' in a Bash command, or absolute paths only; that changes the shared session workflow, so it is the owner's call -- reopened as the ledger entry until that decision is made.","labels":["process","cwd"],"dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-29T21:32:07Z","close_reason":"Fixed by generating a repo-root package.json that forwards npm run into barwise/ (option (a) in the acceptance criteria). CLAUDE.md's claim that the root holds no package.json is updated. Drift is checked both directions by check:root-scripts, wired into ci.yml and therefore into ci:local. The gate half was already fixed by scripts/lib/tracked.mjs."}
{"_type":"issue","id":"barwise-908","title":"resolvable divides between-case spread by repeat count; repeats are cache hits","description":"Two defects, found by reading the 2026-08-29 mipro compile. (1) Every arm returned byte-identical scores across all five repeats. Established, not guessed: DSPy 3.3.1 defaults to cache=True and temperature is left None (provider default, not 0), so real repeats would vary -- these are cache reads. They spend the --max-calls ceiling but no API money. (2) Independent of the cache, and the more serious one: compile.py computes sd = sample_sd(candidate_log.scores) over ALL scores (3 cases x 5 repeats = 15), so the sd is dominated by between-case spread (0.154), not between-repeat noise (0.000). resolvable_difference then divides that by sqrt(samples_per_candidate) -- the REPEAT count. The units do not match, and the result treats 15 observations as independent when there are only 3. Measured on this run: reported resolvable 0.191 against an honest 0.292 (sd over case means / sqrt(cases)). It UNDERSTATES the threshold by 35%, so it calls margins resolvable that are not. The 2026-08-29 verdict is unaffected (|-0.488| clears both), but a 0.25 margin would have been reported as a resolved win.","acceptance_criteria":"resolvable_difference is fed a spread and an n that describe the same unit. Either compute the SE over per-case means with n = number of cases, or turn off the DSPy cache for the measurement arms so repeats are independent observations and pool properly across both dimensions. The delta report must never divide a between-case spread by a repeat count. Add a test that feeds a log whose repeats are identical and asserts the reported resolvable matches the case-level computation, so the current arithmetic cannot come back.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-29T21:43:30Z","created_by":"claude","updated_at":"2026-08-29T22:05:57Z","notes":"Observed only because a real compile was run and its per-call log read. The offline smoke test uses DummyLM with one canned answer, so identical scores there are expected and this could never have surfaced from it.","labels":["optimizer","measurement"],"dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-29T22:05:57Z","close_reason":"Fixed. MetricLog now records case_ids alongside scores through a single _append, exposes case_means(), and owns the pairing in resolvable() -- which returns the figure AND the n so no caller can supply its own. compile.py calls it; report.json records resolvableOver {unit: cases, n}. On the 2026-08-29 run the reported figure goes 0.191 -\u003e 0.292, matching the honest case-level computation. Note the cache question is answered but NOT changed: DSPy's cache=True stays on, and computing over case means is correct either way since it assumes nothing about repeat independence."}
{"_type":"issue","id":"barwise-909","title":"Record the re-scored baseline at suite 2.10.0","status":"open","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-30T01:37:19Z","created_by":"claude","updated_at":"2026-08-30T01:37:19Z","notes":"Workstream 3 of docs/specs/must-validate-outside-the-rubric.spec.md was never done: the 192 committed payloads have not been re-scored since must_validate left the rubric fraction, so no reader can see what the 2.9.0 bump cost each case. The suite has since moved to 2.10.0 (barwise-901), so the re-score is against that, and it covers both changes at once. Offline: barwise prompt rescore over the stored payloads, then record the per-case shift in docs/prompt-optimization-log.md. The log's Open section is also stale -- it still lists 894, 901 and 902 as open and names 2.7.0 as the unmeasured keyed round.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-910","title":"Spec Status headers decay silently; three mechanical proxies tried, all vacuous or noisy","status":"closed","priority":2,"issue_type":"chore","owner":"noreply@anthropic.com","created_at":"2026-08-30T01:41:56Z","created_by":"claude","updated_at":"2026-08-30T12:58:18Z","notes":"must-validate-outside-the-rubric.spec.md read 'Draft for review (design only -- no implementation in this PR)' after two of its three workstreams shipped, hiding the change in both directions: a reader could not tell the design had landed, nor that workstream 3 never had. constraint-enforcement-predicate.spec.md was about to repeat it. Corrected by hand in both; nothing prevents the next one.\n\nPer CLAUDE.md a finding is not closed by a document, so a gate was written -- scripts/check-spec-status.mjs, failing a spec whose Status reads Draft while its tracking issue is closed. RED-TESTED AND IT DID NOT FIRE: barwise-902 is legitimately still open (workstream 3 is undone), so the exact defect it was written for passes it. Deleted rather than shipped; a gate that is green for reasons unrelated to what it verifies is barwise-906's shape and worse than none.\n\nTwo further proxies were tested against real history and rejected on evidence:\n(a) 'any commit mentioning the tracking issue after the spec landed' -- barwise-902 is named in the bodies of commits about barwise-901 and barwise-904, so the signal fires on context mentions, not on implementation.\n(b) 'a commit shipping a workstream must also touch its spec file' -- restricted to the subject line to control (a)'s noise, it becomes vacuous: 074ddb1, which shipped workstream 2 of barwise-902, names no issue in its subject at all.\n\nCandidates not yet priced: (c) forbid the phrase 'no implementation in this PR' in a committed spec, since its truth conditions expire on merge -- mechanical and zero-noise, but it disciplines wording rather than detecting staleness; (d) require a Workstreams table with a per-workstream state, and check that a spec claiming a workstream shipped names the commit -- more structure to author, and the check is still only as good as the row.\n\nRelated stale headers found in the same sweep and NOT fixed: duplication-drift-guards.spec.md still reads 'Draft for review (design only)' although CLAUDE.md cites its ratchet as landed and running in CI.","labels":["process"],"dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-30T12:58:18Z","close_reason":"Closed by a gate that fires on the instance, not by a document: scripts/audit-spec-status.mjs, npm run audit:specs -- --check, in CI and ci:local (25 gates now).\n\nThe rule is narrow so it can be mechanical: a spec whose Status claims no implementation must not be sitting on top of commits that touched the source files the spec itself names, since the spec was last edited. It cannot tell that a workstream shipped; it can tell the claim is no longer safe to believe.\n\nVerified against real history rather than asserted. The gate takes --at \u003ccommit\u003e and scripts/tests/gates.test.mjs pins it at 7fbbba7 -- the commit that shipped workstream 1 of barwise-902 while must-validate-outside-the-rubric.spec.md still read 'design only -- no implementation in this PR'. It fires there and names that commit as the reason. Mutation-verified twice: neutering the Status pattern, and ignoring the commits-since check, each fail exactly that test.\n\nNote for anyone extending it: mutating a Status on today's tree does NOT reproduce the historical condition, because the comparison is against the commit that last touched the spec and every spec involved has been edited since. A red test written that way passes -- the barwise-906 shape -- which is why the red reading comes from --at.\n\nNoise control that made it usable: excluding package barrels (index.ts, registration.ts). A spec naming core/src/index.ts fires on every core change; that exclusion took the sweep from 4 findings (3 of them for unrelated reasons) to 1, the genuinely stale one.\n\nThat one was duplication-drift-guards.spec.md, which claimed 'design only' while all six of its workstreams had landed and CLAUDE.md cited its ratchet as running in CI. Its Status is now accurate and carries a Status-evidence line naming the artifact for each workstream. The baseline (barwise/spec-status-baseline.json) is consequently empty, and ratchets both directions: a new unclassified spec fails, and so does a stale row.\n\nThe three proxies rejected earlier stay rejected; the reasoning is recorded in the script header so nobody re-derives them."}
{"_type":"issue","id":"barwise-911","title":"forbids_population's widened constraint guards are unexercised: external uniqueness and disjunctive mandatory never fire","status":"closed","priority":3,"issue_type":"chore","owner":"noreply@anthropic.com","created_at":"2026-08-30T02:17:31Z","created_by":"claude","updated_at":"2026-08-30T12:51:36Z","notes":"CANDIDATE_GUARDS pairs two constraint types with internal_uniqueness (isInternalUniqueness, isExternalUniqueness) and two with mandatory (isMandatoryRole, isDisjunctiveMandatory), so a candidate expressing the reference's rule in the wider form still passes -- the false-miss shape barwise-892 and barwise-896 fixed. Nothing exercises either widening.\n\nMeasured three ways during barwise-904 workstream 2, on the rows and on the REJECTING_RULES map they were carried forward from (which had the same two-ids-per-kind structure, and was equally unexercised):\n  - drop isExternalUniqueness: 95/95 learn tests pass, forbids_population stays 43/43 discriminating, and prompt rescore moves 0 of 192 committed payloads.\n  - drop isDisjunctiveMandatory: 95/95 learn tests pass.\n  - the same mutation against the pre-904 diagnostic-based implementation (dropping population/external-uniqueness-violation from REJECTING_RULES): 97/97 tests pass.\n\nReading the counterexample shapes suggests the external-uniqueness path may be unreachable by construction rather than merely untested: the internal-uniqueness counterexample repeats ONE key value, while checkExternalUniquenessViolations needs TWO distinct common values sharing a combination, and constrainedByCommon keeps one entry per common value. If that is right the row is dead code with a plausible-sounding comment, which is barwise-811's shape.\n\nDo NOT close this by deleting the rows. Deleting a false-miss guard because nothing currently trips it readmits exactly what 892 and 896 fixed, on a candidate nobody has seen. Close it by either (a) constructing a candidate that expresses a reference internal uniqueness as an external one and passes through correspondence, landing it as a test -- which also settles the reachability question -- or (b) establishing unreachability and recording that, with the row replaced by whatever the reachable widening actually is.","labels":["process"],"dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-30T12:51:36Z","close_reason":"Both widenings are REACHABLE and are now pinned by packages/learn/tests/widenedGuards.test.ts, mutation-verified (dropping either guard fails exactly its own test). The issue's own reachability guess was wrong and is corrected here: it read the counterexample generator and concluded external uniqueness could not fire, because forUniqueness agrees on the constrained roles and so mints ONE entity twice. That holds only when the reference uniqueness sits on the ENTITY role. Put it on the VALUE role -- each VendorNumber belongs to at most one Vendor -- and the generator mints TWO distinct entities sharing a number, which is exactly the pair checkExternalUniquenessViolations needs. With the join's other fact type populated for both (an ordinary candidate shape: the extraction prompt asks for sample populations), the constraint fires. Disjunctive mandatory was reachable straightforwardly: a candidate requiring a number OR an alias still rules out a vendor playing neither. Neither row was deleted, which the issue forbade."}
{"_type":"issue","id":"barwise-912","title":"audit:specs covers only specs claiming NO implementation; 22 claiming partial implementation are outside it","status":"open","priority":3,"issue_type":"chore","owner":"noreply@anthropic.com","created_at":"2026-08-30T13:10:01Z","created_by":"claude","updated_at":"2026-08-30T13:10:01Z","notes":"The gate landed for barwise-910 answers one question: does a spec whose Status claims no implementation sit on top of commits that touched the source it names. That is 1 spec today (implicit-sensemaking, baselined with a reason).\n\nThe larger set is unchecked. 22 of 59 specs claim PARTIAL implementation -- 'Accepted (workstreams 1 and 2 implemented)', 'WS1 implemented 2026-07-25', 'Phases 1-3 shipped' -- and nothing verifies either half: not that the named workstreams shipped, nor that the unnamed ones did not. This is the same decay one step along, and the must_validate spec passed through this state on its way to being wrong (it went from Draft straight to needing 'workstreams 1-2 implemented; 3 not done', which a human had to write).\n\nThe strongest candidate is now evidenced rather than speculative. duplication-drift-guards.spec.md names a per-workstream issue in its Tracking line (barwise-862 W1 through barwise-872 W6). Verifying its terminal Status took one query against .beads/issues.jsonl: all six closed. If a spec's Tracking names its workstream issues, the Status is DERIVABLE from the store rather than written by hand, and the check becomes 'the Status you wrote agrees with the issues you named'. That was option (d) in barwise-910, filed as unpriced; it now has a working precedent in the corpus.\n\nCost, and why this is not a pre-merge fixup: 22 specs would need per-workstream issues retrofitted, most of which never had them, and the ones that do use inconsistent notation ('W1', 'WS1', 'workstream 1', 'Phase 1'). Spec first, per the house rule.\n\nDo not close this by widening the regex on the existing gate. Its narrowness is what makes it mechanical -- 'claims nothing was built' is checkable from the header alone, 'claims workstream 2 shipped' is not.","labels":["process"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-913","title":"Only 1 of ~10 'barwise import' subcommands has CLI-level test coverage","status":"open","priority":3,"issue_type":"chore","owner":"noreply@anthropic.com","created_at":"2026-08-30T13:25:56Z","created_by":"claude","updated_at":"2026-08-30T13:25:56Z","notes":"The import command offers model, norma, dbt, sql, typescript, directory, java, kotlin, transcript, LLM and batch. Grepping packages/cli/tests for each subcommand name finds exactly one: transcript. Nothing else exercises the CLI wiring -- option parsing, the getImporter() lookup, output handling -- so a subcommand can be broken by a rename or a registry change and only a manual run would notice.\n\nThe importers themselves ARE tested, in their owning packages (packages/formats/tests/Norma*.test.ts and friends). What is untested is the seam between the CLI and the FormatDescriptor registry, which is precisely where a connector package's registration can drift out from under a surface.\n\nFound while verifying barwise-cb6 for the barwise-912 pilot: the issue asked for an 'import norma' subcommand, the subcommand exists and works when run by hand, and nothing in CI would have told me that. Not norma-specific, which is why this is filed once rather than per subcommand.\n\nRelated shape: docs/unwired-capability-audit-2026-08-20.md and the capability matrix in CLAUDE.md, both about a surface claiming reach that nothing checks.","labels":["process"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-914","title":"Test LspJsonRpc against a fake ChildProcess","description":"packages/code-analysis excludes src/lsp/LspJsonRpc.ts and src/lsp/LspSession.impl.ts from coverage: both are constructed only inside LspManager.startSession, which spawns a real language server, so no test reaches them.\n\nLspJsonRpc is the tractable half. It takes a ChildProcess and touches only stdin/stdout/stderr plus 'exit', so a stub with two PassThrough streams exercises the whole base protocol: Content-Length framing, split and coalesced chunks, request/response correlation by id, error responses rejecting the pending promise, notification dispatch, and close() rejecting everything outstanding. That is 185 lines of protocol parsing with no test at all today.\n\nLspSession.impl is a thin typed wrapper over the transport and can follow once the transport has a fake.\n\nDoing this retires the two exclude entries in packages/code-analysis/vitest.config.ts.","status":"open","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-30T15:32:51Z","created_by":"claude","updated_at":"2026-08-30T15:32:51Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-915","title":"The Python interpreter is unpinned, and uv.lock resolves differently because of it","description":"Same shape as the Node version bug (PR #405), one lane over.\n\nbarwise/optimizer/uv.lock branches on the interpreter:\n\n    numpy 2.4.6  marker python_full_version \u003c '3.12'\n    numpy 2.5.2  marker python_full_version \u003e= '3.12'\n\nObserved on the same commit: the local venv is CPython 3.11 and holds numpy 2.4.6; CI logs 'Using CPython 3.12.3 interpreter at: /usr/bin/python3' and installs numpy 2.5.2. Both ran 'uv sync --frozen'. The lock was honoured in both and still produced two environments.\n\npyproject says requires-python = '\u003e=3.11'. Nothing pins it: no .python-version, and CI takes whatever python3 the runner ships.\n\nThe ci.yml comment beside that step already makes the argument -- '--frozen respects the committed uv.lock rather than re-resolving, which is the same reason the lock is committed: a compile costs money, so pin what produced it.' The interpreter selects which branch of the lock applies, and it is the one input not pinned.\n\nCarrier: runs-on is 'ubuntu-latest' in both workflows, so the interpreter changes with no commit when GitHub rolls the image.\n\nFix shape: a .python-version file (uv reads it natively, 'uv python pin'), CI installing from it, and the runner image pinned rather than latest.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-30T15:46:45Z","created_by":"claude","updated_at":"2026-08-30T17:40:22Z","dependency_count":0,"dependent_count":0,"comment_count":0,"notes":"Verified which version to pin, rather than assuming.\n\nPython 3.13.12, real tree, committed uv.lock, no re-lock:\n    uv sync --frozen --python 3.13 --extra dev\n    uv run --no-sync --frozen pytest -q\n    -\u003e 99 passed in 73.62s, numpy 2.5.2\n\nSo 3.13 is a zero-cost pin: the existing lock already covers it via its \u003e=3.12 branch.\n\n3.14 is UNVERIFIED, and my first read of it was wrong. uv 0.8.17 in this container can only fetch cpython-3.14.0rc2 (self-update blocked by a GitHub rate limit), and on rc2 the stack dies with\n\n    TypeError: _eval_type() got an unexpected keyword argument 'prefer_fwd_module'\n\nThat is pydantic passing a kwarg the rc2 stdlib does not yet have -- i.e. pydantic targeting 3.14 FINAL and the rc lacking it. It is not evidence that 3.14 is unsupported, and it survives --upgrade because the interpreter is the problem, not the pin. pydantic 2.13.5 carries a 'Programming Language :: Python :: 3.14' classifier and dspy 3.3.1 declares Requires-Python \u003c3.15,\u003e=3.10, so 3.14 is in range for both. Someone with a real 3.14.7 should confirm before we pin it.\n\nRecommendation: pin 3.13 now (verified, no re-lock, and it matches neither of the two environments currently in play, which is the point -- one stated version instead of two accidental ones), and open 3.14 as a follow-up rather than blocking on it.","closed_at":"2026-08-30T17:40:22Z","close_reason":"Pinned to Python 3.13 per the reviewer's call ('only ever use one python version until we make the global change to upgrade').\n\n.python-version sits beside .nvmrc at the repo root; requires-python is \u003e=3.13; ci.yml runs 'uv python install' before the sync. Re-locking collapsed the two-branch resolution -- 'Updated numpy v2.4.6, v2.5.2 -\u003e v2.5.2', and uv.lock now holds zero references to python_full_version \u003c '3.12'. Verified end to end: uv python install, uv sync --extra dev --frozen, 99 passed.\n\nOne finding worth keeping: the pin stops at the MINOR on purpose. An exact patch is not enforced by uv 0.8.17 -- with .python-version reading 3.13.12, 'uv sync' prints 'Using CPython 3.13.7' and exits 0, no warning. A declaration that reads as a guarantee and is not one is the exact class this workstream exists to close, so check:pins must assert the interpreter that actually ran rather than the file that asked for it. Recorded in CLAUDE.md and carried to barwise-920."}
{"_type":"issue","id":"barwise-916","title":"The sqlglot tier is absent in CI, and its tests vanish instead of failing","description":"sqlglot is declared in no manifest and installed by no workflow, and the tests that exercise it use it.runIf(available), so they disappear when it is missing rather than failing.\n\nCI run 33320110460, on a green suite:\n\n    formats: tests/SqlglotBridge.test.ts (8 tests | 5 skipped)   Tests 347 passed | 5 skipped\n    dbt:     tests/sql/SqlglotBridge.test.ts (6 tests | 3 skipped)  Tests 134 passed | 3 skipped\n\nSqlglotBridge.ts reads 49.13% statements in formats and 38.79% in dbt as a direct result. The real parse path -- the one that produces parseLevel 'sqlglot' rather than the regex fallback -- has never run in CI.\n\nTwo things, and the second is the larger one:\n\n1. sqlglot is unpinned. Its dialect parsing changes between releases, so even where it IS present (a developer machine) the SQL tier's output is a function of whichever version happens to be installed.\n\n2. The gate does not notice. A skipped test reads the same as a green one in the summary line. Whatever the resolution, it should fail when the tier is unexercised rather than report 347 passed -- the same lesson as barwise-850 and the llm-usage coverage note in packages/cli/CLAUDE.md.\n\nOptions: install a pinned sqlglot in CI and make the runIf unconditional there; or keep the tier optional and add a check that the sidecar tests actually ran.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-30T15:46:45Z","created_by":"claude","updated_at":"2026-09-05T19:13:53Z","dependency_count":0,"dependent_count":0,"comment_count":0,"notes":"CLOSED by workstream 2 of docs/specs/python-lockfile-execution.spec.md.\n\nThe tier now runs in CI instead of skipping. Measured before and after:\n\n  before:  formats  3 passed | 5 skipped (8)      dbt  3 passed | 3 skipped (6)\n  after:   formats  8 passed | 0 skipped (8)      dbt  6 passed | 0 skipped (6)\n\nRoot cause was two-layered and both layers are closed: sqlglot was declared nowhere (now a dependency-group in the single barwise/pyproject.toml, synced by an unconditional CI step), and the bridges shelled out to an ambient python3 with no version check (now \"uv run --frozen --only-group sqlglot\").\n\nA skipped tier is now a CI FAILURE rather than a silent shrink. Verified against a genuinely absent uv, not argued:\n  CI=1, uv absent     -\u003e Failed Suites 1, \"sqlglot tier unavailable in CI: ...\"\n  CI unset, uv absent -\u003e warning printed, 3 passed | 5 skipped\n  uv present          -\u003e 8 passed | 0 skipped\n\nFollow-ups deliberately NOT closed here: barwise-921 still owns the check:python-uv gate (workstream 3) and audit:skips plus skip-baseline.json (workstream 4). Landing those is what stops the NEXT suite from vanishing; this issue only fixes the one that had.","closed_at":"2026-09-05T19:13:53Z","close_reason":"Workstream 2 landed: sqlglot declared as a dependency-group, both bridges on uv run --frozen --only-group sqlglot, CI syncs the group unconditionally. formats 3 passed|5 skipped -\u003e 8 passed|0 skipped; dbt 3|3 -\u003e 6|0. An absent tier now fails CI rather than skipping."}
{"_type":"issue","id":"barwise-917","title":"OpenAI and Ollama default models are floating aliases, so recorded runs do not identify what produced them","description":"The Anthropic provider defaults to a dated snapshot -- 'claude-sonnet-4-5-20250929' -- so a recorded eval row names exactly what answered. The other two do not:\n\n    providers/openai.ts:43   this.model = options?.model ?? 'gpt-4o'\n    providers/ollama.ts:76   this.model = options?.model ?? 'llama3.1'\n\nBoth are mutable tags. 'gpt-4o' is re-pointed at new snapshots by OpenAI; 'llama3.1' is an Ollama tag that resolves to whatever the local daemon pulled. CompletionResponse.modelUsed records the alias, so a score history row written through either provider cannot be attributed to a specific model.\n\nThat matters more here than in most repos: promptlab exists to make prompt scores comparable across runs, and the whole artifact-resolution design (docs/specs/artifact-resolution-parity.spec.md) is built on a run being 'a pure function of barwise version, surface, provider and model'. For two of three providers the model term in that function is not a constant.\n\nAnthropic is the lane that is actually swept, so this is not urgent -- but the asymmetry is undocumented, which is the part worth closing either way.","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-30T15:46:45Z","created_by":"claude","updated_at":"2026-09-06T01:27:03Z","dependency_count":0,"dependent_count":0,"comment_count":0,"notes":"Closed by workstream 2 of docs/specs/deterministic-guards.spec.md.\n\nThe five modelUsed: this.model sites now read the provider's answer (response.model ?? this.model). Ollama's OllamaChatChunk had to be widened to declare model at all -- the server sends it, the type made it unreadable -- and the value there is still the operator's tag rather than a digest, so this closes the finding for Anthropic and OpenAI and narrows it for Ollama.\n\nTHE SPEC MISSED THE HALF THAT MATTERED. toHistoryEntry built HistoryEntry.model from the CLI --model flag, so a provider reporting a resolved snapshot changed nothing about the recorded row. It now prefers what the runs reported (report.cases[].runs[].modelUsed) and REFUSES to name one when they disagree: a row whose calls were answered by different snapshots is described by no single identifier, and picking one would be a false record where the alias is at least true.\n\nAlso corrected in the spec: its acceptance names ScoreRecord.model, and there is no ScoreRecord type. The recorded row is HistoryEntry.\n\nVerification: seven tests (four on toHistoryEntry, three pairs across the providers), each watched red under its own mutation -- reverting to this.model reddens exactly the three provider attribution tests; reverting the disagreement rule to 'first observed' reddens exactly the two disagreement tests.\n\nSTILL UNVERIFIED: whether the Anthropic API resolves an alias or echoes it back. That needs one live paid call. The change is correct either way, and no alias-echo warning was built, because building one on an unverified fact would pin a guess as behaviour.","closed_at":"2026-09-06T01:27:03Z","close_reason":"Workstream 2 of deterministic-guards.spec.md: providers report the model that answered, and toHistoryEntry records it instead of the requested alias."}
{"_type":"issue","id":"barwise-918","title":"Scripts should resolve paths from the repo root, not the cwd","description":"Every path bug this workstream hit was a cwd bug.\n\nCANDIDATE_DIR in eval-runner.sh defaulted to ../optimizer/out, which resolved outside the repo. That was fixed to optimizer/out -- still cwd-relative, so running the script from the repo root rather than barwise/ breaks it again the same way. The value was fixed; the class was not.\n\nCurrent state:\n\n- The 17 scripts/*.mjs already root themselves from import.meta.url, in three different idioms (const root = ..., const ROOT = ..., const here = ...). They resolve to barwise/, not the repo root.\n- No script uses git rev-parse --show-toplevel.\n- The shell scripts (eval-runner.sh, compile-runner.sh, scripts/*.sh) do not root themselves at all. eval-runner.sh sets 'set -euo pipefail' and then uses bare relative paths.\n\nProposal: one shared way to say 'the repo root', and every script path anchored to it.\n\ngit rev-parse --show-toplevel is the right primitive and has a property worth stating: it reports the REAL path, following symlinks. That is what packages/cli/src/commands/history.ts needed realpathSync for -- on macOS tmpdir() is /var/folders/..., a symlink to /private/var/folders/..., and comparing a resolve()d path against the git root silently produced a truncated relative path. Anything adopting this convention must resolve its own side with realpath too, or it reintroduces that bug.\n\nOne caveat against making it the only mechanism: rev-parse needs git on PATH and a repository. import.meta.url does not. A shared helper can prefer rev-parse and fall back, so a tarball install or a container without git still works.","status":"open","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-30T16:08:33Z","created_by":"claude","updated_at":"2026-08-30T16:14:31Z","dependency_count":0,"dependent_count":0,"comment_count":0,"notes":"SCOPE CONSTRAINT -- this convention is dev-time only, and the codebase already documents why.\n\npackages/cli/src/workspace/provenance.ts hit this exact trap and solved it:\n\n  'Asking git about the current directory would answer about whatever repo\n   the operator happened to be standing in -- their model repo, say -- and\n   record its commit as though it were barwise's. A globally installed CLI\n   sitting in some project node_modules has the same problem. Both produce\n   a confidently wrong SHA, which is worse than none, so the repo is checked\n   for barwise own marker before its commit is believed.'\n\nSo git rev-parse --show-toplevel is not a general rule. In a deployed context it is actively dangerous: it does not fail, it answers about SOMEBODY ELSE'S repository. A globally installed barwise run inside a user's project would resolve 'repo root' to their project, and a tarball install outside any repo has no answer at all.\n\nThe boundary, which follows the repo's existing layering:\n\n- DEV-TIME (scripts/*.mjs, *.sh, the runners, CI helpers): rev-parse is right. These only ever run inside a barwise checkout, and cwd-independence is the whole point.\n- RUNTIME (anything shipped in a package): keep the provenance pattern -- search from process.argv[1], verify barwise's own package.json marker before believing the root. Never ask about the cwd, and never trust an unverified git root.\n\nAlso note packages/cli/CLAUDE.md's standing rule that shared command code must not reach for import.meta at all, because the esbuild CJS bundle leaves it empty. Any shared helper has to work under both builds.\n\nThe deliverable is therefore two helpers with a documented boundary, not one rule applied everywhere -- and the check, if we add one, should enforce the boundary rather than the primitive."}
{"_type":"issue","id":"barwise-919","title":"Version pins are duplicated across files with almost nothing checking they agree","description":"Companion to barwise-915, which is about version inputs that are unpinned. This is the opposite failure: inputs pinned in more than one place, free to disagree.\n\nDEMONSTRATED, and it was ours: package-lock.json embeds a copy of the root package's engines field. Commit 0f8bdaa raised barwise/package.json to '\u003e=26.0.0' and left the lock at '\u003e=20.0.0'. The two disagreed, all 25 gates passed, and CI was green. Fixed in ba5c974 by regenerating the lock.\n\nUnguarded copies as of today:\n\n1. The Node version now lives in three places -- .nvmrc (26.7.0), barwise/package.json engines.node (\u003e=26.0.0), and package-lock.json engines.node. Only the third is derived (npm writes it). Nothing checks .nvmrc against engines, so bumping one and not the other is silent.\n2. The single monorepo version is written 13 times (12 packages plus the root), all currently 1.7.0. Exactly one pair is guarded: packages/mcp/tests/server.test.ts asserts SERVER_VERSION matches its own package.json. The other twelve are held together by the release procedure and nothing else.\n\nAlready fine, recorded so nobody re-audits them:\n\n- uv.lock's requires-python is derived by uv from pyproject.toml.\n- eval-runner.sh maps haiku/sonnet to claude-haiku-4-5 and claude-sonnet-5, and the artifact registry keys on modelPrefix with startsWith ('claude-haiku', 'claude-sonnet-5'). Prefix matching is what decouples them; this pair is safe BY DESIGN, not by accident, and the design is worth preserving.\n\nThis is squarely the convention in CLAUDE.md: 'A copy that must agree with other code is never guarded by a comment: share it, derive it from the authority, register the pair in parity.manifest.json, or give it a drift test.' parity.manifest.json checks file and symbol copies byte-for-byte; it has no notion of a version field inside a JSON or dotfile. Either extend it to field-level pairs, or fold both this and barwise-915 into one check:pins gate.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-08-30T16:08:33Z","created_by":"claude","updated_at":"2026-08-30T17:31:07Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-08-30T17:31:07Z","close_reason":"Closed by workstream 1 of docs/specs/deterministic-guards.spec.md. check-parity.mjs takes a {file,field} member; parity.manifest.json registers node-engines (package.json vs the lockfile's embedded copy) and monorepo-version (13 files, one of which was previously guarded). Shown red on three planted defects and covered by four tests in scripts/tests/gates.test.mjs, themselves verified red."}
{"_type":"issue","id":"barwise-920","title":"Spec: deterministic guards for the four version/skip/pin findings","description":"docs/specs/deterministic-guards.spec.md turns barwise-915, -916, -917 and -919 from findings into mechanisms: field-level members in parity.manifest.json, an audit:skips baseline, recording the model the provider reports, and check:pins.\n\nFour open decisions need a reviewer call before workstream 1 starts: whether to install sqlglot in CI as well as declaring it; what to do if a provider echoes an alias back; whether Ollama is worth closing at all; and whether check:pins covers action refs.\n\nWorkstream 4 is marked provisional and consumes barwise-915's pin, so it lands last.","status":"open","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-08-30T17:04:58Z","created_by":"claude","updated_at":"2026-08-30T17:04:58Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-921","title":"Every Python execution must go through uv; six call sites use bare python3","description":"Standing project rule (CLAUDE.md, Conventions): every Python execution goes through uv -- uv run / uv sync / uv python -- never a bare python3, python, or pip.\n\nThe rule is the operational half of the .python-version pin. The pin binds only what uv selects; a bare python3 runs the ambient interpreter with ambient site-packages, escaping both the pinned minor and the locked dependency set. That is the numpy 2.4.6-vs-2.5.2 divergence barwise-915 closed, reintroduced one subprocess lower.\n\nNon-compliant sites as of 0a3e7dc:\n\n1. packages/formats/src/sql/SqlglotBridge.ts:161,200,257 -- execFileSync(\"python3\", ...)\n2. packages/dbt/src/sql/SqlglotBridge.ts:163,202,259 -- identical; registered parity pair, so both change together or the pair breaks\n3. compile-runner.sh:158 -- python3 - \"${OUT}/report.json\" \u003c\u003cPYEOF\n4. scripts/check-beads.sh:29,30 -- command -v python3, then python3 - \"${f}\" \u003c\u003cPY\n5. .github/workflows/ci.yml:218 -- python3 -m pip install --quiet uv (bootstrapping uv itself; the one standing exception)\n\nVerified mechanism for the SqlglotBridge case: 'uv run --only-group sqlglot node probe.mjs' puts the managed venv on PATH such that a grandchild bare python3 resolves into it (FOUND: sqlglot 27.28.0 at .venv/bin/python3; bare node: ModuleNotFoundError). So the bridges can be left calling python3 IF every entry point that reaches them runs under uv run -- but that is an invariant nothing checks, which is the weaker option.","acceptance_criteria":"A gate (npm run check:python-uv, wired into ci:local and ci.yml) reads tracked source, scripts and workflows and FAILS on any of:\n  - a bare python3 / python / pip invocation\n  - uv run --with, --with-requirements, or --isolated\n  - uv pip\n  - a uv run without --frozen (dev/runtime) or --locked (CI)\n  - PEP 723 inline script metadata (a '# /// script' block) declaring dependencies\nAllowlist: exactly one entry, the uv bootstrap in .github/workflows/ci.yml, with its reason inline. The gate is shown red on a planted violation of each banned form before acceptance, and red on a stale allowlist entry (the ratchet property audit-rubric.mjs documents), so removing a violation forces its row out.\n\nThe six non-bootstrap call sites (both SqlglotBridge copies, compile-runner.sh, check-beads.sh) run under 'uv run --frozen --only-group sqlglot' where they need sqlglot, or plain 'uv run --frozen' where they only need the interpreter. sqlglot is declared once, as a dependency-group in the single pyproject.toml; no version string appears in either bridge.\n\nAccepted behaviour change, stated in the bridge docblock: with no provisioned project, sqlglotAvailable() is false and callers degrade to the regex cascade, where today they would silently use whatever sqlglot the ambient interpreter carries.","status":"open","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-09-05T17:28:55Z","created_by":"claude","updated_at":"2026-09-06T01:27:03Z","notes":"WORKSTREAM 4 DEFERRED BY THE USER (2026-09-06), and a new measurement changes what it should be.\n\nThe framing recorded above -- \"producing a JSON reporter means 12 identical edits\" -- is WRONG, and the correction matters:\n\n  cd packages/formats \u0026\u0026 npx vitest run --reporter=json tests/SqlglotBridge.test.ts\n  -\u003e \"numTotalTests\":8 \"numPassedTests\":8 \"numPendingTests\":0\n\nThe reporter works when INVOKED directly. No package.json edit, no shared base config, no 12-way must-agree copy. deterministic-guards.spec.md workstream 3 had already verified this; what got lost was that it verified it by invoking the flag ad hoc, not by requiring the 12 scripts to emit one. So options (a) and (b) were both answering a problem that does not exist.\n\nBut the same measurement kills the runtime approach for a better reason. That file reported 8 passed / 0 pending here, and 3 passed / 5 skipped earlier in the same session on the same commit. The only difference is whether sqlglot happens to be provisioned. A baseline of RUNTIME skip counts is therefore environment-dependent: it would be a flaky gate, not a ratchet, and it would fail on a developer machine without uv while passing in CI -- the exact local-vs-CI split this whole spec exists to close.\n\nStatic constructs (.skip / .todo / runIf / skipIf, grepped from tracked test sources) do not move with the environment. That is option (c), and it is now the recommendation on evidence rather than on cost.\n\nRevised options for whoever picks this up:\n(c) Ratchet static skip constructs. Environment-independent. Stated gap: a purely runtime skip is not caught.\n(d) Runtime counts via a directly-invoked --reporter=json. Catches runtime skips, but the baseline moves with provisioning (measured: 8/0 vs 3/5) and costs a second suite run against this spec's own no-added-time constraint.\n(c+d) Static ratchet as the portable gate, plus a CI-ONLY assertion that the sqlglot tier reports zero pending -- CI is where provisioning is guaranteed, so the runtime half is stable exactly there.\n\nOptions (a) and (b) from the earlier note are withdrawn: both existed only to serve a reporter that needs no such plumbing.","labels":["python","uv","gates","tooling"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-922","title":"PR creation and PR review skills: the author-side and reviewer-side protocol for barwise PRs","description":"Two skills under .claude/skills/: pr-creation (readiness gate, cutting the diff for reading, the reading guide, house title and body shape) and pr-review (read order, claim verification, findings form, posting rules). The barwise invariants CI cannot check live once, in pr-review/checklist.md, and pr-creation treats that file as its output specification. Spec: docs/specs/pr-skills.spec.md.","acceptance_criteria":"Both SKILL.md files exist with frontmatter; checklist.md is the single owner of the judgment items; CLAUDE.md points at both skills; docs-only CI path green.","status":"closed","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-09-05T20:00:46Z","created_by":"claude","updated_at":"2026-09-05T20:50:39Z","dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-09-05T20:50:39Z","close_reason":"Landed in PR #411: pr-creation and pr-review skills, the shared checklist, one eval each, and docs/specs/pr-skills.spec.md."}
{"_type":"issue","id":"barwise-923","title":"hashModel() is non-deterministic for constraints without explicit ids","description":"hashModel() (packages/core/src/lineage/manifest.ts) re-serializes a loaded model to YAML and hashes the text to detect drift for 'barwise lineage status'. Constraints without an explicit id in the source .orm.yaml get a fresh random UUID assigned on every loadModel() call. Two independent loads of the exact same file therefore produce different serialized YAML (different constraint ids) and hash differently, even though nothing about the model actually changed.","acceptance_criteria":"Loading the same .orm.yaml file (with at least one constraint lacking an explicit id) twice and calling hashModel() on both loaded models produces the same hash. Add a regression test in packages/core covering this case.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-09-06T00:55:32Z","created_by":"claude","updated_at":"2026-09-06T13:32:44Z","design":"Likely fix: derive a stable id for id-less constraints deterministically from their content/position (matching whatever loadModel() does for other id-less elements, if anything already does this), rather than a fresh random UUID per load. Alternatively, hashModel() could canonicalize/strip constraint ids before hashing when they were not present in source, so hashing is insensitive to this synthesized value.","notes":"Discovered by an agent working on packages/cli/tests/lineage.test.ts while closing coverage gaps for the vitest 3.2.7 -\u003e 4.1.11 dependabot bump (PR #415). The agent worked around it in cli's own tests with a fixture that pins all constraint ids, but the underlying bug in core is unfixed. Existing lineage.test.ts reportedly already has a comment noting the 'all fresh' status case was 'tricky' to test -- this bug is likely why.","labels":["core","lineage","bug"],"dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-09-06T13:32:44Z","close_reason":"Merged in PR #422 (main e53f6ab): hashModel canonicalizes ids by first appearance before hashing (packages/core/src/lineage/manifest.ts); tests/lineage/manifest.test.ts pins two loads of learning-design.orm.yaml hashing equal and a rename changing the hash."}
{"_type":"issue","id":"barwise-924","title":"Design review: core's branching load vs. the intended functional/rich-types design","description":"Closing core's vitest-4 coverage gap (PR #415, dependabot vitest 3.2.7 -\u003e 4.1.11 bump) took by far the longest of any package's remediation and touched ~20 files across validation, verbalization, mapping, and project splitting. The coverage-v8 4.x engine's more granular branch counting exposed just how much conditional branching core actually carries -- dozens of files sitting well below full branch coverage purely from if/ternary/??/?. density, not from missing tests alone. This sits in tension with core's intended design: a functional core passing rich types around should often be able to replace branching with exhaustive type discrimination (discriminated unions + type guards, which core already uses for constraints) rather than runtime conditionals. Revisit whether core's current branching load is inherent to the domain (ORM 2 has a lot of real conditional rules) or is absorbing complexity that a richer type design would push to the type system instead.","acceptance_criteria":"A spec (docs/specs/) documenting: which core modules have branching that is domain-inherent vs. representation-driven, and a recommendation (redesign vs. leave as-is) for the representation-driven cases. No code change required by this issue itself -- it produces the design spec that a follow-up implementation issue would use.","status":"closed","priority":2,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-09-06T01:12:38Z","created_by":"claude","updated_at":"2026-09-06T14:52:05Z","design":"A design review, not a rewrite: (1) inventory where core's branch density is highest (the vitest-4 coverage run's per-file branch counts are a ready-made heat map -- validation/rules/*, verbalization/constraints/phase1.ts and phase2.ts, mapping/RelationalMapper.ts, project/splitModel.ts stood out); (2) for each hot spot, ask whether the branching encodes a real domain rule (keep it, make sure it's well-tested) or is compensating for a weakly-typed intermediate representation (candidate for a richer type / exhaustive discriminated union); (3) compare against the DomainModel.principles.md primary principles (orthogonality, composability) and note where a type-level redesign would reduce change amplification per Ousterhout's terms already used in CLAUDE.md.","notes":"Design review written 2026-09-06: docs/specs/core-branching-load.spec.md. Method: coverage-v8 branch heat map over core (3,195 branches, 127 files) plus six parallel read-only classification passes over the eight densest clusters (2,739 branches). Finding: about half inherent, half representation-driven with one cause (unchecked id references, optional fields as invariants, erased intermediates). Four live defects and one gap found and filed: barwise-927 (merge drops six fields, P1), -928 (split keeps cross-domain join constraints), -929 (no value_comparison consistency check), -930 (six ring types on a default arm), -931 (composite PK truncation, latent), -932 (object-type cardinality has no modality). Closes on merge of the spec PR.","labels":["core","design","architecture"],"dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-09-06T14:52:05Z","close_reason":"Merged in PR #423 (main 46bd110): docs/specs/core-branching-load.spec.md classifies core's 3,195 branches by cluster (about half domain-inherent, half representation-driven with one cause), inventories every affected file with a verdict, and recommends the sealed-record metamodel with a builder and derived graph as WS1-WS8; the five defects it found are filed as barwise-927..931 and the gap as 932. The ReScript follow-on is barwise-933."}
{"_type":"issue","id":"barwise-925","title":"Tracker status drifts from its own notes: issues whose PR merged stay open for months","description":"barwise-r4f, barwise-vli and barwise-5dd carried 'Shipped in PR #236/#237' in their notes from 2026-06-21 and were still in_progress on 2026-09-06; barwise-5d7 and barwise-899 were the same shape with different wording. The steward rule (section 5) closes issues in a tracker-only commit after the code PR merges, and that follow-up is a step no gate checks: the PR body says 'closure follows' and nothing verifies it did. A sweep by regex over notes found one more (barwise-897) and three false positives ('Implemented (PR pending)' on 5t9.5/.7/.12, whose importer halves are genuinely open).","acceptance_criteria":"check-beads.sh --strict fails on a non-closed issue whose notes contain 'Shipped|Landed|Merged in PR #N'; scripts/tests/gates.test.mjs pins it red on a planted line and green on the closed and 'PR pending' forms; the live tracker passes.","status":"closed","priority":2,"issue_type":"chore","owner":"noreply@anthropic.com","created_at":"2026-09-06T02:18:12Z","created_by":"claude","updated_at":"2026-09-06T13:28:39Z","notes":"Landed with the PR that files this issue; closure follows its merge.","labels":["process","gate-coverage"],"dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-09-06T13:28:39Z","close_reason":"Merged in PR #421 (main 553498a): check-beads.sh --strict errors on a non-closed issue whose notes say Shipped/Landed/Merged in PR #N; scripts/tests/gates.test.mjs pins it red on a planted open line and green on the closed and 'Implemented (PR pending)' forms; live tracker reads 0 errors under --strict."}
{"_type":"issue","id":"barwise-926","title":"Closed six tracker issues on their own notes before checking primary evidence","description":"Session 2026-09-06: I closed barwise-r4f, -vli, -5dd, -5d7 and -899 because each issue's notes said the work shipped, and only verified against primary evidence (merged PR state on GitHub, the code, the test suites, the archived spec) after the user asked to ground the claims first. All five held up, and the check then stopped three more closures that would have been wrong: barwise-5t9.5/.7/.12 say 'Implemented (PR pending)' but their acceptance criteria include a NORMA importer half their notes say is deferred. Same class as barwise-906 (a green reading accepted without establishing that the instrument could read red): an issue's notes are the author's claim, not evidence of the acceptance criteria. Nothing caught it because 'close' takes any --reason string.","acceptance_criteria":"Decision recorded: whether beads-crud close should require the reason to cite evidence (a PR number, a spec Status line, or a test command), or whether the verification skill text is the landing. Until then this is the ledger entry that makes the next occurrence a repeat.","status":"open","priority":2,"issue_type":"chore","owner":"noreply@anthropic.com","created_at":"2026-09-06T02:18:12Z","created_by":"claude","updated_at":"2026-09-06T02:18:12Z","labels":["process","verification"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-927","title":"ModelMerge drops independent, defaultValue, note, cardinality (object types) and note, derivation (fact types) on every merge","description":"packages/core/src/diff/ModelMerge.ts rebuilds object types from five hand-copied nine-field literals (lines 62-141) and fact types in addFactTypeToMerged (224-231). None of the literals carry independent, defaultValue, note or cardinality; the fact-type copy omits note and derivation. grep -c for those four names in ModelMerge.ts is 0. An UNCHANGED object type loses them through 'barwise merge', and the diff reports 'cardinality changed' / 'derivation changed' for changes the merge cannot carry. Found by the barwise-924 design review (docs/specs/core-branching-load.spec.md, diff cluster); the root cause is per-field copying with no shared field list.","acceptance_criteria":"A merge of a model whose object types carry independent/defaultValue/note/cardinality and whose fact types carry note/derivation preserves all six on the unchanged elements and carries them for accepted incoming ones; a test names each field; the fix goes through ObjectType.toConfig()/FactType.toConfig() (or the field table barwise-924 proposes) rather than a sixth hand-copied literal.","status":"closed","priority":1,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-09-06T03:17:16Z","created_by":"claude","updated_at":"2026-09-06T15:30:49Z","design":"docs/specs/core-branching-load.spec.md, WS0","labels":["core","diff"],"dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-09-06T15:30:49Z","close_reason":"Merged in PR #426 (main b86dd91): ModelMerge routes every object-type reconstruction site and addFactTypeToMerged through toObjectTypeConfig()/toFactTypeConfig(), one function per element kind listing every config field once, so independent/defaultValue/note/cardinality (object types) and note/derivation (fact types) survive a merge instead of being dropped by the prior hand-copied literals."}
{"_type":"issue","id":"barwise-928","title":"splitModel keeps cross-domain join constraints: constraintRoleIds does not know join constraints hold roles in operands","description":"packages/core/src/project/splitModel.ts constraintRoleIds (558-591) walks eight hand-listed keys (roles, subset_roles, superset_roles, role_1, ...) on a RawConstraint. Join subset/equality/exclusion constraints keep their roles in operands[].path.steps and projection, none of which is listed (grep for 'operands' in the file: none), so constraintRoleIds returns [] for them, constraintIsLocal returns true, and a join constraint spanning two domains is silently kept in one of them. Found by the barwise-924 design review (serialization/project cluster).","acceptance_criteria":"Splitting a model with a join constraint whose operands reach roles in two domains drops the constraint with the existing cross-domain warning; the role walk is an exhaustive switch over the typed Constraint union (as elementDiff.constraintTypeKey already does), so a new constraint kind fails compilation rather than being silently local.","status":"closed","priority":2,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-09-06T03:17:16Z","created_by":"claude","updated_at":"2026-09-06T15:38:02Z","design":"docs/specs/core-branching-load.spec.md, WS0 / WS8","labels":["core","project"],"dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-09-06T15:38:02Z","close_reason":"Merged in PR #428 (main ed12d76): splitModel's constraintRoleIds replaced the eight-key raw-field walk with an exhaustive switch over the typed Constraint union, fed by the existing yaml constraint codec (deserializeConstraint), so join constraints (join_subset/join_equality/join_exclusion) whose operand paths reach roles in another domain are now dropped with the existing cross-domain warning instead of silently kept."}
{"_type":"issue","id":"barwise-929","title":"constraintConsistency has no value_comparison arm and no assertNever, so roleId1/roleId2 are never checked against the fact type","description":"packages/core/src/validation/rules/constraintConsistency.ts dispatches on constraint.type with no default: assertNever (grep: none), and value_comparison is absent, so nothing verifies that a value-comparison constraint's two role ids belong to the fact type it is attached to. Found by the barwise-924 design review (validation cluster).","acceptance_criteria":"A value_comparison constraint whose roleId1 or roleId2 is not a role of its fact type produces a constraint/* error; the switch ends in default: assertNever(constraint) so the next constraint kind fails to compile until it is classified.","status":"closed","priority":3,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-09-06T03:17:16Z","created_by":"claude","updated_at":"2026-09-06T15:41:04Z","design":"docs/specs/core-branching-load.spec.md, WS1","labels":["core","validation"],"dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-09-06T15:41:04Z","close_reason":"Merged in PR #429 (main ad609cf): constraintConsistencyRules now validates value_comparison's roleId1/roleId2 against its fact type (constraint/value-comparison-invalid-role) and ends in default: assertNever(constraint), with the three join constraint kinds as explicit no-op arms since their consistency is validated separately in joinConstraintRules.ts."}
{"_type":"issue","id":"barwise-930","title":"Ring verbalization handles two of eight ring types explicitly; the other six fall to a generic default arm","description":"packages/core/src/verbalization/constraints/phase2.ts switches on ringType with explicit cases only for irreflexive and asymmetric (202, 212) and a default at 226 that renders '\u003cLabel\u003e: X pred X.' for the remaining six (antisymmetric, intransitive, acyclic, symmetric, transitive, purely_reflexive). The parameter is typed string rather than RingType, so the switch cannot be exhaustive, and the test at Phase2 ... :497 pins '!=' for the operator, a value the model type forbids. Found by the barwise-924 design review (verbalization cluster).","acceptance_criteria":"verbalizeRing takes RingType, the switch is exhaustive with one sentence shape per ring type (FORML wording per Halpin), and a test asserts each of the eight.","status":"closed","priority":3,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-09-06T03:17:16Z","created_by":"claude","updated_at":"2026-09-06T15:50:17Z","design":"docs/specs/core-branching-load.spec.md, WS2","labels":["core","verbalization"],"dependency_count":0,"dependent_count":0,"comment_count":0,"closed_at":"2026-09-06T15:50:17Z","close_reason":"Merged in PR #432 (main fddc320): verbalizeRing now takes RingType and is exhaustive (default: assertNever(ringType)), with one FORML-style sentence per ring type replacing the generic default-arm rendering for antisymmetric, symmetric, transitive, intransitive, acyclic and purely_reflexive -- the spec's flagged deliberate golden change."}
{"_type":"issue","id":"barwise-931","title":"RelationalMapper takes primaryKey.columnNames[0]! at five sites; a composite PK is silently truncated to its first column","description":"packages/core/src/mapping/RelationalMapper.ts lines 296, 341, 390, 391, 448 read primaryKey.columnNames[0]! and then re-find the column by name with a '?? \"TEXT\"' fallback. PrimaryKey holds names, not columns. Step 2b (line 101) replaces an objectified entity's PK, so a later subtype FK (390) can reference one column of what is now a composite key. Latent: no test builds that shape. Found by the barwise-924 design review (mapping cluster).","acceptance_criteria":"A test with an objectified entity used as a supertype either shows the truncation (then fix: PrimaryKey carries Column[] and FKs reference all of them) or shows the shape is unreachable (then the [0]! sites are replaced by a non-optional accessor and the finding is closed as not-a-bug with the reason).","status":"open","priority":3,"issue_type":"bug","owner":"noreply@anthropic.com","created_at":"2026-09-06T03:17:17Z","created_by":"claude","updated_at":"2026-09-06T03:17:17Z","design":"docs/specs/core-branching-load.spec.md, WS6","labels":["core","mapping"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-932","title":"Object-type cardinality has no modality, so its population rule hardcodes severity error","description":"packages/core/src/validation/rules/population/cardinality.ts lines 27 and 36 emit severity 'error' unconditionally because ObjectType.cardinality carries no modality, while every other constraint maps deontic to warning through severityForModality. A deontic population bound on an object type ('there should be at most 50 Departments') cannot be expressed. Found by the barwise-924 design review (validation cluster).","acceptance_criteria":"ObjectType.cardinality can carry modality (serializer round-trip, schema, NORMA mapping), and the population rule uses severityForModality like the unary-role form.","status":"open","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-09-06T03:17:17Z","created_by":"claude","updated_at":"2026-09-06T03:17:17Z","labels":["core","validation","metamodel"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"barwise-933","title":"Experiment: @barwise/core in ReScript on a long-lived branch, measured against the TypeScript sealed-record metamodel","description":"The core design review (barwise-924, docs/specs/core-branching-load.spec.md) asks TypeScript to enforce by convention what an ML-family language enforces by compiler: sealed sums with exhaustive matching, immutable records, structural equality. ReScript compiles to JavaScript, so it can satisfy the runtime constraint (Node extension host, CLI, MCP server, no serialization boundary between core and its consumers). This issue tracks a measured experiment on a long-lived branch: a ReScript implementation of the sealed-record metamodel, builder and graph, run against the same test suite and golden corpus as the TypeScript implementation, with the decision criteria written down before the branch starts. Spec: docs/specs/core-in-rescript.spec.md.","acceptance_criteria":"The spec's decision criteria are answered with measurements from the branch, and a recommendation (adopt, adopt for core only, or stop) is recorded in the spec's Implementation notes; the branch either merges or is closed with the record.","status":"open","priority":3,"issue_type":"task","owner":"noreply@anthropic.com","created_at":"2026-09-06T14:42:20Z","created_by":"claude","updated_at":"2026-09-06T14:42:20Z","design":"See docs/specs/core-in-rescript.spec.md.","labels":["design","experiment"],"dependency_count":0,"dependent_count":0,"comment_count":0}
