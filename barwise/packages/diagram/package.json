{
  "name": "@barwise/diagram",
  "version": "1.5.0",
  "private": true,
  "description": "ORM diagram layout and SVG rendering (platform-independent)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./theme": {
      "types": "./dist/render/theme.d.ts",
      "default": "./dist/render/theme.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src/ tests/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@barwise/core": "1.5.0",
    "elkjs": "^0.9.3"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "@vitest/coverage-v8": "^3.2.4",
    "vitest": "^3.0.0"
  }
}
