# Room.avsc
{
  "type": "record",
  "name": "Room",
  "fields": [
    {
      "name": "room_id",
      "type": "string",
      "doc": "Primary key [TODO(barwise): No column description. Add one to the dbt YAML.] [TODO(barwise): Data type defaulted to TEXT. Add a data type to the ORM value type or edit the dbt YAML.]"
    },
    {
      "name": "building_code",
      "type": [
        "null",
        "string"
      ],
      "doc": "[TODO(barwise): No column description. Add one to the dbt YAML.] [TODO(barwise): Data type defaulted to TEXT. Add a data type to the ORM value type or edit the dbt YAML.]"
    },
    {
      "name": "room_number",
      "type": [
        "null",
        "string"
      ],
      "doc": "[TODO(barwise): No column description. Add one to the dbt YAML.] [TODO(barwise): Data type defaulted to TEXT. Add a data type to the ORM value type or edit the dbt YAML.]"
    }
  ],
  "doc": "[TODO(barwise): No model description. Add a definition to the ORM entity type or edit the dbt YAML.]"
}

---

# Building.avsc
{
  "type": "record",
  "name": "Building",
  "fields": [
    {
      "name": "building_code",
      "type": "string",
      "doc": "Primary key [TODO(barwise): No column description. Add one to the dbt YAML.] [TODO(barwise): Data type defaulted to TEXT. Add a data type to the ORM value type or edit the dbt YAML.]"
    }
  ],
  "doc": "[TODO(barwise): No model description. Add a definition to the ORM entity type or edit the dbt YAML.]"
}
