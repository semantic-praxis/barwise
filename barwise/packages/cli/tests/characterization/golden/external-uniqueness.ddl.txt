-- Table: room
-- Source: Room (ot-room)
-- TODO(barwise): No model description. Add a definition to the entity type.
CREATE TABLE room (
  -- TODO(barwise): No column description. Add a definition to the value type.
  -- TODO(barwise): Data type was not declared; exported as TEXT. Add a data type to the value type.
  room_id TEXT NOT NULL,
  -- TODO(barwise): No column description. Add a definition to the value type.
  -- TODO(barwise): Data type was not declared; exported as TEXT. Add a data type to the value type.
  building_code TEXT,
  -- TODO(barwise): No column description. Add a definition to the value type.
  -- TODO(barwise): Data type was not declared; exported as TEXT. Add a data type to the value type.
  room_number TEXT,
  PRIMARY KEY (room_id),
  FOREIGN KEY (building_code) REFERENCES building (building_code),
  UNIQUE (building_code, room_number)
);

-- Table: building
-- Source: Building (ot-building)
-- TODO(barwise): No model description. Add a definition to the entity type.
CREATE TABLE building (
  -- TODO(barwise): No column description. Add a definition to the value type.
  -- TODO(barwise): Data type was not declared; exported as TEXT. Add a data type to the value type.
  building_code TEXT NOT NULL,
  PRIMARY KEY (building_code)
);
