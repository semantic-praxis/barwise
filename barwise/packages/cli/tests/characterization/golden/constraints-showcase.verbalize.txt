Person drives Car
Car is driven by Person
  Each Person drives at most one Car.
  Each Person either drives some Person or r-person-rides some r-person-rides but not both.
Person rides Bus
Bus is ridden by Person
  Each Person rides at most one Bus.
Person purchases Ticket
Ticket is purchased by Person
  Each Ticket is purchased by at most one Person.
  Each Ticket is purchased by at least one Person.
  If Person then r-person-rides.
Person is parent of Person
Person is child of Person
  Each combination of Person and Person is unique in Person is parent of Person.
  No Person is parent of that same Person.
  If Person1 is parent of Person2 then Person2 does not is parent of Person1.
