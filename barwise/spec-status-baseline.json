{
  "$comment": "Specs whose Status claims no implementation while commits have landed on the source files they name. Each row needs a note saying why the header is still right, or what it should say instead. Ratcheted by `npm run audit:specs -- --check` (scripts/audit-spec-status.mjs).",
  "specs": {
    "ci-local-condition-parity.spec.md": {
      "status": "Draft -- no workstream implemented",
      "note": "Genuinely unimplemented, and the header is right: this spec's deliverable is teaching ci-local.mjs to read the `if:` conditions on ci.yml's steps and skip the gates CI would skip, and it still reads none -- the gate list is derived exactly as before and all 28 run. The commit below is barwise-960 giving the same script a lock, a run-scoped coverage directory, and full failure logs; it changes how a run is isolated and reported, not which gates it selects. This is the proxy false positive the gate's own header describes: two findings on one script. Remove this row when the condition filter ships and the Status says so.",
      "commits": [
        "0ab1495"
      ]
    },
    "conformance-property.spec.md": {
      "status": "Accepted (all decisions resolved 2026-08-25; both workstreams open -- design only, no implementation in this PR)",
      "note": "Genuinely unimplemented, and the header is right. Both workstreams are property-based testing (an extraction-response arbitrary, then parser totality over generated responses) and nothing of the sort exists: fast-check is not a dependency of any package, and the only 'arbitrary' in llm or promptlab is a comment in an unrelated test. The four commits below are unrelated work on files it names -- a dependabot bump, suite 2.5.0, the assertNever sweep, and the npm-audit gate. Remove this row when workstream 1 ships and the Status says so. Caught only after the gate's pattern was fixed to see the claim mid-line; it had escaped on position alone (barwise-912 class-C pass).",
      "commits": [
        "c14cb71",
        "0a79c25",
        "369f041",
        "1c524f6"
      ]
    },
    "eval-difficulty-calibration.spec.md": {
      "status": "Draft for review (design only -- no transcript authored in this PR)",
      "note": "Genuinely still a draft, and the header is right: this spec's deliverable is authored transcripts for the eval suite, and none exists (promptlab's cases are unchanged since it was written). The commit below is barwise-935 refactoring CounterexampleGenerator.ts, which this spec only CITES as a place ring constraints surface; it authors nothing this spec plans. This is the proxy false positive the gate's own header describes -- a spec naming a file that unrelated work touches. Remove this row when the first transcript lands and the Status says so.",
      "commits": [
        "39092b1"
      ]
    },
    "implicit-sensemaking.spec.md": {
      "status": "draft",
      "note": "Genuinely still a draft; the commits below are unrelated work on modules the spec happens to name. 8a62161 is model-lint's four workstreams touching populationValidation.ts; 8175c7c moved mcp output-size guidance, touching verbalize.ts. Neither implements anything here -- no rival-frame or premortem code exists in any package's src. The spec's own Tracking line says it files bd issues per workstream WHEN IT LANDS, so nothing has landed to report. Remove this row when the first workstream ships and the Status says so.",
      "commits": [
        "8a62161",
        "8175c7c"
      ]
    },
    "functional-design-quality.spec.md": {
      "status": "Draft -- the five open decisions resolved in review 2026-09-09\n(see Decisions); no workstream implemented",
      "note": "Genuinely unimplemented, and the header is right. This spec's deliverables are five detectors -- invariant, escaping mutation, throw, law, api -- each with a baseline that ratchets. None exists: there is no detector script, no baseline file, and check-core-purity.mjs still checks exactly the four things it checked before (no I/O, clock, randomness, or LLM SDK). The commit below changed how that script RESOLVES THE REPOSITORY ROOT -- it now imports the guarded REPO_ROOT from lib/tracked.mjs instead of running its own git rev-parse, so it refuses rather than crashing when git answers emptily (barwise-989 WS2, docs/specs/gate-refusal-contract.spec.md). That is the gate-refusal contract reaching a file this spec happens to name; it measures nothing this spec plans. Same proxy false positive the gate's own header describes. Remove this row when the first detector ships and the Status says so.",
      "commits": [
        "58122dc1"
      ]
    }
  }
}
